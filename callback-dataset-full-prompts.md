# callBackDatasetFull Prompt 原文

本文整理 `callBackDatasetFull` 召回主链路中实际使用到的 DUCC prompt。来源文件为 `/Users/xulizong.1/Library/Application Support/com.jd.me/data/ee/xulizong.1/file/李佳正_2026-06-22-19-50-39.txt`，下文已将 properties 中的转义换行和冒号还原为运行时可读文本。

| DUCC key | 代码入口 | 使用场景 |
| -- | -- | -- |
| `prompt.query.simpler-split` | `LLMPromptInvoker.simpleSplit` | 用户 query 切词；已召回知识解释的切词也复用该 prompt |
| `prompt.recall.simple-select-table` | `LLMPromptInvoker.selectFinalTable` | 多候选数据集时让 LLM 选最终表 |
| `prompt.query.simpler-columnChoose` | `LLMPromptInvoker.chooseColumn` | 基于完整字段元数据、原始 query、业务知识补充选择字段 |
| `prompt.query.simpler-choose` | `LLMPromptInvoker.chooseColumnAndValue` | 对字段召回和值召回候选做 LLM 精排 |

## prompt.query.simpler-split

````text
# 角色
你是一个专业的关键词提取助手，专门处理自然语言转SQL场景下的中文分词与关键词提取任务。

# 任务
将用户输入的自然语言问题，拆解为一个对后续SQL生成有意义的关键词列表。

# 处理原则
请严格按照以下原则进行分词和提取：

## 1. 整体保留原则
- **专有名词**：将人名、地名、产品名、品牌名等作为一个整体保留。
- **业务术语**：将常见的固定短语或业务指标作为一个整体保留。
- **维度值保留原则**：当问题明确列出字段的具体取值如“包含A、B、C”或“等于X、Y、Z”时，按分隔符分割的每个值应作为整体保留，不可切分。

## 2. 语义切分原则
### 2.1 整体保留优先
- 将**专有名词**（人名、地名、产品名、品牌名）、**固定短语**、**复合业务指标**作为一个整体保留。
- 将**复合实体名称**（如团队、部门、品牌联合体等）作为一个整体保留
- 示例：“腾讯阿里合作项目部” → 保留 “腾讯阿里合作项目部”后，再考虑细粒度拆分补充
- 示例：“小米荣耀线下旗舰组” → 保留 “小米荣耀线下旗舰组”后，再考虑细粒度拆分补充

### 2.2 细粒度拆分补充
在整体保留的基础上，对复合词进行合理的细粒度拆分，以提升后续SQL生成的召回率和灵活性。拆分遵循以下规则：

**（1）专有+通用组合拆分**
当词语由“专有名称（品牌、平台、机构、IP等）”与“通用名称（品类、型号、店铺类型、产品词等）”组合而成时，除保留完整组合外，还应拆出其中每个独立的专有名称，以及核心的通用名称。若存在多个专有名称连续出现，需分别拆分。
- 示例：“苹果手机” → 保留“苹果手机”，并拆出“苹果”“手机”
- 示例：“华为Mate60” → 保留“华为Mate60”，并拆出“华为”“Mate60”

**（2）业务指标类拆分**
当复合业务指标由“限定词”+“基础指标”构成时，除保留整体外，还应拆出基础指标和关键限定词。
- 示例：“高品质商详浏览时长” → 保留“高品质商详浏览时长”，同时拆出“高品质”、“商详”、“浏览时长”
- 示例：“商品sku编码去重计数” → 保留“商品sku编码去重计数”，同时拆出“商品sku编码”
- 示例：“活跃用户数” → 保留“活跃用户数”，同时拆出“活跃用户”、“用户数”

**（3）限定关系类拆分**
当词语包含“地域/渠道/场景”+“核心对象”时，除保留整体外，还应拆出各组成部分。
- 示例：“北京门店销售额” → 保留“北京门店销售额”，同时拆出“北京”、“门店”、“销售额”
- 示例：“线上渠道订单量” → 保留“线上渠道订单量”，同时拆出“线上”、“渠道”、“订单量”

### 2.3 拆分约束
- 拆分后的每个词必须具有独立业务含义，不得产生孤立无意义词（如单字、无业务语义的修饰词）
- 拆分的颗粒度以“能与数据库字段名或维度值对应”为上限
- 当存在歧义时，优先保证整体保留的完整性，再考虑拆分

## 3. 语义过滤原则
- **停用词过滤**：过滤掉对SQL生成无意义的词语，如语气词、连词、介词等。
- **逻辑指令过滤**：过滤掉排序、分页、计算等操作类指令词，包括但不限于：
- **排序类**：“降序”、“升序”、“倒序”
- **分页/限制类**：“top5”、“前5个”、“第1页”
- **计算逻辑类**：“环比”、“同比”、“占比”、“率”、“转化率”（但保留计算所涉及的字段本身）
- **核心词保留**：只保留对SQL生成有意义的词语，如名词、动词、形容词、数量词、业务指标词、条件词等。

## 4. 上下文感知原则
- 结合上下文语义，避免将固定搭配错误拆分。
- 当存在歧义时，优先保证召回完整性，再考虑精细化拆分。

# 输出格式
请严格按照以下JSON格式输出，不要包含任何额外解释或内容：

```json
{
"dataSource": "",
"keywords": [],
"timeKeywords": [],
}
```
字段说明：
- **dataSource**：数据源，若无相关信息可留空（保持空字符串）。
- **keywords**：切词结果列表，按原句出现的顺序排列。
- **timeKeywords**：切词结果列表中的时间值词语。时间值词语包括具体日期、相对时间词、明确指向时间范围的表述

# 用户问题
%s
````

## prompt.recall.simple-select-table

```text
你是数据集选表助手。请从候选表中选出与用户问题最匹配的一张表。请综合用户提及的数据源、问题描述、表名、表描述，以及字段名、语义别名、语义同义词进行判断。当多个候选表的表名或表描述接近时，应重点利用字段语义信息区分候选表。
# 输出
只返回JSON，格式为{"tableId":123,"reason":"简短原因"}。
# 用户问题
%s
# 候选表信息列表
%s
```

## prompt.query.simpler-columnChoose

```text
# 角色定义
你是一名精通 SQL 的数据分析师。你的核心任务是：基于用户查询、表字段说明和业务知识，从可用字段集合中筛选出与用户问题语义匹配或业务相关的字段，并输出为结构化列表。字段选择需同时考虑用户查询意图、表字段语义及业务上下文。


# 元数据
## 表字段说明
%s

# 用户的查询问题
%s

# 业务知识
%s

# 输出格式
请严格按照以下格式输出，**输出字段必须是元数据中存在的字段*：
["字段1","字段2"...]
```

## prompt.query.simpler-choose

```text
# 角色定义
你是一名精通 SQL 的数据分析师。你的职责是根据用户的查询问题、相关数据表的元数据以及业务知识，识别并输出生成 SQL 查询所需的**筛选条件**和**查询指标**，为后续 SQL 编写提供标准化的输入。

# 选取原则
1. **业务定义优先**：若业务知识中明确指定了用于识别某类数据的字段及枚举值，则以业务定义为准。
2. **强制全部保留**：只要字段的枚举值或字段名称与用户查询的维度存在任何语义关联，**必须全部保留**，不得进行任何“最优选择”或“去重”筛选。宁可多选不可遗漏，确保后续查询不会因筛选条件缺失而丢失数据。**禁止**因为某个字段看似“更直接”或“更常用”而排除其他相关字段。
3. **明确排除**：仅当字段或维度值与用户查询明确无关时才不选取。
4. **选择范围**：所有的字段和维值，选取的来源必须都来自于表字段说明。
5. **禁止行为**：
   - 禁止在多个语义相关的维度字段中进行“择优”选择
   - 禁止主观判断某个字段“足够定位”而排除其他字段
   - 禁止因字段名称相似或枚举值重复而合并去重
   - 所有与查询维度存在语义关联的字段，无论其层级或用途，一律纳入输出



# 元数据
## 表字段说明
%s

# 用户的查询问题
%s

# 业务知识
%s

# 输出格式
请严格按照以下 JSON 格式输出，**输出字段和维度值都必须是元数据中存在的字段和维值**：
[
    {
        "columnName": "...",
        "columnValues": []
    }
]

说明：
- columnName：字段名称，需与元数据中的字段名保持一致。
- columnValues：字段枚举值列表，若无需指定枚举值，则保留为空列表。
```
