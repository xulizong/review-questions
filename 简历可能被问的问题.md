<!-- BEGIN GENERATED TOC -->
**目录**

- [Summary 后置裁判（node_judge_agent + judge_tool）面试问题](#section-01)
  - [问题：这套后置裁判的整体架构是什么？为什么要拆成两个角色？](#q-01-01)
  - [问题：node_judge_agent 为什么被明确禁止直接输出 pass/score？](#q-01-02)
  - [问题：动态规则（rubric）和静态规则有什么区别？为什么一定要动态生成？](#q-01-03)
  - [问题：eval_cot（压缩思维链）为什么关键？](#q-01-04)
  - [问题：为什么 rubric 里要写"只允许局部修复"？它解决什么问题？](#q-01-05)
  - [问题：NEGATIVE_CONSTRAINTS（负向禁止）为什么必要？"坏规则"会造成什么？](#q-01-06)
  - [问题：judge_tool 返回的 score 有什么用？为什么下游只看 pass？](#q-01-07)
  - [问题：pass=false 之后的重试闭环是怎样的？会不会死循环？](#q-01-08)
  - [问题：引入 judge + memory 后延迟增加了，值得吗？](#q-01-09)
  - [问题：这套设计还有哪些可改进点？](#q-01-10)
- [取数 Agent 的记忆机制（MemoryWorkspaceReactAgent）](#section-02)
  - [问题：这套记忆机制整体是什么？解决什么问题？](#q-02-01)
  - [问题：react_memory 和 compressed_memory 有什么区别？](#q-02-02)
  - [问题：记忆是怎么存的？存在哪？](#q-02-03)
  - [问题：下一轮怎么把记忆取回来？检索范围是什么？](#q-02-04)
  - [问题：为什么"最近一轮用原始 query，更早的轮次才用压缩"？](#q-02-05)
  - [问题：高级记忆模式（加权 + token 预算）是怎么管理上下文的？](#q-02-06)
  - [问题：memory_prompt（压缩提示词）有哪些关键约束？为什么这么设计？](#q-02-07)
  - [问题：压缩失败或异常怎么处理？](#q-02-08)
  - [问题：为什么只有 Text 取数 Agent 有 compressed_memory，别的节点没有？](#q-02-09)
  - [问题：这套记忆和向量数据库 / RAG 记忆有什么区别？](#q-02-10)
  - [问题：MemoryID 标注是干什么的？怎么支撑"历史去重/复用"？](#q-02-11)
  - [问题：引入记忆压缩的延迟代价大吗？](#q-02-12)
  - [问题：这套记忆机制还有哪些可改进点？](#q-02-13)
- [text 取数 Agent 的细节](#section-03)
  - [问题：上一轮取到的数据，下一轮能直接复用吗？](#q-03-01)
  - [问题：每一次取到的数据大概是什么内容？summary 从哪里拿数据总结？](#q-03-02)
  - [问题：Text 取数 Agent 每轮发给 LLM 的完整 prompt（full_memory）长什么样？](#q-03-03)
  - [问题：Text 取数 Agent 如何利用前缀缓存的？](#q-03-04)
- [评测与基准测试（Judge 链路 + 记忆压缩）](#section-04)
  - [问题：这类模块（LLM 裁判 / 记忆压缩）的测试集应该怎么设计？](#q-04-01)
  - [问题：judge 裁判的"判得准不准"用什么指标评？](#q-04-02)
  - [问题：记忆压缩的质量用什么指标评？](#q-04-03)
  - [问题：性能（延迟 / token）这类开销指标，评测方法上有什么讲究？](#q-04-04)
  - [问题：评测结果出来后，怎么读、怎么判断"效果到底行不行"？](#q-04-05)
  - [问题：评测这类 LLM 模块时有哪些容易踩的坑？](#q-04-06)
- [/retrieve 独立召回检索接口面试问题](#section-05)
  - [问题：/retrieve 这条召回链路整体是怎么走的？](#q-05-01)
  - [问题：为什么用 RRF，而不是把 BM25 和向量的分数直接加权相加？](#q-05-02)
  - [问题："两阶段 RRF"具体指什么？两次融合分别融了什么？](#q-05-03)
  - [问题：RRF 里的 rank constant k 是什么？为什么常用 60？](#q-05-04)
  - [问题：BM25 和向量两路是并行发起的吗？](#q-05-05)
  - [问题：向量检索用的是 ANN 还是 KNN？](#q-05-06)
  - [问题：精确匹配是怎么做的？为什么 score 直接置 1？](#q-05-07)
  - [问题：reranker 放在哪一步？送进去的候选量怎么控？](#q-05-08)
  - [问题：为什么可以选用不同的 reranker 模型（Qwen3 / BGE）？两者优劣是什么？](#q-05-09)
  - [问题：query 的 embedding 有缓存吗？缓存怎么设计的？](#q-05-10)
  - [问题：这个接口做了哪些提升效率的设计？](#q-05-11)
  - [问题：index 参数是必传的吗？拿不到怎么降级？](#q-05-12)
  - [问题：这条链路怎么评测？为什么能说明效果提升？](#q-05-13)
  - [问题：这条链路还有哪些可改进点？](#q-05-14)
- [callBackDatasetFull 数据集召回引擎面试问题](#section-06)
  - [问题：这个召回引擎解决的核心问题是什么？为什么要做多路并行召回？](#q-06-01)
  - [问题：整条链路分哪几个阶段？](#q-06-02)
  - [问题：能用一个完整例子把callBackDatasetFull从切词到返回串起来吗？](#q-06-03)
  - [问题：第一步切词在主链路中的位置是什么？为什么能和候选数据准备并行？](#q-06-04)
  - [问题：切词阶段给LLM什么输入？返回的数据结构是什么？](#q-06-05)
  - [问题：切词Prompt具体是怎么设计的？哪些词会保留、拆分或过滤？](#q-06-06)
  - [问题：为什么同一个query要并行切两次？两次结果如何合并？](#q-06-07)
  - [问题：时间词是怎么处理的？它真的不会进入字段和值召回吗？](#q-06-08)
  - [问题：切词时如何判断一个词是指标、维度字段还是维值？](#q-06-09)
  - [问题：切词调用失败时怎么降级？有哪些边界情况？](#q-06-10)
  - [问题：第二步是不是直接根据切词结果选择表？](#q-06-11)
  - [问题：如果候选列表里只有一张表，还会走向量和LLM选表吗？](#q-06-12)
  - [问题：表名规则匹配具体怎么做？会用切出来的所有关键词吗？](#q-06-13)
  - [问题：名称规则无法唯一选表时，表向量召回怎么做？](#q-06-14)
  - [问题：LLM选表的Prompt、输入和模型是什么？](#q-06-15)
  - [问题：LLM返回异常时一定会回退到向量第一名吗？](#q-06-16)
  - [问题：选出最终表后，系统还要做什么？](#q-06-17)
  - [问题：选表后业务知识是怎么召回的？规则和向量结果如何合并？](#q-06-18)
  - [问题：召回到的知识如何影响字段和维值？它会与字段直接绑定吗？](#q-06-19)
  - [问题：知识召回完成后，字段和值的召回关键词怎么准备？两路谁依赖谁？](#q-06-20)
  - [问题：字段召回分为哪几路？原名、别名和同义词如何匹配？](#q-06-21)
  - [问题：值召回必须先确定字段吗？基础维值、自定义维值和维度中心有什么区别？](#q-06-22)
  - [问题：字段和值候选如何过滤？一路失败会不会导致整体失败？](#q-06-23)
  - [问题：多路召回的结果怎么合并去重？命中同一字段的多个来源怎么处理？](#q-06-24)
  - [问题：为什么用向量召回而不是纯关键词/ES匹配？同义词、别名、维值为什么要拆成不同向量空间？](#q-06-25)
  - [问题：dimCode和dimValue的区别是什么？为什么需要"名称→编码"解析？](#q-06-26)
  - [问题：LLM精排是怎么实现的？为什么不用cross-encoder重排模型？](#q-06-27)
  - [问题：全链路并行怎么编排？并发写的地方为什么用了那么多并发工具，是不是冗余？](#q-06-28)
  - [问题：外部依赖（Vearch、LLM、维度中心）挂了怎么办？](#q-06-29)
  - [问题：96%的准确率具体怎么定义和统计？为什么能到这么高？](#q-06-30)
  - [问题：这套召回怎么评测？整体评测方案是什么？](#q-06-31)
  - [问题：评测集怎么设计？](#q-06-32)
  - [问题：怎么判断一次评测算成功/通过？](#q-06-33)
  - [问题：召回结果没有唯一标准答案（一个词可能对应多个合理字段）怎么评测？](#q-06-34)
  - [问题：有没有用自动化评测/LLM as judge？怎么保证judge本身可靠？](#q-06-35)
  - [问题：评测怎么形成闭环、持续迭代？离线好但线上不好怎么排查？](#q-06-36)
  - [问题：这套设计的局限和演进方向是什么？](#q-06-37)
- [C-side 存储知识相关问题](#section-07)
  - [问题：C-side 用哪几张表存数据集知识？各自字段和用途是什么？](#q-07-01)
  - [问题：这些内容分别写到哪张表，又进入哪个 Vearch space？](#q-07-02)
  - [问题：Vearch 里只存向量吗？会不会保存原始文本？](#q-07-03)
- [Tree 探索相关问题](#section-08)
  - [问题：Tree 执行模式到底解决什么问题？和 Planner-Executor 是什么关系？](#q-08-01)
  - [问题：整条 Tree 执行链路是怎么走的？从入口到出结果](#q-08-02)
  - [问题：树里有哪几种节点？各自特点是什么？](#q-08-03)
  - [问题：执行的时候，是把整段树 JSON 一次性丢给 LLM 让它自己决定调用顺序吗？](#q-08-04)
  - [问题：面试重点——为什么用 tree，而不是把 graph 计划存下来复用？](#q-08-05)
  - [问题：Tree_Adjuster 是干什么的？为什么执行前要先"调整"树？](#q-08-06)
  - [问题：并行分支之间会互相污染上下文吗？TreeContextExecutor 怎么做隔离的？](#q-08-07)
  - [问题：subtree / parallel / fan_out 会展开一堆内部节点，前端怎么不被刷屏？](#q-08-08)
  - [问题：tree_master 挂的两个 tool 分别是干什么的？](#q-08-09)
  - [问题：树里 condition 节点匹配 query 时用的是相似度检索吗？](#q-08-10)
  - [问题：每个 Agent 都能拿到用户原始 query 吗？数据是怎么在链路里传递的？](#q-08-11)
  - [问题：一个 compute 节点执行失败了，整棵树会挂吗？失败是怎么兜底的？](#q-08-12)
  - [问题：fan_out 动态扇出的并发是怎么控制的？会不会打爆下游？](#q-08-13)
  - [问题：这套 Tree 执行机制还有哪些可改进点 / 演进方向？](#q-08-14)
  - [问题：Tree_Adjuster 用 LLM 改写树结构，怎么保证改出来的树一定能跑？](#q-08-15)
  - [问题：短记忆（short_memory）在树模式里是怎么起作用的？为什么是"历史"而不是当前轮？](#q-08-16)
  - [问题：文档是如何切分的？](#q-08-17)
<!-- END GENERATED TOC -->

<a id="section-01"></a>

# Summary 后置裁判（node_judge_agent + judge_tool）面试问题

> 背景：这是 Text 取数链路里的"结果后置质检"模块。上游 Agent 生成一份数据分析 Summary（可能上万字），
> 在返回用户前先过一道"裁判"，判断结论口径、单位、业务对齐等是否合规，不合规就打回做一次局部修复。
> 整个模块由两个角色组成：`node_judge_agent`（出题人，生成规则）与 `judge_tool`（阅卷人，给判定）。

---

<a id="q-01-01"></a>

## 问题：这套后置裁判的整体架构是什么？为什么要拆成两个角色？

### 简短回答

它是"出题人 + 阅卷人"的两阶段设计：

- **node_judge_agent**：读完 Summary 后**不打分**，而是产出一份"本次任务专属的动态检查规则（rubric）"和一份"压缩后的评估证据（eval_cot）"，以工具调用的形式输出。
- **judge_tool**：拿到 rubric + eval_cot，才真正给出 `{pass, score, reason, violations}` 的判定。

中间由 `llm_response_parser` 把 node_judge_agent 产出的**动态规则**拼上一套固定的**静态规则**，再调用 judge_tool（见 [llm_response_parser.py:661-668](applications/data_process/util/llm_response_parser.py:661)）。

### 为什么拆开

1. **职责分离，防止走捷径**：如果让同一个模型既定规则又打分，它很容易跳过"列出可核查规则"这一步，直接凭印象给个结论。拆开后，出题人必须先把"这次到底查什么"写成可勾选的清单，判定才有依据。
2. **成本分层**：出题是重推理任务（基准里约 14.3s、约 6.6 万 prompt tokens），判定是轻量聚焦任务（约 9.3s、不到 1900 tokens）。拆开后各用各的算力，判定环节不必再背负一遍长上下文。
3. **可控可观测**：规则和证据被显式落到结构化字段里，事后能看到"这次是拿什么标准、基于什么证据判的"。

---

<a id="q-01-02"></a>

## 问题：node_judge_agent 为什么被明确禁止直接输出 pass/score？

因为出题人不能兼任阅卷人。评分权只留给 judge_tool，这样做有三个好处：

1. **强制产出可核查的 rubric**：不许直接给结论，模型就只能老老实实把检查项写清楚。
2. **保证输出可被程序派发**：它的唯一合法输出是严格 JSON 的工具调用
   `{"tool_name":"judge_tool","arguments":{"rubric":..., "eval_cot":...}}`，
   parser 靠它 `oxy_request.call(callee="judge_tool")` 去派发。一旦输出成 Markdown、```json 代码块或带前后缀文本，JSON 解析就崩，整个裁判步骤失效。
3. **角色边界清晰**：出题、拼接静态规则、判定，三段各司其职，便于分别优化和排障。

---

<a id="q-01-03"></a>

## 问题：动态规则（rubric）和静态规则有什么区别？为什么一定要动态生成？

- **静态规则**：一套通用硬规则，由 parser 固定拼在 rubric 后面，所有任务共用。
- **动态规则**：node_judge_agent 针对**这一次** Summary 现场生成的增量约束。

必须动态的原因是：取数报告千变万化（六区 GMV、家群增长、秒杀……），通用规则只能查"数据要准、格式要规范"这种正确的废话，抓不住每次的具体错误。动态规则要求精确到本次的部门、SKU、时间窗、指标口径，才能抓到"华东的销售被配到了华北区域""TOP3 增量没到 100 万门槛"这类任务专属违规。

**动态规则的四维业务对齐**（prompt 强约束）：

| 维度 | 含义 | 不做的后果 |
| --- | --- | --- |
| 业务对象 | 精确到部门/类目/SKU/渠道/平台缩写 | 抓不到跨区域、跨对象的张冠李戴 |
| 时间范围 | 精确到昨日/上周/去年同期 | 抓不到时间错位 |
| 指标口径 | 率值/金额/同比环比/PP 差值 | 抓不到口径混用 |
| 输出形态 | 表格/图表/模板填空/禁止发散 | 抓不到模板漏填、答非所问 |

---

<a id="q-01-04"></a>

## 问题：eval_cot（压缩思维链）为什么关键？

因为 **judge_tool 判定时只能看到 rubric 和 eval_cot，看不到原始完整上下文**。这决定了裁判是"基于证据判"还是"基于想象判"。

所以 eval_cot 有两条硬要求：

1. **结论数值 + 支撑它的中间数据一起保留**：只留结论不留中间过程，judge_tool 就得靠心算去验证，一心算就幻觉。
2. **证据不足必须显式标注"缺失"**：某结论无法由现有中间数据推出时要点明，禁止下游自行脑补一条计算链路硬判对错。

这是第二阶段的抗幻觉命脉。

---

<a id="q-01-05"></a>

## 问题：为什么 rubric 里要写"只允许局部修复"？它解决什么问题？

要先理解闭环：**judge_tool 判 `pass=false` 不是报错了事，而是触发一次"打回重做"**。parser 会拼一段"上轮已被拦截，请进入修复状态"的通知，把 `violations` 塞进 `<AI_JUDGE_SUGGESTION>`，作为 ERROR_PARSE 重试信号回灌给**生成 Summary 的 Agent**（见 [llm_response_parser.py:87-116](applications/data_process/util/llm_response_parser.py:87)）。

问题在于 LLM 被要求"重做"时的默认行为是**从头重写整篇**。设想一份三万字报告只有一处单位错了（`0.04` 该写成 `0.04pp` 却写成 `0.04%`），若整篇推倒重来：

1. 重写时可能弄丢原本正确的表格/数据（信息损耗）；
2. 生成有随机性，改对这处却可能在别处**引入新错误**；
3. 新版本再被判 `pass=false`，在"改 A 坏 B"之间**来回震荡**，收敛不了，白烧延迟和 token。

"只允许局部修复"就是把"回炉重造"降级成"外科手术式打补丁"——只动被点名处，其余原文逐字保留。这个约束是**双保险**：既写进 rubric（作为常驻契约传给 judge_tool），又写进重试通知（[llm_response_parser.py:109](applications/data_process/util/llm_response_parser.py:109)）。

---

<a id="q-01-06"></a>

## 问题：NEGATIVE_CONSTRAINTS（负向禁止）为什么必要？"坏规则"会造成什么？

关键链条：node_judge 写的每条规则都会进 rubric → judge_tool 拿 Summary 逐条比对 → 只要"违反"就 `pass=false` → 触发重做循环。**所以一条假的、无法满足的、或超出修复能力的规则，会让 judge_tool 反复把一份本来合格的 Summary 判成违规。这就是"坏规则 → 误杀"。**

逐条禁令对应一类坏规则：

| 禁令 | 若不禁，会产生的坏规则 | 危害 |
| --- | --- | --- |
| 禁止校验 SQL 覆盖性 | "报告必须含完整 3 天数据"（但 SQL 只返回 2 天） | 后置环节无法重新查数，陷入永远修不好的死循环 |
| 禁止校验文件存在性 | "确认文件 X 已成功读取" | Summary 无从自证、judge 无从验证，凭空判违规 |
| 禁止额外重格式化数值 | "所有金额保留两位小数"（用户没要求） | 逼模型重排数字，引入原本不存在的四舍五入误差 |
| 禁止把 summary_tool 当业务工具 | "应调用 summary_tool" | 它是终点节点，此规则逻辑上就荒谬 |
| 禁止复用静态规则 | 抄一遍静态硬规则 | 挤占 token；改写后与静态原文口径冲突，给 judge 塞进自相矛盾指令 |

**本质**：node_judge 是个 LLM，天生爱生成"看起来很周全"的清单，里面混着大量看似合理、实则无法核查/无法满足/超出权限的规则。在纯二值门控下，每条这样的规则都是地雷。NEGATIVE_CONSTRAINTS 就是在出题阶段提前拆雷。

---

<a id="q-01-07"></a>

## 问题：judge_tool 返回的 score 有什么用？为什么下游只看 pass？

真正驱动流程的只有 `pass`。消费端只做 `pass_flag = bool(judge_tool_output.get("pass", False))`（见 [llm_response_parser.py:671](applications/data_process/util/llm_response_parser.py:671)），然后二分：通过就继续，不通过就把整份 `judge_output` 当反馈打回。**全链路没有任何一处读取 score，也没有阈值门槛。**

- **score 的角色**：旁路观测信号，跟着 JSON 落到日志/失败记录里，方便人事后看"判得多严"，但不参与决策。
- **有行动价值的三样**：`pass` 决定"过 or 重来"、`violations` 告诉 Agent"违反了哪几条"、`reason` 给整体说明。

**这套设计的局限**：纯二值门控本质是"一票否决"，score 是 2 还是 8 都一样处理。想做"轻微问题放行、严重问题才拦截"目前做不到，需要改成消费端读 score 并设阈值。

---

<a id="q-01-08"></a>

## 问题：pass=false 之后的重试闭环是怎样的？会不会死循环？

不会。这套裁判**最多只跑一轮**，用一个计数器兜底防死循环（见 [llm_response_parser.py:164-176](applications/data_process/util/llm_response_parser.py:164)）：

1. 第一次判定：`not_pass_judge` 从 0 置 1，若 `pass=false`，返回 ERROR_PARSE 重试信号，带着 `violations` 打回让 Summary Agent 局部修复。
2. 修复后的 Summary 再回来时，因 `not_pass_judge >= 1`，**裁判直接跳过、放行**，不再二次评判。

也就是说：判一次、给一次反馈、然后信任这次修复。这既避免了无界震荡，也把裁判引入的额外成本**锁定为最多一次 Summary 重生成**，延迟代价可控。

---

<a id="q-01-09"></a>

## 问题：引入 judge + memory 后延迟增加了，值得吗？

这是准确性与延迟的权衡。基准数据（大 payload，n=30）：

- 整体：p50≈21.7s、p95≈36.8s、avg≈23.6s。
- 其中 node_judge_agent（出题）avg≈14.3s、约 6.6 万 prompt tokens；judge_tool（判定）avg≈9.3s、约 1900 tokens。

代价换来的是：对单位误用、模板漏填、跨区域张冠李戴、门槛未达标等**高频且影响正确性**的错误做一道自动质检并局部修复。由于裁判最多跑一轮、修复只准打补丁，延迟是有上界的、可预期的，对"结果正确性优先于极致速度"的取数报告场景是划算的。

---

<a id="q-01-10"></a>

## 问题：这套设计还有哪些可改进点？

1. **score 接入门控**：目前 score 白算了。可让消费端按 score 分级——高分轻微问题放行、低分严重问题才拦截，减少误杀。
2. **多轮修复而非一轮**：现在为防死循环只判一轮，代价是修复后的版本不再复检。可加"最多 N 轮 + 每轮必须收敛"策略，在防震荡和保质量间取更好平衡。
3. **规则质量的元校验**：可对 node_judge 产出的 rubric 再做一层轻量校验，自动过滤掉违反 NEGATIVE_CONSTRAINTS 的规则，而不是完全依赖 prompt 约束。
4. **证据充分性打标**：把 eval_cot 里"证据不足"的标注结构化，让 judge_tool 对缺证据的结论只提示不拦截，避免因上下文压缩不全导致的误判。

---
---

<a id="section-02"></a>

# 取数 Agent 的记忆机制（MemoryWorkspaceReactAgent）

> 背景：Text 取数 Agent 是一个多轮、可调工具的 ReAct Agent。为了让后续轮次能"记住"前面查过什么、
> 定了什么口径，又不至于把历史原文全塞进上下文，它内置了一套"短期工作记忆"：每轮结束把该轮问答
> 压缩成两句话存进 ES，下一轮按需还原。这套机制和向量库/RAG 的语义检索是两码事。

---

<a id="q-02-01"></a>

## 问题：这套记忆机制整体是什么？解决什么问题？

它是一套**按会话+trace 作用域的短期工作记忆**，核心解决"多轮对话里上下文越滚越长"的问题。

一轮对话结束（模型给出最终答案）时，Agent 会把"本轮 User 提问 + Assistant 最终答案"送给一个廉价的
`memory_llm`，压缩成 `{query, answer}` 两句话（见 [memory_workspace_react_agent.py:570-580](oxygent/oxy/agents/memory_workspace_react_agent.py:570)）。
下一轮启动时，`_get_history` 从 ES 捞回最近若干轮的记忆，拼进上下文再喂给主模型。

这样后续轮次读到的历史是"两句话/轮"的浓缩版，而不是每轮上万字的原文，既保住了跨轮续接所需的关键事实
（日期、口径、查过的结果），又把上下文长度和 token 成本压住。

---

<a id="q-02-02"></a>

## 问题：react_memory 和 compressed_memory 有什么区别？

这是两层粒度完全不同的记忆，一轮对话会同时产出：

| | react_memory | compressed_memory |
| --- | --- | --- |
| 内容 | 本轮 ReAct 的**完整轨迹**：每一步思考、工具调用、工具返回 | 本轮最终问答压成的 `{query, answer}` **两句话** |
| 粒度 | 步骤级，很长 | 一轮一句，极短 |
| 产出 | ReAct 循环里逐步累积 | 本轮结束时调 memory_llm 压缩得到 |
| 用途 | 高级记忆模式下按权重择要召回 | 简单模式下作为历史轮次的浓缩上下文 |

在 `extra` 里两者一起返回（[memory_workspace_react_agent.py:578-579](oxygent/oxy/agents/memory_workspace_react_agent.py:578)）；压缩失败时只返回 react_memory。

---

<a id="q-02-03"></a>

## 问题：记忆是怎么存的？存在哪？

存进 ES 的 `<app>_history` 索引（如 `ge-oxy-mas-pro_history`）。落库逻辑在
[base_agent.py:199-220](oxygent/oxy/agents/base_agent.py:199)：先组一个 `history = {query, answer}`，再 `history.update(oxy_response.extra)`
把 react_memory / compressed_memory 并进去，最后整体 `to_json` 塞进文档的 `memory` 字段。

一条 history 文档的字段很少：`history_id`、`session_name`、`trace_id`、`memory`（一段 JSON 字符串）、`create_time`。
**注意这个索引里没有向量**，全是文本——记忆是靠 trace/session 精确过滤取回的，不是靠语义相似度。

---

<a id="q-02-04"></a>

## 问题：下一轮怎么把记忆取回来？检索范围是什么？

`_get_history` 对 `<app>_history` 做一次 ES 查询（[memory_workspace_react_agent.py:159-178](oxygent/oxy/agents/memory_workspace_react_agent.py:159)），
用两个条件做**双重过滤**：

1. `trace_id` ∈ `root_trace_ids + current_trace_id`——限定在同一条对话血缘链上；
2. `session_name` == 当前节点的会话名（如 `process_expert__Text取数Agent`）——限定是同一个角色的历史。

再按 `create_time` 倒序、取 `short_memory_size` 条。所以记忆是**"同一会话、同一 trace 谱系"作用域**的，
不会串到别的用户、别的节点。这也是为什么之前在 ES 里只有 Text 取数节点那条带 `compressed_memory`——
过滤本身就把范围锁死了。

---

<a id="q-02-05"></a>

## 问题：为什么"最近一轮用原始 query，更早的轮次才用压缩"？

这是简单模式（`is_discard_react_memory`）里一个很关键的设计（[memory_workspace_react_agent.py:211-227](oxygent/oxy/agents/memory_workspace_react_agent.py:211)）：

- **最近一轮**：直接用原始 `query`/`answer`，不压缩。因为它离当前问题最近、最可能被追问和引用，需要全保真。
- **更早的轮次**：用 `compressed_memory` 的两句话，且每条打上 `【MemoryID: xxx】` 标签。因为它们相关性递减，
  用浓缩版即可，把 token 省下来。

一句话概括：**近处保真、远处压缩**——用信息保真度换 token，越远越激进。这正是"为什么要做压缩"的落点：
不是为了压缩而压缩，而是给"够不着的历史"提供一个低成本的可读版本。

---

<a id="q-02-06"></a>

## 问题：高级记忆模式（加权 + token 预算）是怎么管理上下文的？

当不丢弃 react_memory 时走高级模式（[memory_workspace_react_agent.py:228-292](oxygent/oxy/agents/memory_workspace_react_agent.py:228)），本质是一个"带预算的择要"：

1. 把所有 QA 摊平成候选列表：短记忆的 query/answer 一类、react_memory 拆块后一类，各带来源标记。
2. 给每条打分：`func_map_memory_order(位置) × 权重`，短记忆和 react 记忆用不同权重（`weight_short_memory` / `weight_react_memory`），
   位置越靠后（越新）分越高——即"近因加权"。
3. 按分数从高到低选，累加字符数，一旦超过 `memory_max_tokens` 就停——**token 预算裁剪**。
4. 最后按对话原有顺序重建，保证 user/assistant 交替不错乱。

所以高级模式是"压缩 + 加权 + 预算"三件套：先有压缩降体积，再按重要性排序，最后用硬预算封顶。

---

<a id="q-02-07"></a>

## 问题：memory_prompt（压缩提示词）有哪些关键约束？为什么这么设计？

它是记忆的"写入器"，四个设计支点：

1. **固定可解析 schema**：只输出 `{query, answer}`、禁止 Markdown/代码块。因为下游 `json.loads` 直接解析、
   还原代码按固定字段名取值，格式一乱就解析失败触发重试或丢记忆。
2. **激进但对关键事实无损的压缩**：明确列出必须保留（日期、快照版本、筛选条件、SKU、排名、关键数值、
   工具调用意图、历史复用）与可省略（礼貌语、表格具体行、字段清单、格式描述）。保留的恰是"下一轮续接必需"的，
   丢了会导致下一轮重问或用错口径。
3. **忠实原文、禁止主观发挥**：如"2.85 万不可写成约 3 万"。因为记忆对后续轮次是权威事实，一旦四舍五入或编造，
   误差会沿对话链逐轮放大。
4. **任何输入都产出合法 JSON 的健壮性**：连问候、纯工具调用指令这种退化输入也要挤出合法对象，保证解析不崩。

---

<a id="q-02-08"></a>

## 问题：压缩失败或异常怎么处理？

`_compress_memory` 有一套兜底（[memory_workspace_react_agent.py:438-469](oxygent/oxy/agents/memory_workspace_react_agent.py:438)）：

- 调 memory_llm 时开 `response_format={"type":"json_object"}`，并**最多重试 2 次**；
- 每次校验：输出不能是模型的 `friendly_error_text`（异常/过短），且必须同时含 `query` 和 `answer` 字段；
- 校验都不过就返回 `None`——这一轮不写 compressed_memory，下一轮加载时该轮就退回用原始 `query`/`answer`。

而且**只有失败/重试才打日志**（"Memory模型输出异常或过短""未按照要求输出""异常：{e}"），成功压缩不记日志，
所以想看压缩结果只能去 ES 查 `memory` 字段。

---

<a id="q-02-09"></a>

## 问题：为什么只有 Text 取数 Agent 有 compressed_memory，别的节点没有？

因为压缩只发生在 `MemoryWorkspaceReactAgent` 这一类节点的 `_execute` 里（给出最终答案时才调 `_compress_memory`）。
其余的 plan/summary/notice/master 等包装型 Agent 走的是普通落库路径，`extra` 里没有 compressed_memory，
所以它们的 history 文档只有 `query`/`answer`（部分带 `react_memory`），不含压缩记忆。这也解释了在 ES 一批命中里
只有 `process_expert__Text取数Agent` 那条带压缩记忆的现象。

---

<a id="q-02-10"></a>

## 问题：这套记忆和向量数据库 / RAG 记忆有什么区别？

完全是两回事，容易被追问，要分清：

| | 本文的短期工作记忆 | 向量库（vearch）记忆 |
| --- | --- | --- |
| 存储 | ES `<app>_history`，纯文本 | vearch 空间，1024 维向量 |
| 取回方式 | 按 trace_id + session_name 精确过滤 + 时间排序 | 按 query 嵌入做 kNN 语义相似 |
| 记的是什么 | 本会话最近几轮的问答浓缩 | 工具描述（用于工具召回），与对话记忆无关 |
| 作用 | 多轮对话的上下文续接 | 语义检索/工具选择 |

面试时如果被问"你的记忆是不是 RAG"，要点明：**这是作用域精确、时间序的短期记忆，不是语义检索**；
系统里的向量是给工具召回用的，两者不要混。

---

<a id="q-02-11"></a>

## 问题：MemoryID 标注是干什么的？怎么支撑"历史去重/复用"？

加载历史轮次时，每条压缩记忆前会贴 `【MemoryID: xxx】`（[memory_workspace_react_agent.py:222-226](oxygent/oxy/agents/memory_workspace_react_agent.py:222)），
让主模型能明确"引用的是哪一条历史"。配合 memory_prompt 里要求保留"工具调用意图"和显式标注"直接复用历史结果"，
Agent 就能识别出"这个查询上一轮已经做过、结果在 MemoryID X 里"，从而**跳过重复的昂贵查询、直接复用**，
而不是每轮都把同样的取数重跑一遍。

---

<a id="q-02-12"></a>

## 问题：引入记忆压缩的延迟代价大吗？

代价是每轮结束多一次 LLM 调用，但很轻：用的是廉价的 memory_llm，输出只有两句话。实测一份约 3 万字的报告
压成约 135 tokens 的两句话，耗时约 3.6s。由于它发生在"最终答案已产出之后"，不阻塞主推理链路的关键路径，
且换来的是后续每一轮上下文都大幅变短（省 prefill、省钱、也降低长上下文带来的注意力稀释）。总体是"一次性小投入、多轮持续收益"。

---

<a id="q-02-13"></a>

## 问题：这套记忆机制还有哪些可改进点？

1. **压缩失败的降级更平滑**：目前压缩失败就整轮退回原始 query/answer，长报告会瞬间把上下文顶大。可加"截断兜底"。
2. **压缩质量的校验**：本机测试出现过过度泛化（把"六个品牌组合"说成"七个区域"这类）。可加一道轻量一致性校验，
   或让 memory_llm 回填关键数值做自检。
3. **语义召回补充**：现在只按时间序取最近 N 轮，跨度大的对话里"很久以前但强相关"的一轮会被挤掉。可叠加一路向量召回做互补。
4. **压缩可观测性**：成功压缩不打日志，排障只能翻 ES。可在成功路径加采样日志或指标，便于监控压缩率和质量。

---
---

<a id="section-03"></a>

# text 取数 Agent 的细节

<a id="q-03-01"></a>

## 问题：上一轮取到的数据，下一轮能直接复用吗？

**结论**：不能。哪怕这一轮要基于上一轮的内容做分析，也会重新生成 SQL 去取数。

**原因**：临时文件清单存在 `workspace_file_dict` 里，而它挂在 `shared_data` 上——作用域是"单次请求（一个 trace）"。新一轮对话即使带 `from_trace_id`，也只继承 `group_data`/`group_id`，`shared_data` 从空重建，所以 workspace 区域看不到上一轮的文件。跨轮唯一能继承的是 `_history` 里的 query+answer 文本，不含可复用的文件句柄。

这是刻意的取舍，三点考量：一是**时效性**，业务库数据实时变，旧 CSV 只是某时刻快照，复用容易给出"过期或口径错位"的隐蔽错误；二是**无状态可重启**，每个 trace 自洽可重放，不依赖随时会过期的 OSS 临时文件；三是**记忆设计一致**，跨轮只留压缩后的语义问答，明细不跨轮，和"简单模式丢弃 react_memory"自洽。代价是同样 SQL 可能重跑，但换来的是每轮都是新鲜、可溯源的真值。真正的多步加工是放在**同一个 trace 内**用 text2data→py_coder 串起来（此时共享同一份 shared_data，文件才真复用）。

<a id="q-03-02"></a>

## 问题：每一次取到的数据大概是什么内容？summary 从哪里拿数据总结？

**工具返回的内容**：无论 text2data 还是 py_coder，返回给 LLM 上下文的都不是全量数据。大结果会截断——只放前 3 行样例 + 明确警告"禁止基于样例下结论"，外加对全量算出的每列统计信息（min/max/count）；完整明细写进 workspace 的 CSV 文件，通过卡片和文件链接交付，几万行也不会进上下文。

**summary 从哪拿数**：从 react_memory（本轮工具调用的逐条记录）里拿，也就是工具已经算好的结果，而不是原始明细。分析永远是"下推"到工具里做的——聚合靠 SQL 的 SUM/COUNT/GROUP BY 跑全库，复杂加工靠 py_coder 跑全量 CSV，只有算完的小结果回到 react_memory；summary 只是把这些结果组织成话术，分析先于总结。

举例：问"过去一年每天各品牌销售额"，几万行落在 CSV 里，react_memory 只看到 3 行样例；但如果问"哪个品牌销售额最高"，SQL 直接 `GROUP BY 品牌 ORDER BY` 返回那一行结果，summary 就基于这行结果作答。因为 summary 只能复述 react_memory 里出现过的数字，**数值溯源校验**（把总结里所有裸数字逐个和 memory 比对）才能拦住自行计算或幻觉出来的数字。

---

<a id="q-03-03"></a>

## 问题：Text 取数 Agent 每轮发给 LLM 的完整 prompt（full_memory）长什么样？

每轮真正发给 LLM 的是一个 **messages 列表 full_memory**，按固定顺序拼四段，最后按 token 上限截断（[progressive_react_agent.py:537-590](../ge-oxy-mas/oxygent/oxy/agents/progressive_react_agent.py:537)）：

| 顺序 | 角色 | 内容 |
| --- | --- | --- |
| ① system prompt | system | 静态指令模板（见下），每轮把 `${...}` 占位符填成实时值 |
| ② 短期记忆 short_memory | user/assistant 交替 | 历史轮次的问答（最近一轮原文、更早的用压缩版），跨轮上下文 |
| ③ 当前 query | user | 用户本轮问题，尾部拼一句"**不要自行计算**，所有数据必须调工具获取" |
| ④ react_memory | assistant/user 交替 | **本轮**的 ReAct 轨迹：每一步思考 + 工具返回，逐轮累积 |

截断策略是保 system、从最近往前贪心塞，所以 query 和最近的记忆一定保留，被丢的永远是最旧的历史。

其中①的 system prompt 不是写死的，而是 `build_multi_data_prompt(active_tools)` 按启用工具动态拼的模板（[multi_data_prompt_builder.py:255](../ge-oxy-mas/applications/data_process/prompts/multi_data_prompt_builder.py:255)），config 里 `prompt=""` 刻意留空、运行时注入。它本身也是固定骨架，简化说就是三块：

- **规则区**：身份（数据分析程序员）＋时间＋核心纪律（一次只用一个工具、切工具前先 `get_tool_documentation`、最终必须 `summary_tool`、**禁止自行计算**）；
- **流程区**：先取数（text2data）→按需分析（py_coder）→后总结（summary）的四步执行指引，按启用工具即插即用；
- **数据区**：在线数据源概述 ＋「临时数据」区（每个 CSV 文件的序号/标识/链接/字段/样例，`${workspace}` 每轮从 `workspace_file_dict` 重建）＋当前工具详情（`${current_tool_name}`）。

一句话总结：**full_memory ＝ 动态 system 模板 ＋ 跨轮 short_memory ＋ 当前 query ＋ 本轮 react_memory，再按 token 截断**。

---

<a id="q-03-04"></a>

## 问题：Text 取数 Agent 如何利用前缀缓存的？

full_memory 的拼装方式对前缀缓存是**刻意友好**的。system prompt 里 `build_multi_data_prompt` 把静态内容全放前面（身份、工具核心规则、工具总览、核心执行路径、步骤 1~4、输出格式），把会变的两块——`${workspace}` 临时数据区和 `${current_tool_name}` 工具详情——压到 `---` 之后的尾部；再往后才是 short_memory、query、以及**只往尾部追加**的 react_memory。顶部那句 `${now_time}` 也不会破坏缓存：`get_time_info` 取的是抹平时分秒的当天日期、且每次请求只算一次后整轮复用，所以一个 turn 内逐字节不变。

真正稳定、量大的收益是**"---"之前那一大段静态指令头**。它是整个 prompt 里最长的部分（几千 token），而且对**同一 scene、同一组 active_tools 的所有请求都完全相同**——不光同一 turn 的每一轮，还包括同一天不同 turn、甚至不同用户不同 query 的请求。加文件、切工具都发生在它后面、动不到它，所以 vLLM 会把这段指令头 prefill 一次、常驻 KV cache，热供给打到这台 worker 上的几乎每一个取数请求。这本质就是"高 QPS 服务共享同一段系统前缀"这个经典 APC 收益。

而"turn 内后一轮复用前一轮对话尾部"这件事，收益其实分场景。在同一个工具连续推理、又没产生新文件的那些轮里，从 system 顶部一路到 query 完全没变、只有 react_memory 追加了新一步观测，命中几乎是完整的——这主要出现在**复杂链路**（多次 text2data、叠 py_coder、同一工具连推好几轮）。但简单的单次取数链路几乎吃不到这部分：正常情况 text2data 只跑一次、`${workspace}` 只在取数后变化一次然后定住，随后还要切到 summary、`${current_tool_name}` 变一次；这条链路总共就两三轮，而且几乎每轮都踩着一个状态变化，没剩几段"同工具连续推理"可复用。命中会从变化点往后断掉：workspace 加文件从 workspace 那行往下失效，切工具因 `current_tool_detail` 在 system 最尾部、它一变会连带后面的 short_memory/query/react 一起错位，只剩它前面那截静态头还命中。

这里有个诚实的权衡值得主动点出来：正因为 workspace 和当前工具写在 **system prompt 里**（位置在对话前部），一旦加文件/切工具，它不只废掉自己那几行，还连带顶掉后面 short_memory、query、react 的缓存；若纯粹为最大化命中，理想做法是把这种会变的引用信息挪到整个消息序列的**最末尾**（react 之后）。但放在 system 有可读性上的道理——临时数据的字段和链接是模型每一步都要参照的框架性上下文，前置更符合"先交代可用资源、再干活"的结构。所以这是"缓存命中率 vs. prompt 可读性"的取舍：整体设计对前缀缓存是友好的（静态在前、轨迹只往尾部追加、时间戳当天不变），确定收益是那段大的静态指令头被全局复用，但没有做到理论最优。

---
---

<a id="section-04"></a>

# 评测与基准测试（Judge 链路 + 记忆压缩）

> 背景：judge 后置裁判和记忆压缩都是引入额外 LLM 调用的模块，上线前要回答三个问题——**测试集怎么设计、
> 用什么指标评、评出来的结果怎么读**。这里讲的是通用方法论，不依赖某一次具体压测的数字。
> 核心难点在于：这两个模块都是"LLM 评 LLM / LLM 压 LLM"，天然带随机性、没有唯一正确答案，
> 所以评测本身要先解决"拿什么当 ground truth"。

---

<a id="q-04-01"></a>

## 问题：这类模块（LLM 裁判 / 记忆压缩）的测试集应该怎么设计？

先分清评测的**两个层次**：功能质量（判得准不准 / 压得对不对）和性能开销（多少延迟、多少 token）。
两层的样本要求不同，但都从**真实线上 trace 采样**起步，而不是自己编小样本——因为这两个模块对输入规模和分布都极敏感，
编的样本测不出生产表现。

功能质量层的关键是**带人工标注的 ground truth**，这是整套评测能不能量化的前提：

| 模块 | ground truth 标注什么 | 正样本 | 负样本 |
|------|----------------------|--------|--------|
| judge 裁判 | 每条 CoT 标"应通过 / 应拦截"，被拦截的再标出**应命中的 violation** | 完全合规的 Summary，用来测**误杀率** | 埋了真实错误（错字段、缺 WHERE、单位错）的 Summary，用来测**召回** |
| 记忆压缩 | 每份对话标出**必须保留的关键事实**（关键数字、实体、结论） | 忠实压缩的期望输出 | 易被"过度泛化 / 张冠李戴"的长报告，用来测失真 |

采样上要做三件事：**按报告类型 / 错误类型分层抽样**，保证覆盖不同场景而不是集中在一种；**正负样本配平**，
正样本测误杀、负样本测召回，只测一边会掩盖另一边的问题；**专门补一批 hard case 和边界样本**（临界合规、
罕见错误、超长上下文），这些才是暴露模型弱点的地方。最后**冻结一份 holdout 集**专门做回归，
每次改 prompt 或换模型都在同一份上重跑，才能纵向比较。

性能开销层则相反，不需要标注，但**必须用真实的大 payload**（长报告 + 多轮历史），
因为延迟/token 几乎完全由输入规模决定，小样本测出来的是"空转延迟"。

---

<a id="q-04-02"></a>

## 问题：judge 裁判的"判得准不准"用什么指标评？

本质是个**二分类评测**：把裁判的 pass/fail 判定和标注的"应通过/应拦截"做混淆矩阵，算出
**precision / recall / F1**。但对裁判这个场景，最该盯的是两类错误的**非对称代价**：

- **误杀率（false positive，把合格的判成不合格）**：直接触发一次无谓的修复重试，浪费延迟和 token，
  甚至把本来对的答案改坏。对用户体验伤害最大。
- **漏放率（false negative，把有问题的放过去）**：错误结果直接进入下一步或返回给用户，是质量兜底失效。

这两者的容忍度取决于业务——后置裁判是"质量闸门"，通常**更怕误杀**（宁可偶尔漏一个，也别频繁打断正确流程），
所以会把误杀率单列为核心指标，而不是只看一个笼统的 accuracy。

除了通过/拦截这一层，还要评**violation 级别的准确度**：拦截对了，但指出的违规理由对不对？
把裁判输出的 violation 和标注的 gold violation 做匹配（是否命中同一条规则），
避免"结论蒙对但理由是编的"——因为 violation 会被回喂去做局部修复，理由错了会把修复带偏。

最后必须评**随机性一致性**：同一条样本跑 N 次，看判定是否稳定。LLM 裁判有温度和采样波动，
如果同一个 CoT 一会 pass 一会 fail，这套闸门就不可信，这个"翻转率"本身就是要监控的质量维度。

**举个例子说清这些指标。** 标注 10 份 summary：6 份合格（应通过）、4 份有错（应拦截），
裁判跑完得到下面这张混淆矩阵（把"拦截/判不通过"当作正向动作）：

| | 裁判判"拦截" | 裁判判"通过" |
|---|---|---|
| 实际有错（应拦截 4 份） | TP=3 正确拦下 | FN=1 **漏放** |
| 实际合格（应通过 6 份） | FP=1 **误杀** | TN=5 正确放行 |

由这张表：precision = TP/(TP+FP) = 3/4 = 75%（拦下的里有几个真该拦）；recall = TP/(TP+FN) = 3/4 = 75%
（真有错的里抓住了几个）；误杀率 = FP/合格数 = 1/6 ≈ 17%；漏放率 = FN/有错数 = 1/4 = 25%。
注意别只报 accuracy=80%，它把两类代价完全不同的错误混成一个数，看不出问题在哪头。

两类错的代价用实例最直观。**误杀（FP）**：summary 正确写了"环比提升 2pp"，裁判套错规则判它"应写 2%"→
触发修复→把对的 2pp 改成错的 2%，白跑一轮还改坏了。**漏放（FN）**：summary 把差值 0.02 写成"0.02pp"
（单位误用、本该拦），裁判没抓到判通过，错误结论直接进下游。**violation 级别**：一份 summary 真错在
"pp 写成 %" + "模板漏填"，裁判判"拦截"对了，但 violations 却写成"时间口径错"（时间其实没错）——
结论蒙对、理由全错，回喂修复时 agent 去改没问题的时间口径，真错反而没人管。**随机性**：同一份 summary
连跑 5 次得到 pass/pass/fail/pass/fail，翻转率高说明闸门不稳、单次判定不可信，要么降温度、要么跑 N 次取多数票。

---

<a id="q-04-03"></a>

## 问题：记忆压缩的质量用什么指标评？

记忆压缩没有"对/错"的二分标签，评的是**信息保真**，主要四个维度：

1. **忠实度 / 事实性（最重要）**：压缩后的两句话有没有改数字、编实体、张冠李戴。
   对着 gold 关键事实逐条核，任何一个关键数字/实体被篡改都算失真——这类"看起来通顺其实说错了"的
   过度泛化是压缩最典型的失败模式。
2. **关键信息召回**：标注的必留事实里，压缩输出实际保住了几条。测的是"该记的有没有记住"。
3. **格式合法率**：能否 `json.loads`、是否含约定字段（如 `query`/`answer`）。这是**线上判定压缩成功与否的硬条件**，
   格式坏了整条压缩就废、回退到用原文，所以合法率要单独统计。
4. **压缩率**：字符/token 压缩了多少倍。它和忠实度是一对**权衡**——压得越狠越省，但越容易丢信息。

但以上都是**代理指标**，最终要看的是**端到端下游任务成功率**：拿"用压缩记忆 / 用原文 / 不给记忆"三组做 A/B，
比对下一轮任务的实际成功率。如果压缩后下游效果和用原文基本持平、而 token 大降，才真正证明压缩是划算的；
如果下游掉点，说明代理指标再好看也丢了关键信息。

---

<a id="q-04-04"></a>

## 问题：性能（延迟 / token）这类开销指标，评测方法上有什么讲究？

方法论上有几个通用要点，跟具体数字无关：

**先预热、丢弃第一次**。首次调用要付冷连接 + 未命中 prompt 缓存两笔一次性成本，明显高于稳态，
算进统计会污染 avg 和尾延迟，反映不出持续运行的真实表现。

**看分位数而不是只看均值**。延迟要报 **p50 / p95**（甚至 p99），因为 LLM 调用长尾很重，
均值会被少数慢请求带偏或掩盖尾部问题，而用户体验是被 p95/p99 决定的。

**分阶段计时 + 分阶段统计 token**。像两阶段裁判这种多环节链路，要把每一阶段的耗时和 token 拆开测，
才能定位成本大头、指导优化——通常"读长上下文做重推理"的环节是绝对大头，"聚焦判定/压缩"的环节很轻，
这个归因直接告诉你该优化谁。

**用真实规模的 payload**，理由同测试集设计部分：延迟/token 由输入规模主导，必须覆盖"最坏但常见"的高负载点。

---

<a id="q-04-05"></a>

## 问题：评测结果出来后，怎么读、怎么判断"效果到底行不行"？

不看单一总分，按几个角度拆着读：

**分类别拆解 + 错误分析**。不只看整体 F1/召回，要按报告类型、错误类型分桶看哪一类拉胯，
再把判错的样本挑出来人工过一遍，归因是"规则没覆盖"还是"模型能力不够"还是"标注本身有争议"。
错误分析找出的问题，往往就是下一轮改 prompt / 补测试集的输入。

**看权衡曲线而不是单点**。裁判画 **precision-recall 曲线**（在误杀率和召回之间权衡），
记忆画**压缩率-忠实度曲线**，看当前工作点是不是落在拐点附近、还有没有免费的提升空间。

**对着可接受阈值和 SLA 判定**。"行不行"要有事先定好的门槛：比如误杀率 < X%、格式合法率 > Y%、
p95 延迟 < SLA。达标才放行，而不是凭感觉。

**和 baseline 比、在冻结集上追回归**。改动的效果必须是"同一套评测集、同一套指标口径"下对比改动前后得出的，
这样差异才能归因到改动本身；每次上线都在冻结 holdout 上重跑，纵向盯有没有回退。
最后再叠一层**人工抽检**兜底，因为自动指标覆盖不到的"语义是否真的对"还得靠人看一眼。

---

<a id="q-04-06"></a>

## 问题：评测这类 LLM 模块时有哪些容易踩的坑？

主动讲这些，面试官爱追问：

**LLM 裁判自身的随机性**。评测工具本身是 LLM，同一输入多次结果可能不同，所以不能只跑一次，
要多次跑取一致性/多数票，否则评测结论本身就不稳。

**meta-evaluation / 自我偏好偏差**。如果用 LLM 去评 LLM（甚至用同源模型评自己），会有偏袒同风格输出的倾向，
关键判定要用人工标注做锚点校准，别让"AI 评 AI"变成自说自话。

**端点保真度**。离线评测常用公网可达的同系列模型替代线上内网网关模型，延迟数字只是**指示性**的，
不等于生产网关精确值；质量也可能有细微差异，报结论时要说清这个替换。

**样本覆盖与并发缺口**。样本别集中在单一 trace/单一类型，否则方差没测到；单机串行压测也测不出
生产并发下的排队延迟，性能结论要标注这个前提。

**代理指标与真实目标脱节**。格式合法、压缩率好看不代表下游任务就好，一定要有端到端的成功率兜底，
别用中间指标替代最终目标。

---
---

<a id="section-05"></a>

# /retrieve 独立召回检索接口面试问题

> 背景：这是知识库检索的核心召回接口。输入一个 query，做混合召回（BM25 词面 + BGE 向量语义），
> 用两阶段 RRF 做排序融合，再过 reranker 精排，最后返回知识实体列表。老的 `/similar` 用的是
> 分数加权（sigmoid）融合，这条链路换成了 RRF 排序融合。代码主要在 `KnowledgeGatewayImpl`
> 的 `retrieveKnowledgeFromES → searchNerCustomRrfProcess`。

---

<a id="q-05-01"></a>

## 问题：/retrieve 这条召回链路整体是怎么走的？

一句话概括：**query 预处理 → 双路召回（BM25 + 向量）→ 第一阶段 RRF 融合 → 精确匹配置顶 → reranker 精排 → 第二阶段 RRF 融合 → 截断返回**。

具体来说，请求进来先做 query 预处理（`prepareRetrieveProcessedQuestion`，主要是小写归一化），然后进入 `searchNerCustomRrfProcess` 这个共享的 RRF 主流程。里面先发一路 BM25 词面召回拿到 `lexicalMap`，再用 query 的 embedding 发一路向量召回拿到 `semanticMap`；两路结果用 `RrfRankFusionHelper.fuseAll` 做第一次 RRF，把词面排名和语义排名融成一个统一榜单。融合后把命中精确匹配的结果单独拎出来置顶，剩下的非精确候选取 top-K 送 reranker 精排，最后用 `recomputeRrfScores` 把词面、语义、reranker 三路的排名再做一次 RRF 融合，得到最终顺序后按 from/size 截断返回。

这里有两个点面试时要能一口说清：**融合是"排名级"的 RRF，不是"分数级"的加权**；整条链路里出现了**两次 RRF**，不是一次。

---

<a id="q-05-02"></a>

## 问题：为什么用 RRF，而不是把 BM25 和向量的分数直接加权相加？

因为 BM25 分和向量余弦分**量纲根本不可比**。BM25 是个无上界的 tf-idf 累加分，能到几十上百，且随 query 长度、词频、文档长度剧烈波动；向量余弦被归一化在 0~1。这两个数直接加权相加，等于让量纲大的那一路天然主导排序，权重完全失去意义。

老的 `/similar` 走的是分数级融合：先用 sigmoid 把分数压到 0~1 再加权。问题是 sigmoid 对输入分布很敏感，它的拐点位置得跟着分数分布调，换一个域、换一批数据，同一组参数就不成立了，非常难维护。

RRF 换了个思路：**不看分数绝对值，只看每一路里的排名**。融合分是 `Σ weight / (k + rank)`，rank 是文档在某一路里的名次。这样无论 BM25 给了 80 分还是 0.8 分，只要它排第一，贡献就一样。**天然免疫量纲和分布差异，不需要按分数分布调参**，这是它相比 sigmoid 加权最本质的优势。代价是丢掉了分数里"领先多少"的强度信息，但对多路异构召回的融合来说，这个取舍是划算的。

---

<a id="q-05-03"></a>

## 问题："两阶段 RRF"具体指什么？两次融合分别融了什么？

两次 RRF 发生在流程的不同位置，融的东西也不同。**第一次**在双路召回之后（`fuseAll`），融的是 **BM25 词面排名 + 向量语义排名**，目的是先把两路异构召回合成一个统一候选榜，决定"谁进精排、以什么初始顺序进"。**第二次**在 reranker 打完分之后（`recomputeRrfScores`），融的是 **词面排名 + 语义排名 + reranker 排名** 三路，各带一个可配权重（`lexicalRrfWeight` / `semanticRrfWeight` / `rerankerRrfWeight`）。

关键点在于：reranker 不是简单地"用它的分覆盖前面的结果"，而是**作为第三路排名，重新参与一次 RRF**。这样 reranker 的判断和前两路召回的判断是加权协商出最终顺序，而不是一票定音，能避免 reranker 单点误判把一个词面/语义都很强的结果压下去。所以"两阶段 RRF"这个说法是准确的，面试时可以明确讲：第一次融召回、第二次融召回+精排。

---

<a id="q-05-04"></a>

## 问题：RRF 里的 rank constant k 是什么？为什么常用 60？

k 是 `Σ 1/(k + rank)` 公式里那个常数，作用是**压平头部名次之间的差距、控制排名的边际权重**。k 越小，第 1 名和第 2 名的融合分差距越大，头部越"陡"，越信任单路的高排名；k 越大，各名次贡献被拉平，越"民主"，更看重"多路都提到"。

60 是 RRF 原始论文（Cormack 2009）里实验出的经验值，被当成通用默认沿用下来，并没有特别的理论必然性——它是个"头部有区分度、又不至于让第一名一家独大"的折中点。我们代码里 k(`rankConstant`) 和三路权重都是走 DUCC 动态配置的，可以不重启在线调，这也是相比写死参数的一个工程优势。

---

<a id="q-05-05"></a>

## 问题：BM25 和向量两路是并行发起的吗？

是并行的。BM25 词面召回和向量语义召回是两次相互独立、无数据依赖、也不改写任何状态的只读查询，所以用 `CompletableFuture` 把它们提交到一个有界线程池里并发发起，各自带超时，最后 `join` 汇合再进 RRF 融合。这样两路的耗时从"相加"变成了"取最大值"，query embedding 生成也和 BM25 这一路重叠掉，整体召回延迟明显下降。

设计上有两个点可以展开讲：一是**用有界线程池而不是公共 ForkJoinPool**，避免召回并发把公共池打满、影响到同进程里其它任务，同时线程数有上界、可控；二是**每一路都设了超时和降级**，某一路超时或异常不会拖垮整个请求——比如向量路挂了，仍可用 BM25 单路结果兜底返回，保证可用性优先。因为两路无共享状态，并行本身不引入线程安全问题，融合只在两路都回来（或超时降级）后的汇合点做。

---

<a id="q-05-06"></a>

## 问题：向量检索用的是 ANN 还是 KNN？

用的是 **ANN（近似最近邻）**。`nerFeature`/`nerExtraFeature` 建成了 `dense_vector` 且带 HNSW 图索引（`index_options.type:hnsw`，`m:32`，`ef_construction:200`，`similarity:cosine`），检索时走 ES 原生的 `knn` 查询,沿 HNSW 图跳近邻,而不是对全量向量逐条暴力算余弦。这样向量召回的复杂度从暴力精确 KNN 的 O(N) 降到近似 ANN 的对数级,在 PB 级、上千万向量的库上,这是能把向量检索延迟压到可接受范围的关键。

面试可以顺带把参数的含义讲清楚,体现你懂 HNSW:`m` 是图里每个节点的最大出边数,越大图越密、召回率越高但内存和构建成本越高,32 是个偏召回的取值;`ef_construction` 是建图时每个节点的候选池大小,200 越大建出来的图质量越好、构建越慢;查询侧还有个 `ef`(或 `num_candidates`)控制搜索时的候选广度,越大越接近精确、越慢。所以 ANN 的本质是**用可控的一点点召回率损失,换数量级的检索加速**,而 HNSW 的这几个参数就是在"精度 vs 速度/内存"之间调这个平衡点。

如果被追问"ANN 会不会漏掉真正最近的向量":会有极小概率,这正是"近似"的含义;但在混合召回里向量只是其中一路,后面还有 BM25 兜底和 RRF 融合、reranker 精排纠偏,单路的近似误差被整条链路稀释掉了,实际影响很小。

---

<a id="q-05-07"></a>

## 问题：精确匹配是怎么做的？为什么 score 直接置 1？

精确匹配是**独立于 BM25/向量的一路旁路查询**，不是在两路已召回的结果里再挑。它先发一个专门的 ES DSL 去按名字精确命中候选，再在 Java 侧做一次严格归一化相等校验（`normalizedQuery.equals(normalizeExactText(nerName))`），确认是"名字一模一样"而不仅仅是相关。命中后走 `buildExactOnlyHit` 把 `_score`、`RRF_SCORE`、`RERANKER_SCORE` 全置成 1.0 并直接置顶。

为什么要置 1 并置顶、绕过 RRF 和 reranker：因为**名字完全一致是确定性最高的信号**，没有任何理由让它去和模糊召回的结果在 RRF 里"公平竞争"，更不该冒 reranker 误判把它压下去的风险。置 1 是给它一个不可超越的上界、锁死在第一。同时这么做还顺带省了精排预算——精确命中的结果被从昂贵的 reranker 流程里摘出去了。

这套精确匹配置顶在检索链路里是统一启用的,配合 RRF 融合和 reranker 精排一起构成"确定性优先、模糊召回兜底"的完整排序策略。

---

<a id="q-05-08"></a>

## 问题：reranker 放在哪一步？送进去的候选量怎么控？

reranker 在第一阶段 RRF 之后、第二阶段 RRF 之前，对非精确候选做精排。它是个 cross-encoder，每个候选都要过一次模型，是**全链路最贵的一环**，所以候选量必须卡上界。

`resolveRerankerCandidateSize` 用 `min(非精确候选数, RRF 窗口, 请求窗口×可配倍数)` 算出真正送模型的数量，再由 `selectRerankerCandidates` 只取 RRF 排序后的 top-K 送进去，而不是把召回到的全部文档一股脑 rerank。这样精排耗时和"用户真正要的结果数 + 一个倍数"挂钩，和底层召回池大小解耦。精确匹配的结果也不进 reranker。这是这条链路里最实在的一处效率控制。

---

<a id="q-05-09"></a>

## 问题：为什么可以选用不同的 reranker 模型（Qwen3 / BGE）？两者优劣是什么？

分两层答：先讲**为什么设计上能换**，再讲**两个模型怎么分工**。

设计上能换，是因为 reranker 做成了**插件化、配置驱动**的能力：精排调用被收敛到一个统一入口后面，具体用哪个模型（`bge` 还是 `qwen3`）由 DUCC 动态配置决定，代码里维护了一个受支持的模型清单，切换不用改代码、不用重启。这带来三个好处：能在**同一套评测集上做 A/B**，量化对比再决定选型；能**按用途选模型**；能做**降级**，一个模型异常或超时可回退到另一个保住可用性。落点是——不是临时挑一个，而是把 reranker 做成了可插拔、可配置、可 A/B 的能力，选型是数据驱动的结果。

两个模型怎么分工，关键先立一个区分：**"排序准确性"和"分数区分度"是两回事**。**排序上两者评测下来是接近的**——返回的内容和名次体感差不多，Hit@1/Recall@5 也在同一水平；真正的差异在**分数**：`bge-reranker-v2-m3` 是专训的小型 cross-encoder，排序准但**分数容易饱和**（相关文档的分都挤在高位、绝对值不可比），`qwen3-reranker-8B` 是 LLM-based，**分数区分度更好、更可校准**（相关和不相关的分能拉得开）。所以这两句话同时成立、不矛盾：一个说的是**名次排得对不对**，一个说的是**分数能不能当信号用**。

分工就顺理成章：**主链路排序用 BGE**——排序既然和 Qwen3 相当，就选更轻、更快、更省的那个扛高频调用，它决定最终 rank；**对外卡阈值/分级用 Qwen3**——因为 BGE 分数饱和、消费方没法拿它的分设阈值，而 Qwen3 的分区分度好、适合调用方做过滤、分级、业务规则判断。一句话收口：**留两个不是因为排序上谁更强，而是一个负责高频排序（BGE，省成本）、一个负责可消费的分数（Qwen3，能卡阈值）**，各管一摊。

这里还有个能体现系统设计的闭环点值得讲：**第二阶段 RRF 融合用的是名次（rank）而不是分数**，所以 BGE 分数饱和这个缺点**对内部排序完全无害**——RRF 只看它排第几，饱和的绝对分根本不参与融合；而当消费方需要一个有业务含义的绝对相关分时，才把 Qwen3 那个区分度好的分暴露出去。等于系统天然把"内部排序"和"对外打分消费"解耦了，各取所长。

如果被追问"那 Qwen3 的分数怎么评"，就说它不看 Hit@1（那是排序指标、归主链路 BGE），看的是**分数分布的可区分性**——相关/不相关样本的分数重叠度、某个阈值下的准确率，是和排序指标独立的一套维度。

---

<a id="q-05-10"></a>

## 问题：query 的 embedding 有缓存吗？缓存怎么设计的？

有,用 Redis 做了一层集中式缓存,命中就直接拿现成向量,不再走一次模型 HTTP 往返。要先说清一个区分:**文档侧向量不是每次现算的**,`nerFeature` 是写入知识时就算好存进 ES 的;需要实时算的只有 query 这一侧,也就是对用户输入那句话做 embedding。正因为只在 query 侧、且线上 query 有明显热点,缓存收益很直接。

缓存能成立的前提是**同一段文本过同一个模型,输出向量是确定的**,所以它天然可缓存。**key** 用"归一化后的 query 文本 + 模型版本号":文本先做小写、去首尾空白,和检索侧 `processedQuestion` 的归一化保持一致,避免"大小写不同"被当成两条,再对文本取 hash 拼进 key 收敛长度,最终形如 `emb:{模型版本}:{hash(归一化文本)}`;**value** 存 1024 维向量,为省空间和序列化开销用紧凑二进制(float 打包成字节,一条约 4KB),不直接塞 JSON 数组。**一致性靠 key 里的模型版本号兜**:模型换版本后 version 变、key 自然错开,老向量不会被误用,旧 key 靠 TTL 到期自然淘汰,不需要手动清;TTL 在这里主要是防冷门 query 长期占内存的安全网,设几天量级即可。另外**空值和异常结果不写缓存**,避免把一次模型抖动产生的空向量缓存住反复返回错的。

**最关键的是降级**:这层是旁路缓存(cache-aside)——先查 Redis,命中直接用,没命中再调模型算、然后回写(回写可异步、不阻塞主链路)。而且 **Redis 只是加速、不在正确性关键路径上**,所以给它设很短的读超时,一旦 Redis 抖动或挂了立刻降级成"直接调模型现算",绝不能因为等 Redis 反而比不加缓存还慢。

效果上,热点 query 直接命中缓存、省掉一次同步模型调用,把这段固定 RT 从关键路径上摘掉;配合前面双路并行,向量这一路在命中时几乎只剩一次 ES `knn` 查询的开销。

---

<a id="q-05-11"></a>

## 问题：这个接口做了哪些提升效率的设计？

几条主线一起用:延迟侧靠**双路并行 + 向量走 ANN + query embedding 缓存**,计算侧靠"少算、少传、少送模型"。

延迟侧,BM25 和向量两路用 `CompletableFuture` 并发发起、耗时取最大而非相加,向量召回走 HNSW 的 ANN 把复杂度从 O(N) 降到对数级,热点 query 的 embedding 又被 Redis 缓存掐掉了模型往返,这三处直接压的是接口 RT。

计算侧最值得讲的是 **`_source` 字段白名单裁剪**:召回时下推 `"_source":[...]` 只取 `nerName`、`filterId` 等小字段,**白名单里刻意不含 1024 维的 `nerFeature`/`nerExtraFeature` 向量**。检索时用向量算分,但返回结果绝不把向量带回来,否则每条文档光两个向量就是 2048 个 double,序列化和网络开销极大——这在向量库场景是很关键的一处优化。其次是 **reranker 候选量上界控制**(只取 RRF top-K 送模型)、**精确匹配短路**(命中直接置顶、绕过 RRF 和 reranker,省精排预算),以及**用 filter 收窄候选集**减少参与打分的文档数。

---

<a id="q-05-12"></a>

## 问题：index 参数是必传的吗？拿不到怎么降级？

不是强绑死。`resolveRetrieveIndex` 里 index 是可选的，传了就用传入的索引，没传就回退到 `duccConfiguration.getEsGeneralIndex()` 配的默认通用索引。这么设计是为了让接口既能被指定库精确调用，又能在调用方不关心具体索引时有个兜底，不至于因为少传一个参数就整体失败。

---

<a id="q-05-13"></a>

## 问题：这条链路怎么评测？为什么能说明效果提升？

用一个固定的 query→期望知识的标注评测集，端到端跑整条召回链路，主看两个指标：**Hit@1 从 59% 提升到 83%**（第一条就命中正确知识的比例，衡量排序头部质量），**Recall@5 从 70% 提升到 91%**（正确结果落在前 5 的比例，衡量召回覆盖）。

提升主要来自三处：融合从 sigmoid 分数加权换成 RRF 排名融合，免掉了量纲/分布敏感带来的排序错乱；引入 reranker 做二阶段精排并重新参与 RRF；以及精确匹配置顶保证了"名字一样必第一"的确定性。评测时用的是同一套评测集、同一套指标口径，对比的是换链路前后，所以差异能归因到链路改造本身。

有一点要交代清楚，免得被"你有两个 reranker，这指标算的哪个"绕住：**Hit@1/Recall@5 是纯排序指标，衡量的是主链路，主链路排序用的是 BGE**，所以这两个数就是"RRF + BGE 精排"这条排序链路的效果，不用去纠结两个模型怎么合成一个指标。Qwen3 不在这个排序指标里争——它负责对外输出可卡阈值的分数，评的是分数区分度那套独立维度。所以**别把 Hit@1 的提升说成 Qwen3 带来的**，功劳是 RRF + BGE，这条别串。

---

<a id="q-05-14"></a>

## 问题：这条链路还有哪些可改进点？

延迟和重复计算的大头(双路并行、向量 ANN、query embedding 缓存)已经做掉了,剩下可以主动讲的增量方向有几处:**检索结果整段缓存**,知识低频更新、query 有热点,可对归一化 query 的最终结果做短 TTL 缓存,连 ES 查询都省掉,注意知识更新时要能失效;**RRF 权重和 reranker 候选倍数的自动调参**,现在靠 DUCC 人工配,可以基于评测集做离线搜参甚至在线小流量对比;**精确匹配路在 retrieve 与高基维链路的完全对齐**,把置顶逻辑做成两条链路共用的一致行为。

---
---

<a id="section-06"></a>

# callBackDatasetFull 数据集召回引擎面试问题

> 背景：Text2SQL问数场景里，要把用户口语化的query精准映射到底层数据集的真实字段和维值。
> callBackDatasetFull是这条映射的召回引擎：多路并行召回（向量+规则）先保广度，LLM精排再收窄，
> 通过分阶段并行、分侧Embedding缓存和差异化异常处理优化召回链路，字段/维值召回准确率达到96%。
> 核心代码在VectorCallService（多路向量召回）、RerankMergeStage（LLM精排+合并去重）。

---

<a id="q-06-01"></a>

## 问题：这个召回引擎解决的核心问题是什么？为什么要做多路并行召回？

核心难点是：Text2SQL生成SQL前必须知道用户提问对应的底层字段和维值，而用户表达是口语化、非结构化的，query里每个关键词的"身份"事先并不确定——它可能是字段名、字段的同义词/别名、某个维值，也可能是一句业务术语。单路召回一定会漏：用别名说的字段、本质是维值的词，都可能匹配不到。

所以设计成多路召回：每一路只负责一种可能性（字段名、同义词、别名、基础维值、自定义维值、维度中心、业务知识），各自去命中候选，部分检索再按关键词并发。前面先把召回率保住、宁可多捞，设计意图是把噪声交给后面的LLM精排判断，就是"召回重recall、精排重precision"的分层思路；当前字段精排还有并集合并边界，后文会单独说明。

举个例子：用户问「上个月北京的成交额是多少」。这句话里"上个月"是时间条件、"北京"其实是一个维值、"成交额"是字段（而且底层字段可能叫gmv_amt，用户说的是它的别名）。事先没人告诉系统哪个词是字段、哪个是维值，所以只能多路一起上——字段名/别名路去认"成交额"，维值路去认"北京"，各认各的，最后再拼起来。

---

<a id="q-06-02"></a>

## 问题：整条链路分哪几个阶段？

切词与候选准备并行 → 选表 → 知识召回（规则+向量）→ 启动LLM补字段并行任务 → 主线依次执行字段召回和值召回 → 汇合LLM补字段 → LLM精排 → 合并去重 → 组装返回。字段和值两个Stage不是彼此并行，但它们内部各自包含多路检索。

下面再用一个完整例子把每个阶段串起来，后续问题分别展开各节点的实现细节。

---

<a id="q-06-03"></a>

## 问题：能用一个完整例子把callBackDatasetFull从切词到返回串起来吗？

可以。假设用户问「看今年北京地区的GMV，剔除取消订单」，候选数据集和字段如下：

```text
datasetId=101：订单明细表
datasetId=102：退款明细表

订单明细表：
columnId=11：实付金额 pay_amount，别名GMV，同义词成交额，指标字段
columnId=12：城市 city_code，维度字段，字段配置绑定维度编码city（dimCode=city）
columnId=13：订单状态 status_code，维度字段，已支付=1，已取消=0
columnId=14：支付日期 pay_date，日期字段
```

订单明细表还配置了三条业务知识：

```text
K1：GMV按实付金额求和，触发词GMV，useColumnCall=true
K2：有效订单不包含已取消订单，每次召回，useColumnCall/useValCall=true
K3：北京属于华北区域，useValCall=true
```

以下向量命中只是为了说明流程，是否真正命中仍取决于运行时索引、阈值和截断配置。

**1. 切词与候选准备并行执行。** 同一个query并行调用两次切词LLM，合并后假设得到普通词`GMV、北京、取消订单`，时间词`今年`，`dataSource`为空；另一条任务同时加载101、102两张候选表的schema、字段启停、别名、同义词、维度绑定和虚拟字段。切词两路都失败时，系统会把整句原始query作为兜底关键词。

**2. 从候选表中选择一张最终表。** 本例没有明确的数据源词，名称规则无法唯一确定，系统继续用原始query做数据集向量召回，再由LLM从收窄后的候选中选择`datasetId=101`。从这一刻开始，知识、字段和维值都只围绕订单明细表处理；`datasetIdList`表示备选范围，不表示两张表都会参与查询。

**3. 召回并切分业务知识。** 系统先加载`bookId+datasetId=101`下的全部知识：K1因query包含GMV而被规则召回，K2因配置为每次召回而直接命中；规则命中后仍会继续用`北京、GMV、取消订单、今年`做知识向量召回，假设K3也超过阈值。最终返回的知识由`recalledKnowledgeIds`控制，而K1、K2、K3的解释还会按照`useColumnCall/useValCall`再次切词，例如得到`实付金额、订单状态、已取消、华北区域`，用于扩充后续召回词。

**4. 异步启动LLM补字段。** 知识召回结束后，系统让LLM直接查看原始query、订单明细表的全部字段名称/别名/同义词以及完整知识缓存，假设返回`实付金额、城市、订单状态`。这一步不是再次切词，程序会通过预先建立的名称映射把它们转换为`columnId=11、12、13`；该任务与后面的主召回链并行，只补字段，不补维值。

**5. 组装字段词和值词并生成Embedding。** Query普通词先同时复制到字段和值两侧，知识切词再按开关分别扩充，得到类似下面两组词：

```text
columnKeywords：GMV、北京、取消订单、今年、实付金额、订单状态、已取消
valueKeywords：GMV、北京、取消订单、今年、订单状态、已取消、华北区域
```

字段和值分别批量生成Embedding：字段原名向量和别名/同义词向量复用字段Embedding，基础维值和自定义维值复用值Embedding；维度中心直接使用原词调用NER，不使用这批Embedding。当前代码在知识召回阶段原地把时间词加入普通词集合，因此`今年`也会重新进入两侧，这是集合复用造成的实现边界。

**6. 字段召回按“字符串规则→字段原名向量→别名/同义词向量”顺序执行。** `GMV`精确命中`实付金额`的别名并映射到`columnId=11`，`实付金额`命中原字段名，`订单状态`命中`columnId=13`；规则已命中的词不再进入后续字段向量查询。剩余词继续查询两个字段向量空间，命中结果最终都通过`columnId`回到订单明细表的原始字段，而不是靠名称临时猜测；本例假设`北京`没有达到城市字段的向量阈值，因此字段主链暂时只有11和13。

**7. 值召回在字段Stage之后执行，但不依赖字段召回结果。** 所有`valueKeywords`分别尝试适用的维值来源，并不是每个字段固定查询三遍：

```text
基础维值：已取消 → 订单状态columnId=13 → 展示值已取消，真实值0
自定义维值：取消订单 → 用户配置的“已取消” → columnId=13，真实值0
维度中心：北京 → 字段绑定维度city → 城市columnId=12 → 展示值北京，真实维值编码110000
```

基础维值来自提前建立的原始维值索引，不会在回调时扫描业务底表；自定义维值只在配置过的字段上有结果；维度中心只处理预先绑定了维度编码的字段。这里字段配置的`dimCode=city`表示去哪个维度空间查询，返回的`110000`才是“北京”的真实维值编码；`dimCode=city`不一定是数据库中的物理列，数据库真实列仍是`city_code`。

**8. 等待LLM补字段并按缺失columnId合并。** 字段主召回已有11和13，LLM补字段结果为11、12、13，因此程序只补入缺少的`columnId=12`，不会覆盖已经召回的字段证据。如果字段向量已经命中城市字段，LLM结果就不会重复补充；LLM选字段重试耗尽会终止本次完整召回。

**9. LLM对字段和值候选做精排。** 精排输入包括原始query、字段/维值候选表和完整知识缓存，假设保留实付金额、城市、订单状态，以及`北京→110000`、`已取消→0`。精排失败或返回空时会直接保留初召结果；当前实现中维值会被精排结果真正过滤，字段却会把精排结果与原字段结果取并集，所以字段噪声不一定能被删除。

**10. 按关键词归属合并最终字段和值。** 程序逐词检查两侧是否实际命中：`GMV`只命中字段，归为`COLUMN_ONLY`；`北京`只命中维值，归为`VALUE_ONLY`，但维值本身带有`columnId=12`，所以城市字段仍会进入最终结果；如果一个词同时命中字段和值，则归为`COLUMN_AND_VALUE`，表示两类候选都保留，不是LLM必须二选一的冲突。字段和值两侧都没有命中的词会被丢弃。

**11. 执行固定字段收口规则。** 系统会排除FULL表的分区字段、补齐配置为始终召回的虚拟字段，并自动加入选中表的日期字段，因此本例的`支付日期 pay_date`也会返回。`callBackDatasetFull`没有把`今年`一对一绑定到某个日期字段；如果表中同时存在下单日期、支付日期和发货日期，当前可能把这些日期字段都返回，由调用方结合原始问题选择具体字段并生成时间范围。

**12. 组装返回并完成外层收尾。** 简化后的核心结果是：

```text
最终数据集：datasetId=101 订单明细表
召回知识：GMV口径、有效订单口径、华北区域知识

字段：
columnId=11 实付金额 pay_amount
columnId=12 城市 city_code，fieldEnum=[北京/110000]
columnId=13 订单状态 status_code，fieldEnum=[已取消/0]
columnId=14 支付日期 pay_date
```

其中`enumValue`是给人和模型理解的展示值，`originEnumValue`是底表查询真正使用的值或编码。领域层随后遍历原始候选ID触发数据集向量元数据更新，RPC层把BO转换为DTO、补充总耗时并返回；`callBackDatasetFull`到这里结束，它不生成也不执行SQL。

下游根据原始query和这份召回结果，才可能生成类似：

```sql
SELECT SUM(pay_amount)
FROM order_detail
WHERE city_code = '110000'
  AND status_code <> '0'
  AND pay_date >= <今年开始时间>
  AND pay_date < <明年开始时间>
```

这里`北京→110000`和`已取消→0`由召回链提供，`SUM`、`<>`以及具体时间范围由后续SQL生成阶段结合query和业务知识确定。当前链路最终只返回一张表；如果问题必须同时使用订单表和退款表，调用方需要拆成两次单表召回与查询后再聚合，明细级Join则要依赖预关联宽表或扩展多表协议。

---

<a id="q-06-04"></a>

## 问题：第一步切词在主链路中的位置是什么？为什么能和候选数据准备并行？

`executeFull`进入实际召回前，会在`runSplitAndCandidatePrepare`里同时启动`SplitQueryStage`和`DatasetPrepareStage`，等待两者都完成后再进入选表。切词只依赖用户原始query，不依赖候选表和字段schema；候选准备则负责加载数据集、字段和语义配置，两者没有前置依赖，因此可以并行执行，让切词LLM调用与候选准备的耗时重叠。

这里其实有两层并发：外层是“切词”和“候选数据准备”并行，内层是同一个query同时发起两次LLM切词。外层解决阶段间等待问题，内层通过合并两次结果扩大关键词覆盖；两层`CompletableFuture`都没有显式传入自定义Executor，当前使用的是默认异步执行器。

---

<a id="q-06-05"></a>

## 问题：切词阶段给LLM什么输入？返回的数据结构是什么？

切词Prompt中的动态业务输入只有原始query，不包含最终数据集的schema、字段名或维值列表，所以这一步不可能确认某个词对应底表里的哪个唯一字段。模型被要求返回`dataSource`、`keywords`、`timeKeywords`三个字段，代码再把原始query回填到`SimpleSplitKeywordBO.query`中。

- `dataSource`：用户问题里可能出现的数据源或表线索。
- `keywords`：对后续SQL有意义的业务词，源码明确说明这里“不区分是字段还是值”。
- `timeKeywords`：日期、相对时间和明确时间范围等时间表达。

`keywords`和`timeKeywords`在Java对象里都是`Set`，因此会自动去重。虽然Prompt要求关键词按原句顺序输出，但两次结果还会进入`HashSet`合并，实际代码并不保证顺序。

---

<a id="q-06-06"></a>

## 问题：切词Prompt具体是怎么设计的？哪些词会保留、拆分或过滤？

Prompt的核心要求是“整体保留优先，细粒度拆分补充”。它要求模型先整体保留专有名词、固定业务术语、复合指标和明确列举的维值；如果复合词由品牌、地域、渠道、业务限定词和基础指标组成，再补充拆出有独立业务含义、能够和字段名或维值对应的部分，但不能拆出无意义单字。

Prompt同时要求模型过滤停用词以及排序、分页、计算指令，例如“升序、前5个、同比、环比、占比”，但保留计算涉及的业务字段。输出被约束为纯JSON；程序会先清理首尾代码围栏和`json`前缀，再反序列化，格式仍不合法时会进入重试。

例如用户问「查询上月北京门店销售额及同比」，一次合理的模型输出可能是：

```json
{
  "dataSource": "",
  "keywords": ["北京门店销售额", "北京", "门店", "销售额"],
  "timeKeywords": ["上月"]
}
```

这里先保留完整业务短语“北京门店销售额”，再补充拆出“北京、门店、销售额”；“上月”进入时间词，“查询、及、同比”不作为普通召回关键词。这个JSON只是按Prompt规则给出的示例，实际模型输出可能有差异。

---

<a id="q-06-07"></a>

## 问题：为什么同一个query要并行切两次？两次结果如何合并？

两次调用使用的是同一份query、同一份Prompt和同一套模型配置，不是一路识别字段、另一路识别维值。代码效果是把两次结果取并集：`keywords`分别合并去重，`timeKeywords`也分别合并去重；其中一路失败返回`null`时，另一路仍可单独提供结果。

例如第一次返回`{北京门店销售额, 北京, 门店, 销售额}`，第二次返回`{北京门店, 北京, 销售额}`，合并后会得到`{北京门店销售额, 北京门店, 北京, 门店, 销售额}`。这种设计能容纳两次模型切分粒度的差异，但也可能带来更多噪声，后面仍要结合真实schema的实际命中和合并规则判断每个词的归属。

`dataSource`没有做并集：只要第一次结果对象不为`null`，就直接采用第一次的`dataSource`，只有第一次整体为`null`才使用第二次。因此第一次返回空字符串、第二次识别出数据源时，第二次结果也不会被采用，这是当前实现的边界。成功结果中只要`dataSource`非空，`SplitQueryStage`还会把它补进`keywords`，避免模型把一个业务词单独识别成数据源后导致普通关键词丢失。

---

<a id="q-06-08"></a>

## 问题：时间词是怎么处理的？它真的不会进入字段和值召回吗？

按切词代码的设计，两次`timeKeywords`先取并集，然后从普通`keywords`中删除完全相同的词。例如初始结果是`keywords={销售额, 2025年}`、`timeKeywords={2025年}`，过滤后应变成`keywords={销售额}`，时间词只作为独立信息保留。

但按当前完整链路，不能说时间词一定不会进入字段和值召回。`KnowledgeRecallStage`里的`allWords`直接引用原始`keywords`集合，再执行`addAll(timeKeywords)`用于知识召回，这会同时把时间词写回`SimpleSplitKeywordBO.keywords`；随后`initRecallKeywords`又把这个集合复制到字段和值两套关键词中。所以上面的例子会重新变成`{销售额, 2025年}`，两条召回链都可能收到“2025年”。

这是集合引用复用造成的实现问题，不是Prompt设计目标。要真正隔离，应在知识召回阶段先复制一份`keywords`再追加时间词，不能原地修改切词对象。

---

<a id="q-06-09"></a>

## 问题：切词时如何判断一个词是指标、维度字段还是维值？

切词阶段不做这个判断，因为LLM没有看到真实schema，输出对象也没有“指标/维度字段/维值”的类型标签。知识召回完成后，`initRecallKeywords`会先把query切词复制到`columnKeywords`和`valueKeywords`两侧，再按照已召回知识的`useColumnCall`和`useValCall`配置分别扩展字段词或值词，让两条召回链各自尝试命中。

例如「查询北京的成交额」可能切出“北京、成交额”。假设最终表存在成交额指标列和城市维度，“成交额”可以在字段链命中指标列，“北京”可以在值链命中城市维度下的枚举值；如果表中还存在“地区”字段，用户说“地区”时也可能在字段链命中一个维度字段。也就是说，类型由后续对最终数据集schema和维值空间的实际命中决定，字段链命中只说明它是字段，不能直接等同于指标；同一个词也允许两边都命中，再由后续合并阶段处理。

---

<a id="q-06-10"></a>

## 问题：切词调用失败时怎么降级？有哪些边界情况？

单次LLM调用有可配置的业务尝试次数，默认最多尝试3轮（含首次）、轮次间隔2秒；主模型请求异常时还可以顺序尝试备用模型。外层两路切词各自捕获异常并返回`null`，因此一路失败、另一路成功时仍可继续；两路都失败，或者两路原始结果合并得到的`keywords`在剔除时间词前为空，`SplitQueryStage`会把整句原始query作为唯一关键词、把`timeKeywords`置空，保证后续召回不断链。

降级的代价是关键词粒度变粗，例如「查询上月北京门店销售额及同比」会整体作为一个词，后续精确匹配和向量召回的效果通常不如正常切词。还有一个代码边界：空结果判断发生在时间词过滤之前，如果`keywords`最初只有时间词，过滤后即使变成空集合也不会再次触发原query兜底；当前知识召回的集合复用又可能把时间词加回来，但这不应当作为合理的兜底机制。

---

<a id="q-06-11"></a>

## 问题：第二步是不是直接根据切词结果选择表？

不完全是。时序上确实是“切词与候选准备汇合后选表”，但选表代码不会遍历`keywords`或`timeKeywords`逐词匹配；它直接使用的切词结果主要是`dataSource`，同时还会使用原始query、调用方给定的候选`datasetIdList`以及候选表元数据。

当前主顺序是：候选加载 → 表名规则匹配 → 表向量召回 → LLM选表 → 绑定最终表字段。多候选场景中，名称规则唯一命中或向量达到直选条件时，后续选择方式就短路；单候选在加载时结果已经确定，但当前代码仍会经过一次名称规则步骤。只有规则未定、向量仍返回候选但未达到直选条件时，才把收窄后的候选交给LLM；向量空结果或异常不会进入LLM。因此更准确的说法是：**切词提供数据源线索，选表综合原始问题和候选表信息做分层决策**。

---

<a id="q-06-12"></a>

## 问题：如果候选列表里只有一张表，还会走向量和LLM选表吗？

不会。`loadCandidates`发现只有一张候选表时就设置`finalCallDataSet`，因此这张表从选择结果上已经确定，后续不会执行表向量召回和LLM选表。

不过当前控制流还有一个实现细节：加载单候选后没有立即检查最终表，仍会执行一次表名规则匹配；规则步骤结束后发现`finalCallDataSet`已经存在，才绑定这张表的字段并返回。所以面试时可以说“单候选无需向量和LLM裁决”，但不能说当前代码连规则步骤都完全跳过了。

---

<a id="q-06-13"></a>

## 问题：表名规则匹配具体怎么做？会用切出来的所有关键词吗？

不会使用所有`keywords`，只看原始`query`和切词得到的`dataSource`。每张候选表先确定一个匹配名：优先使用`datasetCaption`，为空时才使用`schemaName`，然后按以下优先级短路匹配：

1. `dataSource`与表匹配名完全相等，大小写敏感。
2. 表匹配名出现在原始query中，按忽略大小写的字面子串匹配。
3. `dataSource`与表匹配名存在互相包含关系，大小写敏感。

某一层有结果后就不会继续执行下一层。只命中一张表时直接定表；命中多张时只保留这些表进入向量阶段；完全没有命中时保留原候选集不变。

例如候选表名分别是“订单明细”“商品流量”“售后明细”，用户问「查询订单明细上月北京成交额」，切词又得到`dataSource=订单明细`，第一层就能精确选中“订单明细”。如果模型没有识别出`dataSource`，第二层仍可通过原始query中出现“订单明细”完成命中。

---

<a id="q-06-14"></a>

## 问题：名称规则无法唯一选表时，表向量召回怎么做？

向量召回全程只在当前候选 datasetId 范围内比对，不会跨候选集。做法是把 query 向量化，如果切词还切出了表名信号 dataSource，就额外单独向量化它。一次 Vearch 查询同时比对三路向量——表名、描述、摘要：表名向量优先用 dataSource（没有才退回用 query），描述和摘要一律用 query 向量；三路各自的相似度再由加权 ranker 按 DUCC 权重合成一个表分，当前权重是摘要 0.5、表名 0.35、描述 0.15，摘要占比最高。

拿到按分数降序、保留 vectorDatasetTop（默认 3）条的结果后，能不能直接定表看两道阈值：只召回一张表时，分数要 ≥ vectorFinalMinScore（线上 0.7）才直接选它；召回多张时，要 top1 先达到最低分，且 top1 相对 top2 的分差比例 (first-second)/second ≥ vectorFinalDiffRatio（默认 0.3，即 top1 至少比 top2 高出 30%）才敢直接选 top1。两个条件都不满足，就把候选裁剪到 llmTableCandidateLimit（默认 3）条，连同字段名、别名、同义词一起交给 LLM 结合语义选表。

举个直观例子（分数仅示意）：top1 是 0.82、top2 是 0.55，相对差约 49%，超过 30%，可以直接选 top1；如果是 0.68 和 0.62，相对差只有约 10%，说明两张表都很像，就交给 LLM 再判一次。也就是说，向量负责「粗筛 + 高置信直选」，只有它自己都拿不准的模糊 case 才升级到 LLM 兜底。

---

<a id="q-06-15"></a>

## 问题：LLM选表的Prompt、输入和模型是什么？

LLM选表使用DUCC配置`prompt.recall.simple-select-table`，要求模型从候选中返回严格JSON：`{"tableId": 123, "reason": "简短原因"}`。代码会校验`tableId`是否属于当前候选；模型即使幻觉出候选集外的ID，也不会被接受为最终表，而是回退当前候选top1。

每个候选实际传给模型的信息包括`tableId`、`schemaName`、`datasetCaption`、原始query，以及字段中文名、字段别名和字段同义词；不包含表摘要、字段描述，也没有把切词后的`keywords`列表直接传入。当前对象里`tableName`放的是`schemaName`，`tableDesc`实际放的是`datasetCaption`，并不是数据集的`description`。

模型来自`dataset-call.llm.config`中的`finalCallModel`，调用异常时还可尝试`lastCallModel`。2026-06-22的本地配置快照中，主模型是`DeepSeek-V3-2`，备用模型是`data-qwen-32b`，线上当前值仍以DUCC为准。

例如原始问题是「上月北京成交额」，向量阶段留下`tableId=101`的“订单经营”和`tableId=202`的“商品流量”两张相近候选；前者字段包含“下单城市、成交额、支付时间”，后者字段包含“曝光量、点击量、访客数”。LLM可以结合字段名和语义配置返回`{"tableId": 101, "reason": "包含城市和成交额字段"}`，代码再校验这个ID确实属于候选集。

还有一个实现边界：`readyName`使用的是`dataSource != null`判断，而不是非空判断，所以Prompt按默认值返回空字符串时，`readyName`会被设置成空字符串而不是回退原始query；不过选表Prompt还会单独携带原始query，因此不是完全丢失问题信息。

---

<a id="q-06-16"></a>

## 问题：LLM返回异常时一定会回退到向量第一名吗？

不一定，要区分“返回内容无效”和“调用直接抛异常”。如果LLM返回可成功解析的`{}`、`tableId=null`或候选集外的`tableId`，代码会回退到当前向量候选的第一名；这个回退没有再次校验分数，虽然方法名叫`fallbackToTop1IfConfident`，实际只要候选非空就直接取top1。

但如果LLM调用或JSON解析经过内部重试后仍然抛异常，`DatasetRecallStage`没有捕获，异常会继续向上抛，并不会进入top1回退。表向量查询也是类似边界：Vearch返回成功但结果为空时，候选会被清空，LLM不再执行，也不会产生最终表；Vearch接口报错则直接抛异常。

候选准备同样不是“尽力而为”：当前按ID逐个加载详情，其中一张表查询失败，或者字段语义加载、字段ID解析失败，都会中断整个选表流程，而不是跳过坏表继续选择。

---

<a id="q-06-17"></a>

## 问题：选出最终表后，系统还要做什么？

选表结果会组装为`FinalCallDataSetBO`，包含`tableId`、表名、表描述和完整数据集schema。随后`bindFinalDatasetColumns`把这张表预加载好的字段语义、字段列表和`columnId`索引绑定到正式召回上下文，并准备虚拟字段；后面的知识、字段和维值召回只能基于这一张最终表执行，不会再跨表搜索。

如果最终表在候选字段缓存中不存在，代码会直接抛出“最终表字段准备异常”；如果缓存存在但字段列表为空，绑定阶段只记录日志，主流程随后因没有字段候选而停止字段和值召回。也就是说，选中表不代表后续一定能召回字段，候选表的schema准备仍是必要前提。

---

<a id="q-06-18"></a>

## 问题：选表后业务知识是怎么召回的？规则和向量结果如何合并？

知识召回发生在最终表确定之后，只查当前 bookId + datasetId 下配置的业务知识，不跨表（book 级知识代码还没启用）；这张表没配知识就直接返回 null，主流程继续用 query 切词的关键词往下走。整体是「先规则、后向量」两条路：规则是确定性命中，向量是语义补充，最后按 knowledgeId 合并去重。

规则命中有三种方式，按 knowledgeId 去重，一条知识可同时记多种命中：every_time 无条件召回（不看向量分和条数）；custom 只要原始 query 包含它配置的任一关键词就召回；此外无论哪种规则，只要 query 里直接出现知识词条 word 本身，都作为兜底召回（注意只比 word，不比它的同义词）。这三种都不用等向量分数。

规则跑完后，只要还有切词，就把全部切词批量 embedding、并发查知识向量空间（不管配置的知识和 query 内容是否字面匹配都会去查，目的是尽量多召回、宁多勿漏），Vearch 只按 bookId AND datasetId 过滤；已被规则命中的知识不下推给 Vearch，而是在返回后由 Java 排除。向量结果先在每个词内部按分数降序、knowledgeWordMinScore 阈值和条数过滤，再跨词按 knowledgeId 去重（同一知识被多词命中只留最高分），最后全局限制到 knowledgeCallCount 条。关键点：规则直接命中的知识不占向量名额，所以最终知识总数可能超过配置条数。异常上不做部分降级——批量 embedding 或任一关键词的 Vearch 任务报错，整个知识召回直接抛异常；只是某个词查不到结果不算异常。

举个短例：选中「销售订单」表，用户问「今年北京的 GMV，剔除取消订单」，切词得到「北京、GMV、今年」。假设配了三条知识：「有效订单口径」是 every_time，无条件命中（标记 ALWAYS）；「GMV 口径」是 custom 且配了关键词「GMV」，query 含「GMV」故命中（标记 CUSTOM_QUERY）；「华北大区（北京属华北）」没配前两种规则，但「北京」和它语义相关，向量分数过阈值就被召回（标记 VECTOR）。于是本次 recalledKnowledgeIds = 这三条，其余只留在 fullKnowledgeCache 里不返回。若「华北大区」还开了 useValCall，它的解释会再切词把「北京、华北大区」喂给后续维值召回——但这属于知识的关键词扩充角色，知识召回本身不产出 SQL 编码。

---

<a id="q-06-19"></a>

## 问题：召回到的知识如何影响字段和维值？它会与字段直接绑定吗？

知识不会直接绑定字段。`KnowledgeBO`没有`columnId`，最终字段对象也不携带知识ID；它主要通过“解释文本切词扩展召回关键词”和“作为LLM上下文”两种方式影响后续流程。

系统会同时保留`fullKnowledgeCache`和`recalledKnowledgeIds`：前者保存当前数据集下的全部知识，后者只标记本次真正召回的知识。对于已召回且`termType=CUSTOM`的动态自定义知识，还会尝试根据`erp + 原始query`获取动态解释并替换原解释；这里的`termType`是知识内容类型，不等同于`recallRule=custom`。动态接口失败时继续使用静态解释。

第一种方式只处理`recalledKnowledgeIds`中的知识，并且要求知识至少开启`useColumnCall`或`useValCall`之一。系统对每条知识的`explanation`并发调用一次与query切词相同的`prompt.query.simpler-split`和`simpleSplitModel`，不是像query那样并行切两次；最终只取知识切词结果里的`keywords`，忽略其中的`dataSource`和`timeKeywords`。

知识切出的词按开关进入不同链路：`useColumnCall=true`只扩充`columnKeywords`，`useValCall=true`只扩充`valueKeywords`，两者都开就同时扩充，Set负责去重，关键词来源Map会标记为`KNOWLEDGE_SPLIT`。例如知识解释是“GMV使用支付金额字段，活动类型取618”，切词可能得到“GMV、支付金额、活动类型、618”；只开字段开关时这些词只帮助找字段，只开值开关时只帮助找维值，两者都开时两边都会尝试，但并不预先建立“这条知识对应支付金额字段”的固定映射。

知识批量切词使用线程池并发执行，整批默认最多等待5秒；单条LLM切词失败或整批等待超时后，代码会使用当时已经完成的部分结果，不会让所有知识一起失败。因此某条知识即使已经召回，也可能因为解释为空、LLM失败或超时而没有贡献扩展词，但它仍可以出现在最终返回的知识列表中。

第二种方式存在一个当前实现边界：`ChooseColumnLLMStage`和`RerankMergeStage`拼接Prompt时遍历的是`fullKnowledgeCache`，不是`recalledKnowledgeIds`。也就是说，**召回集合控制的是哪些知识返回给调用方、哪些知识参与解释切词扩词，但当前并没有把选字段和精排Prompt限制为仅使用已召回知识**；未召回知识仍可能进入LLM上下文，这会增加无关信息，是后续可以收敛的点。

最终响应只根据`recalledKnowledgeIds`组装知识列表，返回知识ID、词条、关系、解释和同义词；字段结果与知识结果仍是两个平行列表，不形成显式血缘关系。

---

<a id="q-06-20"></a>

## 问题：知识召回完成后，字段和值的召回关键词怎么准备？两路谁依赖谁？

字段召回和值召回使用两份独立关键词集，但初始都包含Query切词：

- Query切词同时复制到`columnKeywords`和`valueKeywords`，此时还没判断某个词是字段还是维值。
- 已召回知识的解释切词，按`useColumnCall`和`useValCall`分别加入两侧；两个开关可以同时开启。
- 字段和值的Embedding分别批量生成。字段侧复用一份缓存，值侧再复用另一份，不是一份全局Embedding缓存。

主线中`ColumnRecallStage`先调用，`ValueRecallStage`后调用，但值召回不读取字段召回结果，因此不是“先确定字段，再只查这些字段的值”。另外一路LLM补字段任务与这条主线并行。

简单例子：

```text
Query切词：北京、GMV
“GMV口径”解释切词：实际支付金额
知识开关：useColumnCall=true，useValCall=false

columnKeywords = 北京、GMV、实际支付金额
valueKeywords  = 北京、GMV
```

---

<a id="q-06-21"></a>

## 问题：字段召回分为哪几路？原名、别名和同义词如何匹配？

候选只来自最终选中表，包括active实体字段和预加载的虚拟字段。字段原名、别名和同义词都会映射回同一个`columnId`，字段初召有四个入口：

1. **字符串规则召回**：代码名叫“正则”，实际做的是区分大小写的相等和包含判断，候选名包含原名、别名和同义词。规则命中分数固定为`1.0`，命中关键词不再进入后续向量检索。
2. **字段原名向量召回**：用剩余关键词查字段原名向量空间，Vearch按当前`datasetId`过滤，返回后再校验字段是否仍在当前表字段缓存中。
3. **别名/同义词向量召回**：两者共用独立向量空间，Vearch按`bookId + datasetId`过滤，Java再按候选`columnId`和当前有效语义配置二次校验。
4. **LLM补字段**：LLM直接看原始Query、最终表全部字段和知识内容，返回字段名称。它是精排前的补充路，只补充前三路没有命中的`columnId`，不补维值。

向量阈值和条数取DUCC配置；源码默认原名向量、别名/同义词向量每个关键词各保留Top3，查询窗口默认为100，不能当作线上当前值。

简单例子：

```text
字段101：原名=实际支付金额，别名=GMV，同义词=成交额
字段102：原名=省份，别名=地区，同义词=区域

Query：按地区看GMV
地区 → 字符串规则命中字段102的别名
GMV  → 字符串规则命中字段101的别名

如果Query改成“成交规模”，字符串规则无法命中，
才会继续用它查原名与别名/同义词向量空间。
```

---

<a id="q-06-22"></a>

## 问题：值召回必须先确定字段吗？基础维值、自定义维值和维度中心有什么区别？

不必须。值召回把全部`valueKeywords`直接送到三类值来源，命中结果自带`columnId`，或根据维度绑定关系反查到`columnId`。这一阶段不实时扫描底表数据。

1. **基础维值**：面向普通表中`dimType=null/1`的非虚拟维度字段。每个词分别查一次编码向量和展示值向量，Vearch按`datasetId`过滤，Java再只保留基础维度字段。
2. **自定义维值**：查用户配置的维值原词和同义词，Vearch按`bookId + datasetId`过滤。索引文档已携带`columnId`和原始真实值，最后还会过滤非active字段。
3. **维度中心**：面向`dimType=2`且配有`dimCode`的字段，用原词调用NER检索。维度中心返回`dimValue + dimCode`，系统再把结果绑定到使用该维度的一个或多个字段。
4. **手动值替换**：`dimType=2`但没有`dimCode`的字段当前只记录日志，不执行值召回。

绑定维度中心的字段如果先命中了自定义非数值原词，还会尝试再去维度中心解析底表编码；解析失败时保留原自定义命中。

简单例子：

```text
valueKeywords：北京、GMV
字段city_code：绑定维度中心的city维度

北京 → 维度中心命中city
     → columnId=city_code字段ID
     → dimValue=北京
     → dimCode=110000

GMV  → 没有命中维值
     → 是否为字段由字段召回链判断
```

如果自定义维值配置了“帝都 → 北京”，Query中的“帝都”可先命中自定义维值，再尝试把原词“北京”解析为编码`110000`。

---

<a id="q-06-23"></a>

## 问题：字段和值候选如何过滤？一路失败会不会导致整体失败？

- **字段候选**：规则命中分数为`1.0`；向量候选按`fieldMinScore`和每词TopN过滤。同一字段的多个命中证据先按`columnId`归组，不会立即折叠成一条。
- **值候选**：先用各来源的初筛阈值，再按“关键词 + 来源 + columnId”分组。组内达到高分线的全保留；没有高分项时，只保留最高分及并列项。
- **数值特殊处理**：如果关键词是数字，候选的`dimValue`或`dimCode`必须数值精确相等；例如`01`和`1`可视为相等，`1`和`10`不会靠语义相似保留。
- **值的最终语义**：`enumValue`是命中的展示词，`originEnumValue`取`dimCode`，也就是下游使用的真实值或编码。`dimCode`不一定是数字。

当前代码没有使用`vectorDimValTop=3`做最终截断；基础维值和自定义维值的源码默认查询窗口分别为200和100，但查询窗口不等于最终保留条数，线上值仍以DUCC为准。

异常处理要分开看：

1. 字段或值的前置Embedding失败，会直接中断整个完整召回。
2. 字段向量召回中任一关键词查Vearch失败，等待其他任务完成后整个字段向量阶段抛错，不使用部分结果。
3. 基础/自定义维值某个词查Vearch失败时跳过该词；维度中心某个词失败时，也会保留其他词的成功结果。
4. 并行的LLM补字段调用重试后仍异常，在汇合Future时会向上抛出，不是静默跳过。

另外两个当前实现点需要留意：基础/自定义维值循环的`index++`位于`try`内，某个词异常后下一个词可能错用Embedding；`isDsl`使用的是`datasetIdList[0]`而不是最终选中表ID，多候选表时需要重点检查。

---

<a id="q-06-24"></a>

## 问题：多路召回的结果怎么合并去重？命中同一字段的多个来源怎么处理？

字段和值都先以`columnId`归组，但不是看到同一`columnId`就立即折叠成一条：

- 字段精排前后合并时，只去掉`columnId + callType + 命中名称 + keyword`完全相同的召回项。
- 最终列累加时，字段变体的key为`columnId + 名称类型 + 命中名称`，同一变体的多个关键词会合并到`allKeywords`。
- 维值最终按“忽略大小写的展示值 + 真实值”去重，重复项保留更高分。同一`dimCode`对应不同展示词时，这些展示词仍会分别保留。

例如“GMV”和“成交额”都命中`columnId=101`：别名和同义词两种命中变体可以同时保留，但它们都指向同一个底表字段。维值“北京 → 110000”被多路召回时，展示值和真实值都相同的记录才会合并。

---

<a id="q-06-25"></a>

## 问题：为什么用向量召回而不是纯关键词/ES匹配？同义词、别名、维值为什么要拆成不同向量空间？

用户表达高度口语化，"成交额、GMV、销售额"指的是同一字段，字面匹配匹不上，向量靠语义相似度才能捕捉这种同义近义。但向量不是万能的，系统是规则（部分词常驻、精确包含）加向量兜底结合用的。

拆成不同向量空间是因为语义粒度和过滤条件都不同：字段名、同义词、别名描述的是"某一列"，维值描述的是"某个取值"，混在一个空间里会互相干扰、阈值也没法分开调。拆开后每个空间独立设相似度阈值和topN，过滤条件（datasetId、bookId、columnId）也各不相同，比如同义词/别名召回要按columnIds过滤到具体字段上，基础维值召回要求字段本身是基础维度字段。

举个反例说明为什么要拆：假如把字段名"下单城市"和维值"北京""上海"塞进同一个向量空间，用户说"北京"时，"下单城市"这个字段名和"北京"这个维值语义上都沾边，很可能互相挤占topN，阈值调高了漏维值、调低了混进字段名。拆开后"北京"只在维值空间里比、"成交额"只在字段/别名空间里比，各自的阈值互不牵连。

---

<a id="q-06-26"></a>

## 问题：dimCode和dimValue的区别是什么？为什么需要"名称→编码"解析？

dimValue是给人看、用来匹配的显示名称（比如"北京"），dimCode是数据库里真正存储的编码（比如110000）。召回阶段用名称找到"用户说的是哪个维值"，但SQL的WHERE条件要填的是真实存储值，所以必须拿到对应的dimCode。这个"名称→编码"的翻译由维度中心（callDimCenter）专门完成，它走的是NER服务而不是Vearch；而基础维值召回会对编码和名称各做一路向量，因为用户两种说法都可能出现。这也是为什么维度和维值都召回了还需要dimCode——召回解决"是哪个"，dimCode解决"WHERE里填什么"。

举个例子：用户说"北京"，维值召回先确认这指的是下单城市字段里的"北京"这个取值，但底层表`order_city`列存的不是汉字而是行政区划码110000，所以最终SQL不能写`WHERE order_city = '北京'`，得翻译成`WHERE order_city = '110000'`。这一步"北京→110000"就是dimCode解析在做的事，缺了它SQL跑出来会一条都匹配不到。

---

<a id="q-06-27"></a>

## 问题：LLM精排是怎么实现的？为什么不用cross-encoder重排模型？

这里的“精排”本身就是LLM裁决，不是LLM之后再挂一个cross-encoder。`RerankMergeStage`把字段和维值候选拼成表格，连同Query和业务知识让LLM输出结构化白名单。

- LLM能结合业务知识做语义判断，但延迟、token成本和输出稳定性都弱于cross-encoder；候选量大或延迟敏感时可考虑后者。
- Prompt约束模型只在给定候选中选择，输出JSON白名单。LLM调用失败或返回空时，整个精排步骤跳过，原始候选保留。
- **当前实现边界**：维值使用LLM过滤结果替换原结果，因此删减真正生效；字段则把原召回和LLM过滤结果取并集，所以当前不会真正删掉字段初召噪声。

例如LLM从`gmv_amt、refund_amt、gmv_cnt`中只选`gmv_amt`，从“北京、上海”中只选“北京”：当前代码会删掉维值“上海”，但三个字段初召结果仍会因并集合并而保留。

---

<a id="q-06-28"></a>

## 问题：全链路并行怎么编排？并发写的地方为什么用了那么多并发工具，是不是冗余？

并发边界要按代码的实际调用顺序来讲：

1. `SplitQueryStage`和`DatasetPrepareStage`用`CompletableFuture`并行，两者都完成后才选表。
2. 知识召回后，LLM补字段异步启动；主线则依次调用字段召回和值召回，两个Stage彼此不并行。
3. 字段原名向量和别名/同义词向量内部按关键词并发，使用线程池、`CountDownLatch`、`ConcurrentLinkedQueue`和`AtomicBoolean`完成执行、汇合、采集和失败标记。
4. 维度中心使用`CompletableFuture`按关键词并发，再用`Semaphore`限制最大并发，源码默认为10；基础维值和自定义维值则按关键词依次查询。

Embedding缓存是“分侧复用”：字段侧批量生成一份Map，供原名与别名/同义词向量召回共用；值侧另外生成一份Map，供基础和自定义维值共用。同一个词如果同时在两侧，当前仍可能分别做一次Embedding。

例如关键词是“北京、GMV”：LLM补字段与主线同时执行；主线先完成字段多路召回，再执行值召回，其中“北京”的维度中心请求可以和“GMV”的维度中心请求并发。

---

<a id="q-06-29"></a>

## 问题：外部依赖（Vearch、LLM、维度中心）挂了怎么办？

不能笼统地说“某一路挂了不影响其他路”，要按阶段区分。选表是后续召回的前置单链路：Vearch接口报错或LLM重试后仍抛异常都会中断，Vearch返回空结果也不会继续LLM；只有LLM返回可解析结果但`tableId`为空或不属于候选集时，才回退向量top1。字段、维值、维度中心和最终精排各自还有不同的异常策略，需要按具体阶段说明。

---

<a id="q-06-30"></a>

## 问题：96%的准确率具体怎么定义和统计？为什么能到这么高？

要如实讲口径：这是字段/维值召回准确率，分母是评测集的query数（或字段数），分子是召回结果与标准答案一致的数量，是离线标注集还是线上回流统计的也照实说，含糊反而显得数字不可信。

能到96%这么高，要主动讲边界：召回被限定在单个数据集范围内，候选字段和维值空间本身就有限，不是全库大海捞针，所以准确率天然偏高。这体现的是对指标边界的清醒认知；一旦放开到多数据集选表，选表的难度和不确定性都会上升，这个数字就不成立了。典型badcase包括别名未覆盖导致漏召、维值编码不一致、LLM误删相关字段、时间词干扰等，都是靠补同义词、调阈值、改prompt约束、剥离时间关键词持续迭代出来的，不是一次成型。

举个例子说明"空间小所以准确率高"：一个订单数据集可能就二三十个字段，用户问"成交额"，候选本来就没几个近义字段，选对的概率自然高；但如果是几百个数据集混在一起选，光"金额"相关的字段就可能有几十个（成交额、退款额、优惠额、客单价……分散在不同表里），难度是数量级的差异。再举个典型badcase：早期"客单价"没配进gmv相关别名，用户一说"客单价"就漏召，后来把它补进同义词才修好——这类就是靠badcase回流一点点补上来的。

---

<a id="q-06-31"></a>

## 问题：这套召回怎么评测？整体评测方案是什么？

以离线评测集（golden set，可重复、可进CI回归）为主，加上线上抽样人工评估或AB为辅。离线用来快速迭代和防回归，线上用来验证真实分布。评测对象既包括单点的字段/维值召回准确率，也包括端到端的问数正确率——单点召回好但最终SQL跑不出正确结果，仍不算成功。

---

<a id="q-06-32"></a>

## 问题：评测集怎么设计？

从几个方面同时保证质量。数据来源上，用线上真实query回流加人工构造的难例/badcase，避免只测简单case；标注上，每条query标注"应召回的字段集合、维值集合"作为标准答案（golden）；覆盖与分层上，按数据集覆盖、按query复杂度分层（单字段、多字段、含维值、含业务知识、含时间、含别名同义词）、按难度分层，保证长尾也被覆盖；规模与平衡上，正负例配平、常见与长尾配平，样本量足够大让指标稳定、不被小样本抖动带偏。

评测指标主要是召回率（应召回的里被召回的比例，衡量漏不漏）、准确率（召回的里正确的比例，衡量准不准）、F1（两者综合），再加端到端的SQL可执行率和问数正确率；而且要分层看——各数据集、各query类型分别统计，避免整体数字掩盖某一类的差表现。

举个一条评测样本长什么样：query="上个月北京的成交额"，golden标注={应召回字段:[gmv_amt]，应召回维值:[北京→110000]}。系统跑完如果召回了{gmv_amt, 北京}就全对；如果漏了"北京"，那这条的维值召回率就掉了；如果多召回一个refund_amt，字段准确率就掉了。把成百上千条这样的样本按"含维值/含时间/含别名"分桶统计，就能看出到底是哪类query拖后腿。

---

<a id="q-06-33"></a>

## 问题：怎么判断一次评测算成功/通过？

看四条一起达标。绝对阈值：核心指标达标，比如字段/维值召回准确率≥96%、召回率≥设定线；分层达标：每个分层不低于各自基线，不允许"整体达标但某一类崩了"；无回归：新版本在全量评测集上不劣于线上旧版本，重点badcase集必须保持通过；端到端兜底：单点召回再好，如果端到端问数正确率没提升，仍不算成功。

---

<a id="q-06-34"></a>

## 问题：召回结果没有唯一标准答案（一个词可能对应多个合理字段）怎么评测？

标注时把标准答案定义成"可接受字段集合"而不是单一字段，用集合级的Recall/Precision而不是严格全等来算；对模糊case做多人标注取一致，或者引入"top-k内命中即算对"的宽松口径，避免因为一个词天然多义就把模型判错。

举个例子：用户只说"金额"，没说是成交额还是退款额，这时候gmv_amt和refund_amt都算合理候选。如果按"必须精确等于某一个字段"来判，模型召回哪个都会被判错一半，不公平；改成标准答案是集合{gmv_amt, refund_amt}、召回命中其中之一或都命中都算对，才符合这个词本身就有歧义的事实。

---

<a id="q-06-35"></a>

## 问题：有没有用自动化评测/LLM as judge？怎么保证judge本身可靠？

端到端语义正确性很难用规则表达时，可以用LLM做自动判定。常见做法是先让一个agent根据case生成评分rubric（评分规则），再由judge按rubric打分或给pass判定。关键是judge要用人工标注集做校准——统计它和人工判定的一致率，并固定rubric减少主观漂移；对高风险结论仍保留人工抽检兜底，不能让"AI评AI"变成自说自话。

---

<a id="q-06-36"></a>

## 问题：评测怎么形成闭环、持续迭代？离线好但线上不好怎么排查？

线上badcase定期回流进评测集，修复后进回归集防止复发；每次改prompt、调阈值、加同义词都先过评测集再上线，形成"发现→标注→修复→回归"的闭环。如果出现离线好线上差，多半是评测集分布和线上真实分布不一致（评测集偏简单或覆盖不全），要补线上回流样本、对齐query分布，同时排查线上特有因素——新数据集冷启动、维值/同义词没同步、延迟超时触发降级等。

---

<a id="q-06-37"></a>

## 问题：这套设计的局限和演进方向是什么？

主要局限有三处：LLM精排的延迟和token成本、知识与字段未显式绑定导致血缘丢失、多数据集选表时准确率下降。对应的演进方向是：保留关键词到知识的血缘关系、引入更轻量的重排替代或分流部分LLM精排、增强选表策略、把评测体系自动化。另外新数据集冷启动（向量和同义词还没建好）时，靠规则层兜底并逐步补齐，或对新数据集降级到更宽的召回策略。

---

<a id="section-07"></a>

# C-side 存储知识相关问题

> 背景：C-side（黄金眼配置侧）主要维护字段别名/同义词、自定义维值、业务名词和召回参数等原始配置。配置页的 `fieldUpdate` 以及业务知识的 `semantics/add`、`semantics/edit` 会同步调用 query-data-analysis 的 JSF 接口，由算法侧生成向量并写入 Vearch；普通 CRUD 接口不一定触发该同步。运行时 query-data-analysis 还会通过 `DatasetRpcService` 拉取字段配置、业务知识和召回参数，再结合 Vearch 完成匹配。
>
> 这里要区分两条写入链路：C-side 保存动作负责字段别名/同义词、自定义维值和业务知识；`DatasetAIEnableService` 则从数据集服务获取名称、字段和部分基础维值，独立构建 `dataset_embedding`、`dataset_schema_embedding` 等向量文档。两条链路不能混为一谈。

---

<a id="q-07-01"></a>

## 问题：C-side 用哪几张表存数据集知识？各自字段和用途是什么？

这套数据集知识配置主要涉及 5 张表，按“数据集元信息 → 字段配置 → 自定义维值 → 业务知识 → 召回参数”拆分。这里说的是核心知识表，不代表 C-side 里只有这 5 张数据集相关表。

**① `t_dataset_info` —— 数据集基本信息（按 bookId + datasetId 定位）**

| 字段 | 含义 |
|---|---|
| id | 自增主键 |
| book_id / dataset_id | 笔记 ID / 数据集 ID，联合定位一个数据集 |
| name | 数据集名称 |
| scene_desc | 数据集场景描述 |
| description | 数据集简介 |
| table_type | 表类型：增量 `incremental` / 全量 `full` |
| modified_by / modified_time | 修改人 / 修改时间 |
| yn | 逻辑有效位，1 有效、0 无效 |

用途：`POST /dataset/add` 只把数据集元信息写入 C-side，本身不调用向量化接口。算法侧 `dataset_embedding` 的 `caption/description/summary` 来自 `DatasetAIEnableService` 获取的数据集详情和生成的摘要，不是直接读取这张表。

**② `t_dataset_fields` —— 字段语义配置（按数据集字段保存）**

| 字段 | 含义 |
|---|---|
| id | 自增主键 |
| book_id / dataset_id | 定位笔记和数据集 |
| field_scene | 字段场景：维度 `dim` / 指标 `index` |
| field_id / field_code / field_name | 原始字段 ID / 编码 / 名称 |
| field_type | 字段类型，如 `numeric`、`text`、`string` |
| field_alias | 字段别名，如“销售额”的别名“GMV” |
| field_synonyms | 字段同义词，C-side 中以逗号分隔字符串保存 |
| field_description | 字段描述 |
| calculation_method | 聚合方式，如 `sum/count/distinct_count/avg/max/min` |
| if_active | 字段是否参与运行时召回 |
| modified_by / modified_time | 修改人 / 修改时间 |
| yn | 逻辑有效位 |

用途：`POST /v1/dataset/fields/fieldUpdate` 更新字段配置，并同步调用算法侧 `updateSchemaSemantic`。算法侧会把别名和每个同义词分别构造成向量文档，写入字段同义词 space。`if_active` 不写入 Vearch，但 query-data-analysis 运行时会从 C-side 拉取该配置，用它过滤候选字段。

**③ `t_dataset_dimension_synonyms` —— 自定义维值及同义词（按自定义维值保存）**

| 字段 | 含义 |
|---|---|
| id | 自增主键 |
| book_id / dataset_id | 定位笔记和数据集 |
| field_id / field_code | 维值所属字段 |
| dim_id | C-side 保存的维值 ID |
| dim_name | 真实维值原词，如“北京市” |
| synonym_word | 维值同义词，逗号分隔，如“帝都,首都,北京” |
| modified_by / modified_time | 修改人 / 修改时间 |
| yn | 逻辑有效位 |

用途：`fieldUpdate` 仅在 `field_type=string` 时维护这张表，更新方式是先物理删除该字段已有维值，再批量插入本次 `dimensionList`；空列表表示清空。同步到算法侧时传递的是 `dim_name` 和拆分后的 `synonym_word`，算法侧会同时保存真实维值和同义词文档；`dim_id` 只保留在 C-side，没有进入这次 Vearch 写入参数。

**④ `t_business_semantics` —— 业务名词知识（按业务名词保存）**

| 字段 | 含义 |
|---|---|
| id | 自增主键，同时作为算法侧 `knowledgeId` |
| book_id / dataset_id | 定位笔记和数据集 |
| term | 业务名词，如“大促”“客单价” |
| connector | 连接关系，如包含、等于 |
| explanation | 业务名词的语义说明 |
| synonyms | 同义词，逗号分隔 |
| recall_rule | 召回规则：`follow` / `custom` / `every_time` |
| keyword | 自定义召回关键词，逗号分隔 |
| involved_recall_name | 是否参与字段名相关处理 |
| involved_recall_value | 是否参与字段值相关处理 |
| term_type | 0 通用业务知识 / 1 自定义知识 |
| term_detail | 扩展信息，Java 参数是 Map，入库前序列化为 JSON 字符串 |
| modified_by / modified_time | 修改人 / 修改时间 |
| yn | 逻辑有效位 |

用途：`POST /v1/semantics/add` 新增，`POST /v1/semantics/edit` 编辑，并同步调用算法侧业务知识接口，当前 C-side 这条链路写入的是 dataset 级 `semantic_knowledge_embedding`。运行时 `every_time` 直接召回，`custom` 在关键词命中时直接召回；没有被前置规则命中的剩余知识还会进入向量召回，因此不要简单说成“只有 follow 才走向量”。

**⑤ `t_recall_settings` —— 业务知识向量召回参数（按 bookId + datasetId 使用）**

| 字段 | 含义 |
|---|---|
| id | 自增主键 |
| book_id / dataset_id | 定位笔记和数据集 |
| recall_threshold | 业务知识向量召回的相似度阈值；未配置时查询链路补 0.8 |
| recall_limit | 业务知识向量召回条数；未配置时查询链路补 2，表中是 Float，算法侧向上取整为整数 |
| modified_by / modified_time | 修改人 / 修改时间 |
| yn | 逻辑有效位 |

用途：`POST /v1/recallSettings/semanticsEdit` 按 `(bookId,datasetId)` 查询后插入或更新。它只影响业务知识的向量召回；字段和维值召回使用 DUCC 中的另一套阈值与条数配置。

补充一个一致性边界：这里的“同步”指应用层同步发起 JSF RPC，不是 C-side 数据库与 Vearch 的分布式事务。尤其 `semantics/add` 在本地插入成功后会捕获并记录下游异常，因此可能出现 C-side 已保存、Vearch 尚未更新的情况，不能描述成强一致。

---

<a id="q-07-02"></a>

## 问题：这些内容分别写到哪张表，又进入哪个 Vearch space？

实际 space 名来自 DUCC 配置，下面使用仓库静态建表文件中的名称。核心原则是：C-side 表保存配置原文，保存动作只同步可配置的语义知识；数据集名称、原始字段和基础维值由算法侧 AIEnable 独立处理。

| 内容 | C-side 落表 | Vearch space | 实际写入来源 |
|---|---|---|---|
| 数据集名称、描述、摘要 | `t_dataset_info` 只保存一份 C-side 元信息，不直接同步 | `dataset_embedding` | AIEnable 从数据集服务取详情并生成摘要 |
| 原始字段名、字段描述 | `t_dataset_fields` 只保存一份 C-side 配置，不直接灌该 space | `dataset_schema_embedding` | AIEnable 从数据集详情的字段列表构建 |
| 字段别名、字段同义词 | `t_dataset_fields` | `dataset_schema_synonyms_embedding` | C-side `fieldUpdate` 同步 RPC |
| 自定义维值及同义词 | `t_dataset_dimension_synonyms` | `dataset_schema_custom_dimension_value_embedding_new` | C-side `fieldUpdate` 同步 RPC |
| 基础维值 | 无对应 C-side 表 | `dataset_schema_dimension_value_embedding` | AIEnable 异步抽取符合条件字段的去重维值 |
| dataset 级业务知识 | `t_business_semantics` | `semantic_knowledge_embedding` | C-side `semantics/add`、`semantics/edit` 同步 RPC |
| 业务知识阈值和条数 | `t_recall_settings` | 不写 Vearch | 运行时通过 JSF RPC 从 C-side 获取 |

这里最容易说错的是基础维值：它不是“全量业务数据”，也不一定覆盖该字段的所有历史取值。算法侧只处理符合条件的维度字段，按单字段执行 `SELECT DISTINCT`，受分区条件和 `queryDimLimitForEmbedding` 限制，然后把查到的有限去重维值写入 Vearch。

---

<a id="q-07-03"></a>

## 问题：Vearch 里只存向量吗？会不会保存原始文本？

会同时保存“原始文本/元数据 + 向量”，不是只存一串向量。这样召回命中后，可以直接知道命中了哪个数据集、字段、维值或业务词，不需要用向量再反查原文。

| Vearch space | 保存的原始文本和元数据 | 向量字段 |
|---|---|---|
| `dataset_embedding` | `datasetId/caption/description/topic/owner/creator/createTime/modifyTime/summary` | `vectorCaption/vectorDescription/vectorSummary` |
| `dataset_schema_embedding` | `datasetId/columnId/columnEn/columnCn/columnDescription/columnType/fieldType` | `vectorColumnCn/vectorColumnDescription` |
| `dataset_schema_dimension_value_embedding` | `datasetId/columnId/dimCode/dimValue` | `vectorDimCode/vectorDimValue` |
| `dataset_schema_synonyms_embedding` | `bookId/datasetId/columnId/type/synonym` | `vectorSynonym` |
| `dataset_schema_custom_dimension_value_embedding_new` | `bookId/datasetId/columnId/type/dimOriginalValue/dimCode/dimValue` | `vectorDimValue` |
| `semantic_knowledge_embedding` | `bookId/datasetId/knowledgeId/word/explanation/relation/type` | `vectorWord` |

业务知识的 Java 文档对象还携带 `termType/termDetail`，但仓库内 `semantic_knowledge_embedding` 静态建表模板没有声明这两个字段。它们线上是否真正落入 Vearch，要以 DUCC 当前指向的 space schema 为准，仅凭静态代码不能确认。

所以“不会存实际值”这个说法不准确：基础维值和自定义维值都会把查到或配置的原始词写进 Vearch，只是不会保存整张 Dataset 的完整明细行，也不会把每条业务记录复制一份到 Vearch。

运行时可以按用途理解：选表查 `dataset_embedding`，原始字段查 `dataset_schema_embedding`，字段别名/同义词查 `dataset_schema_synonyms_embedding`，基础维值查 `dataset_schema_dimension_value_embedding`，自定义维值查 `dataset_schema_custom_dimension_value_embedding_new`，业务知识查 `semantic_knowledge_embedding`。

---

<a id="section-08"></a>

# Tree 探索相关问题

> 背景：这是取数分析 MAS 里的"树形执行器"（Tree Executor）模式，作为 Planner-Executor 之外的另一条执行链路。
> 核心思路是把常见分析场景沉淀成一棵配置化的树（JSON），执行时由代码按节点类型确定性地调度，
> 而不是每次都让 planner 现场生成一大段计划。整条链路涉及：执行前的"树调整"（Tree_Adjuster）、
> 五种节点类型的分派、按依赖闭包做的上下文隔离（TreeContextExecutor）、以及面向前端的虚拟容器折叠。

---

<a id="q-08-01"></a>

## 问题：Tree 执行模式到底解决什么问题？和 Planner-Executor 是什么关系？

它解决的是"常规、可复用的分析场景每次都要重新规划"这个浪费。Planner-Executor 的流程是：用户来一个 query，planner 现场生成一大段计划，再由下游 executor 逐步执行；这套灵活，但对那些反复出现、链路基本固定的分析场景（比如"品牌 GMV 对比行业、异常时下钻 SKU"）来说，每次都让 planner 从零规划既慢又不稳定。

Tree 模式把这类沉淀下来的分析链路提前写成一棵结构化的树，执行时不再调用 planner，而是由执行器代码顺着树走、按节点类型分派。它和 Planner-Executor 不是替代关系，而是互补：常规场景走树（快、稳、可控），开放式或没有对应模板的场景仍然走 planner。触发方式也是自动的——[master_tool.py](applications/master_agent/tools/master_tool.py) 的 `get_input_mode` 检测到 `shared_data["analysis_knowledge"]` 是非空 dict 时返回 `tree_executor`，否则回退到常规规划链路。

---

<a id="q-08-02"></a>

## 问题：整条 Tree 执行链路是怎么走的？从入口到出结果

入口在 `super_master_workflow` 命中 `"tree_executor"` 分支。它先调用 `prepare_adjusted_tree` 加载参考树、交给 Tree_Adjuster 结合 query 做一次结构调整；如果调整结果为 None（判定 query 与这棵树无关）就直接回退到普通报告链路，否则带着 `{query, adjusted_tree}` 去调 `tree_master`。

`tree_master` 是个 WorkflowAgent，真正的执行逻辑在 `tree_executor_workflow`：它先把 `plan_type` 标成 tree_mode，用 `query + tree_id/title/root_node_id` 去 ES 知识库做一次树级别的知识召回并缓存，然后创建 `TreeContextExecutor` 开始执行。执行器从 `root_node_id` 出发，把树当成一个链表，顺着每个节点的 `next` 指针往下走（`_execute_from_node`），每到一个节点就按它的 `type` 分派（`_dispatch_node`）到对应处理函数。走到某个节点 `next` 为空时链路结束，最后做一次树级总结返回。

关键点在于：**这条主干的推进完全由执行器代码控制，LLM 不参与"下一步该走哪"的决策**。

---

<a id="q-08-03"></a>

## 问题：树里有哪几种节点？各自特点是什么？

一共五种节点类型，覆盖了顺序、分支、并行、循环、嵌套这几类控制流需求：

| 节点类型 | 作用 | 特点 |
|---|---|---|
| compute | 真正干活的数据处理节点 | 调下游 expert（取数/分析/可视化），是唯一产生业务数据的节点 |
| condition | 条件分支 | 由 LLM 做一次二选一/多选一判断，决定接下来走哪个分支 |
| parallel | 静态并行 | 树里预先写死的若干条分支并发执行 |
| fan_out | 动态扇出 | 运行时根据数据动态决定重复 N 次（比如对召回到的 N 个 SKU 各跑一遍） |
| subtree | 子树嵌套 | 递归执行一棵完整的子树，支持模块化复用 |

其中 compute 是唯一"产生数据"的节点，condition 只做路由判断，parallel/fan_out/subtree 都会展开出内部节点、并被包进一个虚拟容器节点里（见后面容器折叠的问题）。

---

<a id="q-08-04"></a>

## 问题：执行的时候，是把整段树 JSON 一次性丢给 LLM 让它自己决定调用顺序吗？

不是。是**代码拿到 JSON，按结构依次调用下游 agent，而不是 agent 自己看着 JSON 决定该调谁**。

执行器把树当链表遍历，`_execute_from_node` 顺着 `next` 走，`_dispatch_node` 用 `match node.type` 分派到 `_execute_compute` / `_execute_condition` / `_execute_parallel` / `_execute_fan_out` / `_execute_subtree`。LLM 只在两个很窄的地方被调用：一是 compute 节点里执行"这一个节点的具体任务"，二是 condition 节点里做"走哪条分支"的判断。**路由、循环、并发编排这些控制流全都是确定性的 Python 代码**，LLM 看不到全局树、也无权决定整体走向。这样做的好处是执行路径可控、可复现、可观测，避免了让 LLM 自主编排带来的不确定性。

---

<a id="q-08-05"></a>

## 问题：面试重点——为什么用 tree，而不是把 graph 计划存下来复用？

这个问题的关键不在"树还是图"这个拓扑形状上，动态图完全也能实现路由。真正的区别在**控制流的归属**。

现有 Planner-Executor 的范式是：planner 生成的每一步任务，都**无条件地**交给下游 executor LLM 去执行——计划里有 5 步就一定调 5 次 LLM，没有"先判断要不要执行"这一层。Tree 模式引入的，是一个**由代码掌控的"先判断、再执行"层**：condition 节点让代码根据 LLM 的一次判定结果决定走哪条分支、甚至提前终止某些分支，parallel/fan_out 让代码决定并发与重复次数。也就是说，它把"执行与否、执行几次、执行哪条"的控制权从 LLM 手里收回到代码里。

所以正确的回答是：不是因为树的形状更好，而是因为我们需要一个**代码拥有控制流、能做条件短路和早停**的执行层，而这恰好是原有 planner-executor 缺失的（它每一步都必然触发一次 LLM 调用）。把它做成配置化的树只是承载这套控制流最直接的形式；换成动态图也能做，但那要改动更多现有代码，而树以最小代价实现了"决策与执行分离"。

---

<a id="q-08-06"></a>

## 问题：Tree_Adjuster 是干什么的？为什么执行前要先"调整"树？

参考树只是一个模板/知识，用户的真实 query 未必和它完全对得上——可能分支多余、维度缺失、条件失效、节点类型需要转换。Tree_Adjuster 就是在真正执行前，由 LLM 结合 query 把模板树改写成"这次真正要跑的树"。

它的核心是一个**三态相关性判断**：query 与树完全无关时输出空树 `{}`，执行器据此直接跳过树执行（返回 None）；query 命中树根、整棵都要用时保留全树；query 只涉及部分维度时做剪枝或重新选根。举例：模板里 `parallel_gmv_vs_industry_diagnosis` 下挂了好几条诊断分支，如果用户只说"分析一下 GMV"，Adjuster 会把无关分支裁掉，只保留相关的那条。

它的安全性设计值得讲：LLM 输出的 JSON 会经过代码侧校验（根节点存在、引用的节点都存在、无环），再剪掉从根不可达的孤立节点；整个调用包在 try/except 里，**一旦解析或校验失败就安全回退到原始树**，保证执行不中断。而且 Tree_Adjuster 本身是 ChatAgent，没单独设 retries，继承默认值 2，所以是"重试 + 回退原树"的双保险。

---

<a id="q-08-07"></a>

## 问题：并行分支之间会互相污染上下文吗？TreeContextExecutor 怎么做隔离的？

如果不管，是会污染的：两条并行分支各自产生的中间文件、中间结论会堆在同一片共享空间里，A 分支能看到 B 分支的半成品，互相干扰。所以专门有 `TreeContextExecutor` 来做隔离，一句话概括它的规矩就是——**每个节点只保存"自己新造的东西"，谁要参考上游就执行时顺着依赖关系现去取，兄弟分支之间因为没有依赖关系，所以互相取不到**。

用"档案柜 + 办公桌"来理解最直白。给每个节点想象两样东西：一个**档案柜**，永久保存、里面只放这个节点自己产出的成果；一个执行时临时支起来的**办公桌**，把需要参考的上游文件复印几份摊上去，活干完桌子一收就空了。这两样是分开的，关键就在这。

`TreeContextExecutor` 重写了三个钩子来落实这套规矩：compute 节点开工前，`_augment_call_arguments` 顺着 `depends_on` 往上追出这个节点的所有祖先，`inject_visible` 把祖先们产出的文件和记忆复印到它的办公桌上（只复印祖先的，不相干的分支一律不给）；节点干完活，`_register_node_files` 用"结束时的桌面 - 开工时复印进来的"做差集，把这个节点**新造**的那部分文件/记忆归进它自己的档案柜——注意复印来参考的那些不会被归档，桌子收掉就没了；遇到 subtree 这类分支时，`_scope_branch_request` 会把桌面清空、做进一步隔离，防止子树串用外面的东西。

结合例子。假设 A 查出全店 GMV 明细产出 `gmv.csv`，进了 A 的档案柜；B 依赖 A，把 `gmv.csv` 复印到桌上参考，算出"同比下滑 20%"产出 `trend.csv`，**只把 `trend.csv` 归进 B 的档案柜**（`gmv.csv` 是借来看的，没归档）。现在有两条并行分支：C 依赖 B 要写归因结论，D 是另一条独立分支在算别的指标。C 开工时系统会顺着 C→B→A 一路复印，所以 C 的桌上既有 B 的 `trend.csv` 也有 A 的 `gmv.csv`，够它核对；但 D 跟 A、B、C 都没有依赖关系，系统不会把它们的文件复印给 D，反过来 C 也看不到 D 的中间产物——两条并行分支就这样天然隔开了，谁也污染不了谁。

有个容易绕进去的点顺带提一句：既然 B 执行时看到过 A，为什么 C 不直接从 B 那儿拿 A、非要自己追到 A？因为 B 看 A 用的是"复印件摊桌上"，活干完桌子收了，A 的东西从没进过 B 的档案柜——B 的柜子里只有 `trend.csv`。C 去翻 B 只能翻到 `trend.csv`，想要 `gmv.csv` 就只能自己再往上追到 A 的档案柜。这么设计是为了不滚雪球：要是让每一步都把看到的上游也存一份，`gmv.csv` 就会在 B、C、D……每个柜子里各存一份，越滚越肿；现在全系统每份原件只有一处，谁要谁去源头取。

最后一句可以点出设计上的统一性：这套"只存增量、按依赖闭包现取"的机制，和图执行器（graph_executor）共用同一个上下文中枢（`shared_data['_node_ctx']` 上的 `NodeContext`/`NodeContextGraph`/`TreeContext`），所以树模式和图模式的隔离逻辑是一套东西，没有各写各的。

---

<a id="q-08-08"></a>

## 问题：subtree / parallel / fan_out 会展开一堆内部节点，前端怎么不被刷屏？

靠一个"虚拟容器节点"来折叠。执行器遇到这类会展开的分支时，`_create_container_node` 会先调 `branch` 工具造出一个虚拟容器节点，让分支内部的所有真实节点都挂在这个容器下。

`branch` 来自 [container_tools.py](applications/master_agent/agent/tree_executor/container_tools.py)，是一个**不走 LLM 的占位工具**，实现就是把传入的 label 原样返回、没有任何真实计算。它的意义有两个：一是前端可以把整条分支折叠成一个卡片，用户看到"一个分支块"而不是一屏散落的子节点；二是主干链路的前驱指针推进到这个容器（`container_father_id`）而不是分支内部节点，把分支内部封装起来，不污染主干的串行建链。

配合它的还有**分层总结**：每条分支跑完先做 `branch_summary`，整棵树跑完再做 `tree_summary`，让长链路的输出逐层收敛而不是把所有中间结果堆给用户。

---

<a id="q-08-09"></a>

## 问题：tree_master 挂的两个 tool 分别是干什么的？

`tree_master` 挂了 `branch` 和 `planner_tools` 两个 tool，目的完全不同。

`branch` 就是上一题讲的虚拟容器占位工具，为前端折叠和链路建链服务，跟数据分析本身无关。`planner_tools` 是一个 FunctionHub（工具集合），里面有 `make_graph_plan`（"设定图结构的分析计划"，这是 planner-executor 模式用的，树模式下不会真正调用，函数体甚至直接返回"此时使用 make_graph_plan 是错误的"）和 `search_knowledge`（"检索知识库，获取分析大纲、背景知识"）。因为把一个 FunctionHub 挂成 tool 等于把它内部所有 tool 一次性暴露出去，所以 tree_master 同时拿到了这两个的权限，但树模式真正用到的只有 `search_knowledge`——在 `tree_executor_workflow` 里用它做树级知识召回并缓存到 `shared_data['knowledge_cache']`。`make_graph_plan` 只是同属一个 hub 被顺带带进来，在树模式里不发挥作用。

---

<a id="q-08-10"></a>

## 问题：树里 condition 节点匹配 query 时用的是相似度检索吗？

不是。这里要区分两种完全不同的"匹配"。query 和树结构之间的匹配（Tree_Adjuster 做的、condition 节点做的）是**LLM 的语义推理**，不是向量相似度；而 query 和知识库之间的匹配（`search_knowledge` 做的）才是 **ES 相似度检索**（阈值 0.4），并且那套检索算法在外部的 ge-data-mas 服务里，不在本仓库。

具体到 condition 节点，它是让 LLM 读当前上下文做一次分支判断（`_parse_condition_result` 解析结果决定走哪个 target），本质是推理而非检索。参考树本身也不是从外部存储召回的，而是从本地文件（测试阶段是 `test_real_case.json`）加载的模板，`analysis_knowledge` 只是触发树模式的开关。

---

<a id="q-08-11"></a>

## 问题：每个 Agent 都能拿到用户原始 query 吗？数据是怎么在链路里传递的？

能，但要分清几种作用域不同的容器。OxyRequest 里有三层嵌套的数据作用域：`arguments`（单次调用作用域）⊂ `shared_data`（单个 request/trace 作用域）⊂ `group_data`（会话组作用域）。用户原始 query 放在 `shared_data["query"]` 里，整条链路上所有 Agent 都能通过 `shared_data` 拿到；而每个节点自己那一小步的任务描述放在 `arguments["query"]`。

区分当前层还是顶层用的是 `master_level` 参数：`get_query(master_level=False)` 取当前节点这一层的 `arguments["query"]`，`master_level=True` 取顶层用户的 `shared_data["query"]`；short_memory 同理，`master_short_memory` 对应顶层用户会话。`get_query` 返回的不只是纯 query，还会拼上附件（attachments）转成的 markdown 链接，所以看起来"多了一堆东西"。

顺带一提，`short_memory` 是**历史记忆**（从 ES 的 `{app}_history` 索引里查过去已完成的对话轮次），不是当前这一轮的内容；master 模式下用 `call_stack[:2]` 拼出会话名来定位顶层用户会话的历史。

---

<a id="q-08-12"></a>

## 问题：一个 compute 节点执行失败了，整棵树会挂吗？失败是怎么兜底的？

不会一挂到底。节点执行走的是 OxyRequest 的重试机制，`retries` 默认是 2，失败会重试；但要注意这是**无状态重试**——重试时不会把上一次的失败原因带进去（这一点和 ReAct 那种"带着报错信息再想一次"不同），所以它更适合应对偶发的超时/抖动，而不是靠反馈纠正逻辑错误。重试仍失败会返回 FAILED 状态，执行器拿到失败输出后不会中断整条链路，而是把它当作这个节点的产出继续往下走。

这里可以延伸出一个诚实的局限点：树模式目前没有"某节点失败就走备用分支"这种细粒度的失败路由，condition 是按业务语义判断而不是按成败判断。如果面试官往这个方向追问，可以把它作为一个明确的可改进方向来谈（下面单列了）。

---

<a id="q-08-13"></a>

## 问题：fan_out 动态扇出的并发是怎么控制的？会不会打爆下游？

fan_out 是运行时根据数据决定重复 N 次（比如召回到 N 个 SKU 各跑一遍诊断）。它和 parallel 的区别在于：parallel 的分支数在树里写死，fan_out 的次数到运行时才知道。正因为 N 不可控，这里是需要重点说清楚的地方——如果不加限制，N 很大时会同时发起大量下游调用，既可能打爆下游服务，也会让上下文和 token 成本失控。

面试时如果被问到这点，比较稳的回答是：先说明 fan_out 的每个分支同样会被包进虚拟容器、按依赖闭包隔离上下文（所以彼此不串扰），再坦诚"对超大 N 的并发上限 / 分批 / 采样"是需要显式设计的点——如果代码里做了并发上限就直接讲清楚上限值和位置；如果还没做，就把它作为已识别的风险和改进项讲出来，比硬说"没问题"更可信。

---

<a id="q-08-14"></a>

## 问题：这套 Tree 执行机制还有哪些可改进点 / 演进方向？

这类"你自己怎么看这套设计的不足"是高频收尾题，准备三个方向就够：

第一是**参考树目前从本地文件加载、且只有测试用例**，覆盖不了真实场景的多样性；演进方向是把树沉淀为可管理的知识资产（版本化、按场景检索选树），让 Tree_Adjuster 从"调整一棵给定树"升级到"先选对树再调整"。

第二是**失败处理偏粗**：无状态重试 + 失败不阻断，但缺少按成败的失败路由和带反馈的重试；可以引入失败降级分支，或让 condition 支持基于节点状态的判断。

第三是**fan_out 的并发与成本治理**：动态 N 需要并发上限、分批、必要时采样，以及对总 token 预算的约束，避免大扇出场景下延迟和成本失控。

如果还想再加一个，可以提"树与图执行器的进一步统一"——两者已经共用上下文中枢，但调度层还是两套，长期看有收敛空间。

---

<a id="q-08-15"></a>

## 问题：Tree_Adjuster 用 LLM 改写树结构，怎么保证改出来的树一定能跑？

靠"LLM 负责语义、代码负责合法性"的分工。LLM 只管根据 query 决定删/增/改哪些节点，但它的输出绝不被直接信任：代码侧会先用 `extract_first_json` + `json_repair` 把输出抠成 JSON（容忍 LLM 输出里的格式噪声），再做三项硬校验——root 节点必须存在、所有 `next`/`parallel_ids`/`conditions` 引用的节点都必须存在、从根出发不能有环，最后还会剪掉从根不可达的孤立节点。

任何一步失败都不是报错退出，而是**安全回退到原始参考树**照常执行。所以最坏情况也只是"没调整成功、用了原模板"，而不会因为 LLM 吐了一棵非法的树导致执行崩溃。这套"LLM 提议、代码把关、失败兜底"的模式，和前面后置裁判里"出题人不许直接打分、判定权归代码派发"是同一种防御性设计思路——凡是 LLM 的结构化输出要驱动后续确定性流程的地方，都不能省掉代码侧的校验与回退。

---

<a id="q-08-16"></a>

## 问题：短记忆（short_memory）在树模式里是怎么起作用的？为什么是"历史"而不是当前轮？

`short_memory` 承载的是**跨轮次的历史对话**，来源是 ES 的 `{app}_history` 索引，查的是过去已经完成并落库的对话轮次，所以它天然是"上几轮"的内容，而不是当前这一轮正在进行的中间态——当前轮还没结束，也就还没写进 history 索引，自然拉不到。默认每个 local_agent 在 `_pre_process` 里如果发现请求还没带 short_memory，就会自己去 `_get_history` 拉一次。

树模式的特殊之处在于两点。一是它区分了当前节点层记忆和顶层用户记忆：普通 short_memory 用 `session_name`（caller+callee）定位，`master_short_memory` 则用 `call_stack[:2]` 拼出顶层用户会话名去拉，让节点能看到"用户和主 agent 之间"的历史而不只是自己这一小段。二是它允许运行时通过 `arguments` 覆盖 `is_retain_master_short_memory`，而不必依赖 agent 实例上写死的静态配置——这样同一个 compute agent 在树模式下能被灵活控制"要不要带上顶层用户历史"。而节点执行期间**当前轮内**产生的中间信息，走的不是 short_memory，而是前面讲的依赖闭包上下文（`_node_ctx`）。

---

<a id="q-08-17"></a>

## 问题：文档是如何切分的？

我用的是一个**结构感知的三层递进式切分器**（`StructureAwareChunker`），思路是尽量沿文档天然的语义边界切，而不是无脑按长度切。参数是单块上限 1024 字符、硬切时保留 32 字符重叠。

第一层**按 Markdown 标题切**：正则识别 `#`~`######` 六级标题，维护一个标题栈，把从根到当前标题的路径用 ` > ` 拼成 title path（如 `使用手册 > 安装 > 环境要求`），两个标题之间的正文连同这条路径组成一个块。这一步最关键的收益是**给每个块注入了层级上下文**——存进 ES 时块内容会以标题路径开头，解决了 RAG 里"孤儿块脱离上下文后语义不完整"的经典问题：哪怕正文里没出现"打印机、安装"这些词，用户查"打印机安装环境"也能靠标题路径的向量命中。第二层**QA 抽取**：用一个 TEXT/QA 状态机识别以 `Q:/Question:/问题：` 开头的问答段，把问和答绑成一个 `qa` 块，避免问答对被拦腰切断。第三层**定长回退**：只对超过 1024 字符的普通文本触发，按 `\n\n → \n → 。！？ → .!? → 空格` 的优先级从段落到句子递归找最靠近上限的切点；只有一个分隔符都找不到时才硬切，并让下一块从 `上限-32` 处起始形成重叠。所以正常按标点切时块之间是**没有重叠**的，重叠只是极端情况的兜底。

面试里我会补充自己发现的几个短板和优化方向，体现对方案边界的判断：一是**代码块保护**——现有正则只看行首有没有 `#`，不判断是否在 ``` 围栏内，导致代码里的 `# 安装依赖` 这类注释被误判成一级标题、把代码块切碎；优化是加一个 `inCodeFence` 开关，围栏内的行一律当正文、跳过标题匹配（顺带还省掉了正则开销）。二是**表格保留表头**——定长切分不理解表格结构，大表会在某个 `\n` 处被切开，后半块丢了列名变得不可用；优化是切分前先分出表格区，超长表格按数据行分批、每批重新拼上表头行，保证每个表格块自带列名。三是**长 QA 答案**——`qa` 块目前不走定长切分，超长答案会超出 embedding 输入被截断，可以对超限的 qa 块也补一层切分。

关于性能：有人会问"逐行判断会不会慢"，答案是不会。切分本来就是对文档一次 O(n) 单趟扫描，新增的围栏/表格判断只是廉价的字符前缀比较，比正则便宜一个量级；而整条链路的耗时几乎全在下游——每个块要调 LLM 生成摘要、调 embedding 向量化，都是几百毫秒的远程 HTTP 调用，切分本身的 CPU 开销在总时间里连零头都不算。真正的性能优化方向是 ES 批量写入（`_bulk`）和给块级 embedding 换一个带限流的专用线程池，而不是省这几个字符比较。
