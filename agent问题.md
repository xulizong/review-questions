# Agent 面试问题

## 问题：什么是 Agent 范式？

### 简短回答

Agent 范式是一种构建 AI 系统的方式：让大模型不只生成一次性回答，而是围绕一个目标，持续进行“观察、思考、行动、反馈”，并根据执行结果动态调整下一步，直到完成任务或触发终止条件。

一句话概括：**Chatbot 的重点是回答问题，Agent 的重点是把事情推进到完成。**

### 核心运行循环

```text
接收目标 → 获取信息 → 制定计划 → 调用工具执行
   ↑                              ↓
   └──────── 检查结果并修正 ────────┘
```

这一过程通常会循环多次，而不是只进行一次模型推理。

### Agent 的主要组成部分

1. **目标（Goal）**：系统最终需要完成的任务。
2. **感知与上下文（Perception / Context）**：用户输入、文件、网页、数据库以及环境状态等信息。
3. **推理与规划（Reasoning / Planning）**：分析当前状态、拆解任务并决定下一步行动。
4. **工具（Tools）**：终端、浏览器、搜索引擎、数据库、外部 API、代码编辑器等。
5. **记忆（Memory）**：保存任务进度、历史交互、用户偏好或长期知识。
6. **反馈机制（Feedback）**：根据工具返回的结果判断行动是否有效，并修正计划。
7. **终止条件（Termination）**：任务完成、达到时间或成本限制，或者遇到需要用户决策的情况。

### 与传统 Chatbot 和 Workflow 的区别

| 类型 | 主要特点 |
| --- | --- |
| Chatbot | 根据当前输入生成回答，通常不会主动执行后续步骤。 |
| Workflow | 执行开发者预先定义好的固定流程，稳定且可预测。 |
| Agent | 根据目标和环境反馈动态决定下一步，更灵活，但也有更高的不确定性。 |

### 示例

用户提出：“帮我找出这个项目测试失败的原因并修复。”

普通聊天模型可能只会给出排查建议；Agent 则可能：

1. 查看项目文件和错误日志。
2. 推断可能的故障原因。
3. 运行测试验证推断。
4. 修改代码。
5. 再次运行测试。
6. 如果仍然失败，继续分析和调整。
7. 测试通过后，汇报修改内容和验证结果。

### 常见 Agent 形式

- **工具调用 Agent**：根据任务自动选择搜索、代码执行或数据库等工具。
- **ReAct Agent**：交替进行推理（Reasoning）和行动（Acting）。
- **规划型 Agent**：先将复杂任务拆解，再逐步执行和校验。
- **多 Agent 系统**：多个 Agent 分别承担研究、编程、审查等角色并相互协作。
- **人在回路 Agent（Human-in-the-loop）**：在高风险或关键操作前请求人工确认。

### 面试中的补充说明

Agent 并不意味着 AI 拥有真正的自主意识。它本质上是一种工程架构：以大模型作为决策核心，配合工具、状态管理、循环执行、反馈机制和安全边界，使系统能够处理多步骤、路径不完全确定的任务。

实际设计时还需要关注幻觉、错误累积、循环失控、权限安全、执行成本以及结果可观测性等问题。对于规则明确、步骤固定的任务，传统 Workflow 往往更稳定；对于开放性强、需要根据环境动态决策的任务，Agent 更有优势。

---

## 问题：Loop Engineering 是什么？

### 简短回答

Loop Engineering（循环工程）是一种围绕 AI Agent 设计**自动运行闭环**的工程方法。它把原来由人反复完成的“发现任务、输入下一条 Prompt、检查结果、纠偏、继续执行”变成一套可持续运行的系统：系统负责触发任务、调用 Agent、验证产出、保存状态并决定下一步，直到目标完成、触发停止条件，或者把异常升级给人。

因此，它不是简单地让 Agent 多执行几轮，也不是彻底移除人。人的角色会从“逐轮操作 Agent”转变为“设计目标、规则、完成条件和风险边界，并处理异常与最终验证”。

### 它处在 AI Agent 工程的哪一层？

文章把 Agent 工程能力的演进概括为四层，瓶颈逐渐从模型内部向外移动：

| 工程范式 | 主要解决的问题 | 典型工作 |
| --- | --- | --- |
| Prompt Engineering | 这一轮应该怎么说 | 设计指令、角色、示例和输出格式 |
| Context Engineering | 应该给模型看什么 | 按需提供代码、文档、工具结果和历史信息 |
| Harness Engineering | Agent 在什么环境中执行 | 提供工具、权限、沙箱、子 Agent 和上下文管理 |
| Loop Engineering | 谁来持续驱动下一轮 | 自动触发、分配任务、验证结果、记录状态并判断是否继续 |

四者不是替代关系。Prompt、Context 和 Harness 都可以成为 Loop 的内部能力；Loop 位于更外层，负责不断调度这些能力，使 Agent 从一次性交互工具变成围绕目标持续工作的系统。

### 一个 Loop 如何运行？

```text
定时或事件触发
      ↓
发现任务并读取目标、规则和历史状态
      ↓
分配给 Agent 执行
      ↓
测试、规则或独立 Agent 验证结果
      ↓
  ┌───┴─────────────┐
  ↓                 ↓
通过              未通过
  ↓                 ↓
持久化结果      反馈进入下一轮
  ↓                 ↓
达到目标？────── 否 ─┘
  ↓
停止并汇报；高风险或无法判断时升级给人
```

真正的 Loop 必须回答几个问题：由什么触发、每轮做什么、什么证据代表完成、失败后如何调整、状态保存在哪里、最多运行多久，以及什么情况必须停止并交给人。

### 核心组成：五大件加持久记忆

| 组成 | 作用 | 典型实现 |
| --- | --- | --- |
| 自动化触发 | 作为 Loop 的“心跳”，让任务不依赖人手动启动 | 定时任务、CI 失败、PR 合并或监控告警事件 |
| Worktree 隔离 | 让多个 Agent 并行工作且互不覆盖代码 | 每个任务使用独立工作目录和分支，完成后分别提交 PR |
| Skill | 固化项目规范、操作步骤和经验，使每轮不必从零理解项目 | 仓库中的规范文档、检查清单和可复用工作流 |
| Connector | 连接真实的外部系统，让 Agent 能读取状态并执行受控动作 | 通过 MCP 访问 Issue、CI、监控、代码托管和消息系统 |
| Sub-agent | 分离执行与检查，降低同一 Agent 对自己产出过于宽松的问题 | 一个 Agent 实现，另一个使用干净上下文审查 |
| 持久化记忆 | 跨会话保存已完成事项、阻塞点和下一步，避免每轮重新开始 | Markdown 状态文件、任务看板或数据库 |

除此之外，还必须定义完成条件、重试和预算上限、停止条件及人工升级规则。对于目标型 Loop，最好让测试、客观状态或独立检查者判断是否完成，而不是让执行 Agent 自己宣布成功。

### 实际例子：每天自动分诊并修复项目问题

文章给出了一个晨间分诊 Loop，大致流程如下：

1. 定时任务每天早晨启动分诊 Skill。
2. 系统读取前一天的 CI 失败、未关闭 Issue 和近期代码提交。
3. 值得处理的问题被筛选出来并写入状态文件。
4. 每个问题获得独立 Worktree，执行 Agent 在其中分析并起草修复。
5. 另一个审查 Agent 按项目规范和测试证据检查修改。
6. 审查通过后，Connector 创建 PR 并更新相关 Issue。
7. 无法安全处理或无法判断的问题进入人工待办箱。
8. 本轮结果全部写回状态文件，下一次运行从已有进度继续。

在这个流程中，人主要在设计 Loop 时定义规则，在异常出现时做判断，并对关键结果负责；不需要在每一步之间手动输入下一条 Prompt。

### 两种常见的 Loop

- **节奏型 Loop**：按照固定时间或事件反复执行，例如每天分诊 Issue、每隔一段时间检查一次 CI。
- **目标型 Loop**：围绕一个可验证目标持续迭代，例如修复 PR，直到测试和 Lint 全部通过。

一个最小 Loop 可以只有四项：触发节奏、执行任务、停止条件和结果汇报。例如持续检查 PR 的 CI：发现失败就读取日志、尝试修复并重新推送，所有检查变绿后停止；若超过轮数或成本上限，则交给人。

短时间轮询可以依赖当前会话；需要在关机后、夜间或跨多天持续运行的 Loop，则需要计划任务或云端运行机制。可以先从最小闭环开始，再按并行度和可信度要求逐步加入 Worktree、Skill、Connector 和审查 Agent。

### 适合的场景与主要价值

Loop Engineering 更适合重复性高、触发条件明确、结果可验证、停止条件可以定义的任务，例如：

- 定期检查并修复 CI；
- 自动分诊 Issue 或批量处理彼此独立的问题；
- 创建、审查和更新 PR；
- 从外部系统读取状态、更新工单并发送通知；
- 持续修改代码，直到测试、类型检查和 Lint 全部通过。

它的价值在于减少人反复提示和切换上下文的成本，支持多个隔离任务并行执行，并把项目知识、检查标准和异常处理方式沉淀成可复用资产。优化一条 Prompt 往往只影响当前一轮，优化一个 Loop 则能改善后续的每次运行。

### 风险和边界

Loop 延长了 Agent 的执行杠杆，也会放大错误，因此不能把“自动运行”理解为“无需负责”：

- **最终验证仍然属于人**：审查 Agent 声称完成只是一种判断，不是可靠证明；关键结果仍需测试证据和人工判断。
- **可能产生理解债**：系统生成代码越快，代码库中“已经存在但团队并不真正理解”的内容可能越多。
- **可能发生认知投降**：如果人不再独立分析，只因系统给出结论就接受结果，Loop 会削弱而不是放大人的判断力。
- **Token 与工具成本上升**：每次迭代和每个 Sub-agent 都会增加开销，独立复核应优先用于关键节点。
- **必须限制失控范围**：目标不清、结果不可验证或风险较高时，应设置最大轮数、时间、预算、权限边界和人工升级机制，避免无限重试或扩大操作范围。

Loop Engineering 改变的是人的参与位置，而不是责任归属。它更适合用在团队已经能够理解、验证和接管的工作上，不适合作为逃避理解复杂系统的手段。

### 面试回答模板

> Loop Engineering 是在 Agent 外层设计一个能够自动触发、发现和分配任务、验证结果、保存状态并决定是否继续的运行闭环。它是在 Prompt、Context 和 Harness Engineering 之上的进一步抽象：前面三者分别解决怎么提问、提供什么信息以及 Agent 如何执行，Loop 则解决谁来持续驱动下一轮。一个完整 Loop 通常包含自动化触发、Worktree、Skill、Connector、Sub-agent 和持久化记忆，同时必须有明确的完成条件、预算与停止规则、人工升级机制。它能提高自动化、并行度和知识复用，但不会消除人的验证责任，还可能带来理解债、认知依赖和更高的 Token 成本。

### 参考资料

- [小林 Coding：Loop Engineering](https://xiaolinnote.com/agent/engineering/loop-engineering.html)

---

## 问题：如何设计一个输出特定内容并持久化到关系型数据库的 Agent？

### 简短回答

我会把它设计成一个**有状态、可验证、幂等的任务系统**：LLM 只生成候选内容，确定性程序负责格式校验、业务校验、权限控制和事务写入。Agent 不直接持有数据库账号，也不能自由生成并执行 SQL。

### 核心流程

```text
接收任务 → 构建受控上下文 → Agent 生成结构化内容
                              ↓
                      Schema 与业务校验
                        ↓             ↓
                      不通过          通过
                        ↓             ↓
                  有界重试/人工审核  持久化服务
                                      ↓
                                事务写入数据库
```

### 关键设计

1. **结构化输出**：使用 JSON Schema 或类型化工具限制字段、枚举、长度和必填项；ID、时间和状态等系统字段由程序生成。
2. **分层验证**：先做 Schema 校验，再检查业务规则、敏感信息和数据引用；主观内容可以增加独立审查 Agent，但不能只依赖模型自评。
3. **受控持久化**：Agent 只能调用类似 `save_validated_content()` 的工具，由服务内部使用参数化 SQL、最小权限和数据库事务完成写入。
4. **幂等与重试**：为任务设置唯一的 `idempotency_key`，结果表对 `task_id` 建唯一约束。消息重复投递时返回已有结果，避免重复插入。
5. **状态与审计**：记录任务状态、尝试次数、Prompt 和 Schema 版本、验证结果、Token 消耗及错误信息，方便追踪和回放。
6. **人工门禁**：低风险且验证充分的内容可以自动生效；低置信度或涉及资金、安全、隐私和合规的内容先保存为草稿，等待人工批准。

### 数据表建议

- `agent_tasks`：任务输入、幂等键、状态、尝试次数和错误信息。
- `content_records`：最终业务内容，通过 `task_id` 与任务关联。
- `agent_runs`：每一轮模型、版本、输出、验证结果和成本记录。
- `outbox_events`：如果写入后还要可靠地通知其他系统，可通过事务型 Outbox 发送事件。

### 示例

例如 Agent 根据客服反馈生成 `category`、`priority`、`summary` 和 `suggested_action`。如果“重复扣款”被错误标记为低优先级，业务验证器会返回明确错误并要求 Agent 修正；最多重试 3 次。全部验证通过后，持久化服务在同一事务中写入分析结果、更新任务状态并记录审计信息。若置信度过低或涉及自动退款，则进入人工审核，而不是直接生效。

### 面试回答模板

> 我不会让 Agent 直接操作数据库，而是把它限制在结构化内容生成层。结果先经过 Schema 和业务规则验证，通过有界重试修正，再由独立持久化服务使用事务、唯一约束和幂等键写入关系型数据库。同时记录任务状态和审计信息，高风险或低置信度结果进入人工审核。这样既利用了 Agent 的灵活性，又把数据一致性和安全性控制在确定性代码中。

---

## 问题：Agent 大量调用工具造成上下文冗余，如何解决？

### 简短回答

核心原则是：**上下文是模型当前的工作内存，不是日志仓库。** 完整工具结果应保存在外部可审计存储中，模型上下文只保留当前目标、关键事实、最新结果和可追溯引用。

### 分层管理

| 层次 | 保存内容 |
| --- | --- |
| 当前上下文 | 目标、约束、当前计划、最近结果和待解决问题 |
| 结构化状态 | 已完成任务、关键决策、失败原因和下一步，持久化到数据库或状态文件 |
| Artifact 存储 | 完整日志、大型查询结果和文件，只在上下文中保留 `artifact_id/path/hash` |
| 审计轨迹 | 完整工具调用、参数和结果，供追踪与复盘，但不重复塞回模型 |

### 主要措施

1. **缩小工具输出**：工具默认返回摘要、关键字段、数量、分页游标和 Artifact 引用；支持过滤、字段选择和分页，避免返回整张表或完整日志。
2. **服务端预处理**：聚合、过滤、排序和连接等确定性操作尽量在工具或代码环境中完成，只把高信号结果交给模型。
3. **滚动压缩**：达到 Token 或工具调用阈值后生成结构化 Checkpoint，保留目标、决策、证据、失败方案和待办，再清除旧的原始 Tool Result。
4. **按需加载**：工具定义和外部材料需要时再加载；重复调用使用缓存，并通过工具名、参数和结果哈希检测重复查询。
5. **隔离子任务**：工具密集但相对独立的任务交给拥有干净上下文的子 Agent，主 Agent 只接收结论、证据和 Artifact 引用。
6. **限制循环**：同时设置最大轮数、工具调用数、Token、执行时间和单次输出大小；连续重复相同调用或多轮没有新增信息时停止并转人工。

### 示例

Agent 查询十万条订单时，工具不应把十万行全部返回，而是在执行环境中完成筛选和聚合，只返回：异常订单数量、最重要的 5 条记录、统计摘要、分页游标以及完整结果的 `artifact_id`。后续需要核查某条订单时，再根据 ID 精确读取。这样既减少 Token，也保留了完整证据。

### 面试回答模板

> 我不会把完整工具调用历史一直放在上下文中，而是区分工作上下文、结构化状态、原始 Artifact 和审计轨迹。工具从源头返回精简的结构化结果，大数据在工具侧过滤；旧轨迹定期压缩成带证据引用的 Checkpoint，独立子任务使用新上下文。同时设置 Token、调用次数和无进展检测，防止 Agent 在重复工具调用中耗尽上下文。

---

## 问题：Agent 调用工具发生数据库误删或错误扣款等高风险操作，如何解决？

### 简短回答

这类问题不能只依赖 Prompt 或 Agent 自我审核，而要建立纵深防御：

```text
生成操作提案 → 确定性策略校验 → 精确授权 → 受限执行
       → 执行后验证 → 审计与补偿/恢复
```

高风险操作应当**默认拒绝**。Agent 只能提出意图，真正执行必须经过独立的权限、规则和审批系统。

### 执行前：尽量阻止错误发生

1. **工具分级**：根据是否写入、是否可逆、权限范围和资金影响，将工具分为低、中、高风险；删除数据、扣款和生产发布属于高风险。
2. **最小权限**：不给 Agent 任意 SQL、管理员账号或通用支付接口，只暴露领域工具，例如 `delete_draft(id, expected_version)`、`prepare_payment(order_id, amount, currency)`。
3. **提案与执行分离**：先执行 Dry Run，展示目标对象、变更前后内容、影响行数、金额、币种、收款方和副作用，再请求确认。
4. **精确授权**：用户批准必须绑定具体参数和有效期；只要对象、金额或操作发生变化，就必须重新审批，执行前还要再次校验对象状态。高金额等场景可以要求双人复核，不能用一句“后续都允许”作为长期授权。
5. **确定性约束**：限制单次影响行数、单笔和每日金额、重试次数及调用频率；验证租户、对象状态、版本号和业务前置条件。

### 执行中与执行后：限制影响并验证结果

- **数据库操作**：优先软删除；使用事务、行锁或版本校验、行级权限和 `DELETE ... RETURNING`。如果实际影响行数不符合预期，立即回滚；禁止 Agent 使用无条件 `DELETE`、`TRUNCATE` 和 DDL。
- **支付操作**：使用 Payment Intent 或“授权—捕获”两阶段流程，所有写请求带幂等键；账本采用不可变记录和冲正分录，不能通过删除历史交易来修账。
- **跨系统一致性**：支付平台和本地数据库无法依靠单个数据库事务保证原子性，应使用状态机、Outbox/Saga、Webhook 和定期对账完成最终一致性与补偿。
- **结果核验**：执行后重新读取数据库状态或支付平台结果，核对对象、金额和状态；记录提案、审批人、实际参数、返回结果和关联任务 ID。
- **熔断机制**：连续失败、金额异常、删除数量异常或行为偏离预期时立即停止工具、撤销临时凭证并通知人工。

### 如果错误已经发生

1. **止损**：冻结写操作网关，暂停 Agent、任务队列和相关 Worker，关闭高风险工具或撤销凭证，取消或检查在途操作并禁止自动重试。
2. **确定范围**：保全日志，根据任务 ID、幂等键和审计记录确认影响的行、用户、订单及金额。
3. **数据库恢复**：未提交事务直接回滚；已经提交则从软删除、审计表、备份或 PITR 恢复。PITR 通常先恢复到隔离实例，再选择性取回误删数据，避免覆盖正常的新数据。
4. **资金修正**：尚未捕获的支付应取消授权；已经扣款的交易通过退款、撤销或补偿分录修正，并继续使用幂等键，不能直接删除财务记录。
5. **对账与复盘**：核对支付渠道和本地账本，通知受影响方，并把本次事故转化为新的权限规则、回归测试、金额限制和告警。

### 例子

删除数据库记录时，Agent 只能先调用 `preview_delete(order_id, expected_version)`。系统返回将删除的唯一记录和业务影响，用户确认后生成短时授权令牌，执行器在事务中校验租户、版本和影响行数必须等于 1，使用 `RETURNING` 留存旧值，再提交并重新读取结果。

扣款时，Agent 只能创建包含订单、用户、金额和币种的支付提案。策略引擎校验订单金额与限额，用户确认后先授权资金，独立执行器再捕获；请求使用幂等键并在执行后通过支付状态和 Webhook 对账。任一参数变化都使原批准失效。

### 面试回答模板

> 对高风险工具，我会采用默认拒绝和纵深防御。Agent 只能生成操作提案，确定性策略引擎负责权限、对象、影响范围和金额校验；删除与扣款采用 Dry Run、精确人工授权、最小权限工具、事务或两阶段执行、幂等键和执行后核验。同时保留完整审计、熔断和恢复机制。误删提交后通过软删除、审计或 PITR 恢复，错误扣款通过取消、退款或冲正修复，而不是让 Agent 再次自由操作系统。

### 参考资料

- [Anthropic：Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)
- [Anthropic：Code execution with MCP](https://www.anthropic.com/engineering/code-execution-with-mcp)
- [OpenAI：A practical guide to building agents](https://openai.com/business/guides-and-resources/a-practical-guide-to-building-ai-agents/)
- [OpenAI：Running Codex safely at OpenAI](https://openai.com/index/running-codex-safely/)
- [OWASP：Excessive Agency](https://genai.owasp.org/llmrisk/llm062025-excessive-agency/)
- [NIST：Security and Privacy Controls, SP 800-53](https://csrc.nist.gov/pubs/sp/800/53/r5/upd1/final)
- [Stripe：Idempotent requests](https://docs.stripe.com/api/idempotent_requests)
- [Stripe：Separate payment authorization and capture](https://docs.stripe.com/payments/place-a-hold-on-a-payment-method)
- [PostgreSQL：Row security policies](https://www.postgresql.org/docs/current/ddl-rowsecurity.html)
- [PostgreSQL：Point-in-time recovery](https://www.postgresql.org/docs/current/continuous-archiving.html)

---

## 问题：如何在等待用户审批时设计可维持暂停状态的 Checkpoint 快照？

### 简短回答

不要让进程、线程或数据库事务一直挂起，而应把 Agent 运行状态持久化为 `WAITING_APPROVAL`，然后释放 Worker。用户审批后，系统重新加载 Checkpoint、校验现实状态，并从确定的执行节点恢复。

这里保存的是**可重建的应用状态**，而不是模型的内部推理状态或 KV Cache。

### 状态机

```text
RUNNING
  → WAITING_APPROVAL
  → APPROVED / REJECTED / EXPIRED
  → RESUMING
  → EXECUTING
  → VERIFYING
  → COMPLETED / FAILED
```

### Checkpoint 应保存的内容

- 执行位置：当前Workflow节点、步骤ID、Agent轮次。
- 任务状态：待执行、执行中、成功、失败，以及失败原因。
- 关键上下文：用户目标、已确认参数、Planner生成的计划、步骤依赖。
- 中间结果：已生成的SQL、工具返回结果ID、数据文件路径、阶段性结论。
- 副作用记录：哪些数据库写入、消息发送、文件修改已经完成，避免恢复后重复执行。
- 版本信息：工作流版本、Prompt版本、模型与工具版本，防止新代码错误读取旧Checkpoint。
- 恢复控制信息：重试次数、超时时间、最后更新时间、下一步执行动作。

```text
checkpoint = {
    "workflow_id": "wf_1001",
    "current_step": "execute_sql",
    "status": "running",
    "context": {
        "query": "查询昨日成交金额",
        "sql": "SELECT SUM(gmv) FROM orders WHERE dt='2026-08-04'"
    },
    "completed_steps": ["intent", "plan", "generate_sql"],
    "result_refs": {
        "knowledge_result_id": "result_123"
    },
    "retry_count": 1,
    "workflow_version": "v2"
}
```
数据库连接、文件句柄、临时令牌等运行时资源不能放入快照，恢复时应重新获取短期凭证。

### 暂停流程

Agent 产生高风险 Tool Call 后，只生成操作提案，不立即执行。在同一个数据库事务中：

1. 写入不可变 Checkpoint。
2. 创建状态为 `PENDING` 的审批单。
3. 将 Run 更新为 `WAITING_APPROVAL`。
4. 写入用于发送审批通知的 Outbox 事件。

事务提交后，Worker 退出并释放资源。这样可以避免审批通知已经发出但快照尚未保存，也不会在等待期间长期占用数据库连接和锁。

### 恢复时候的核心流程
- 读取检查点：加载current_step、已完成步骤、上下文和中间结果引用。
- 校验兼容性：检查Workflow版本、Checkpoint Schema版本、Prompt和工具版本是否兼容。
- 获取执行锁：为workflow_id加锁或租约，防止原实例尚未停止，新实例又重复恢复。
- 核对真实状态：Checkpoint可能写着running，但外部操作可能已经成功，需要查询数据库、任务平台或第三方接口确认。
- 确定恢复位置：通常从最近一个已提交的原子步骤之后继续，而不是从进程中断的代码行继续。
- 恢复上下文：加载参数、计划、步骤结果和外部文件引用，并验证引用仍然存在。
- 处理重试：增加retry_count，检查是否超过重试上限，决定立即重试、延迟重试还是人工介入。
- 重新执行并写新Checkpoint：状态先更新为recovering，步骤成功后原子写入新的Checkpoint。

### 建议的数据表

- `agent_runs`：当前状态、最新 Checkpoint 指针和并发版本号。
- `agent_checkpoints`：不可变运行快照及父快照 ID。
- `approval_requests`：审批对象、参数哈希、状态和过期时间。
- `approval_decisions`：审批人、决定、意见和时间。
- `action_executions`：执行状态、幂等键和结果引用。
- `outbox_events`：可靠发送审批和恢复事件。

### 注意事项

- Checkpoint 必须在副作用执行之前保存。
- 恢复时某个节点可能从头运行，因此暂停前的操作必须幂等。
- 审批不能只绑定工具名，必须绑定具体参数和资源状态。
- 等待时间可能跨越代码发布，因此应保存 Workflow 版本，并提供状态迁移或重新规划机制。
- 拒绝、过期和取消必须是明确终态，不能被普通重试逻辑重新触发。

### 面试回答模板

> 我会把审批设计成持久化状态机，而不是挂起进程。Agent 在执行高风险工具前，将当前节点、业务状态、操作参数、参数哈希、前置条件和幂等键保存为不可变 Checkpoint，然后进入 `WAITING_APPROVAL` 并释放 Worker。审批必须绑定具体操作和有效期；恢复时重新校验权限和现实业务状态，只有审批仍然有效才幂等执行，否则重新申请审批。这样可以支持长时间暂停、进程重启和重复消息，同时避免重复执行与过期授权。

### 参考资料

- [OpenAI Agents SDK：Human-in-the-loop](https://openai.github.io/openai-agents-python/human_in_the_loop/)
- [LangGraph：Interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts)
- [LangGraph：Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)

---

## 问题：一个成熟的 Harness 分为几层？各层分别有什么作用？

### 简短回答

一个成熟的 Harness 大致包含 **六层**：

1. 上下文的精细化管理。
2. 工具系统的可控调用。
3. 任务执行的全局编排。
4. 记忆与状态的分层管理。
5. 独立的评估与观测体系。
6. 约束校验与失败恢复机制。

它们共同解决的是：让 Agent **看得准、做得对、能判断结果，并在失败后恢复**。

### 六层及其作用

| 层次 | 核心问题 | 主要作用 |
| --- | --- | --- |
| 1. 上下文精细化管理 | 模型这一轮应该看到什么？ | 固定角色、目标和成功标准；按需加载真正相关的信息；将固定规则、动态证据和中间结论分开组织，避免上下文腐化和错误结论相互污染。 |
| 2. 工具系统的可控调用 | 模型使用什么工具动手？ | 控制提供哪些工具、何时调用以及如何处理工具结果。工具应当少而明确，返回结果先过滤和提炼，再放入模型上下文。 |
| 3. 任务执行的全局编排 | 模型下一步应该做什么？ | 给 Agent 建立清晰的工作轨道，把多个单步能力串成长链路任务，并维护当前步骤、下一步、完成条件和失败重试。 |
| 4. 记忆与状态的分层管理 | 模型跨轮应该记住什么？ | 将状态外化到文件或数据库，并按生命周期区分任务进度、会话中间结果和长期记忆，使任务能够跨轮、跨天继续。 |
| 5. 独立的评估与观测体系 | 模型做得好不好，有没有尺子？ | 用 Eval 集衡量效果和做回归测试；用 Trace、日志和指标记录每一步决策、工具调用、结果及 Token 消耗，定位失败发生在哪里。 |
| 6. 约束校验与失败恢复机制 | 模型出错后能否恢复？ | 通过硬约束限制危险行为，在执行前后自动校验，并为限流、工具失败、预算耗尽等典型故障设计重试、降级、保存进度和继续执行方案。 |

### 三组职责

六层还可以按照职责归为三组：

- **输入侧**：第 1 层上下文管理 + 第 4 层记忆与状态管理，让模型看到正确的信息。
- **动作侧**：第 2 层工具系统 + 第 3 层执行编排，让模型以正确的顺序完成动作。
- **校验侧**：第 5 层评估观测 + 第 6 层约束恢复，判断是否做对，并在失败后兜底。

```text
输入侧：看得准
       ↓
动作侧：做得对
       ↓
校验侧：可验证、能恢复
```

第 1 层和第 4 层容易混淆：第 1 层管理的是“空间”，即当前一轮上下文包含什么以及如何排列；第 4 层管理的是“时间”，即上一轮状态如何传递到下一轮。

### PR Review Agent 示例

如果 Agent 每天扫描 GitHub PR、选出重要 PR、生成摘要并发送到 Slack，那么六层分别体现为：

1. **上下文层**：只加载当前 PR 的标题、描述、相关 diff、模块说明和 Review 规范。
2. **工具层**：提供 `gh`、文件读取、代码搜索和 Slack 发送工具，搜索结果先筛选再返回。
3. **编排层**：按照“拉取 PR → 分析 → 评分 → 选择前三名 → 生成摘要 → 发送 → 检查结果”的顺序执行。
4. **记忆层**：记录当天处理进度、已经审查的 PR 和用户长期关注偏好，避免重复处理。
5. **评估层**：使用标注过的历史 PR 作为 Eval 集，并记录 Trace、工具结果和 Token 成本。
6. **约束恢复层**：限制最多分析的 PR 数量和 Token 预算；发送前校验 Slack 频道；遇到 GitHub 限流时等待重试，发送失败时进入队列，预算不足时保存进度后继续。

### 如何利用六层定位问题

- Agent 总是漏掉必要信息：改进第 1 层。
- Agent 总是选错或滥用工具：改进第 2 层。
- Agent 执行步骤混乱：改进第 3 层。
- Agent 跨轮或跨天丢失进度：改进第 4 层。
- 无法判断 Agent 做得好不好：建设第 5 层。
- Agent 一遇到失败就整体中断：强化第 6 层。

这六层不是必须一次性全部搭完的固定流水线，而是一张持续改进路线图：通过 Eval 和 Trace 找到失败位置，再把修复沉淀到对应层，使 Harness 随运行经验不断增强。

### 面试回答模板

> 一个成熟的 Harness 可以分为六层：上下文管理决定模型这一轮看到什么；工具系统决定它能用什么动手；执行编排决定下一步做什么；记忆与状态管理保证跨轮连续；评估与观测提供效果度量和问题定位；约束与恢复负责限制危险行为并处理失败。六层又可以归为输入侧、动作侧和校验侧，整体目标是让 Agent 看得准、做得对、结果可验证，并且出错后能够恢复。

### 资料来源

- [小林面试笔记：Harness Engineering 是什么？AI Agent 越用越聪明的秘密](https://www.xiaolinnote.com/agent/engineering/harness-engineering.html)

---

## 问题：如果让你考虑一个 Agent 的能力，但不考虑模型，你会如何考虑？

### 简短回答

如果不考虑底层模型，我会把 Agent 看成一个由上下文、工具、状态、编排、反馈和安全机制组成的执行系统。评估的核心不是它接入了多少工具，也不是某次演示看起来多聪明，而是它能否在明确的任务边界、权限和资源约束下，稳定地把用户目标转化为经过验证的实际结果。

我会把它概括为四个问题：**目标是否可达、执行是否可控、结果是否闭环、过程是否可托付。**

### 先定义能力边界

评估前应先定义 Agent 的“能力契约”：它支持哪些任务，需要什么输入、数据和权限，可以产生哪些副作用，什么证据代表任务完成，什么情况必须停止并交给人。如果没有先明确这些边界，就无法区分“Agent 能力不足”和“任务本来就不在支持范围内”，也容易把一次偶然成功误判为稳定能力。

### 四个核心维度

| 维度 | 核心问题 | 主要考察内容 |
| --- | --- | --- |
| 目标可达性 | Agent 是否真的具备完成任务的条件？ | 能否获取完整、及时且可追溯的上下文；工具是否覆盖必要动作；权限和环境是否允许它改变真实状态。接入工具不等于具备能力，只有能正确选择工具、构造参数、理解返回结果并产生预期改变，才形成有效能力。 |
| 执行可控性 | Agent 能否把多步任务稳定推进？ | 是否能拆解任务、处理步骤依赖、维护当前状态、根据新结果重新规划，并在中断后从 Checkpoint 恢复。还要考察并发控制、幂等性、重试边界和停止条件，避免重复执行、无限循环或部分完成后失去进度。 |
| 结果闭环性 | Agent 能否证明目标已经完成？ | 工具返回成功不等于用户目标已完成。Agent 应通过测试、规则、状态回读或独立审查来验证最终结果，识别部分成功、过期数据和状态不一致。验证不通过时，它应能诊断原因、切换方案、有界重试、回滚或明确升级给人。 |
| 过程可托付性 | Agent 在自主执行时是否仍然安全、可解释和可接管？ | 是否采用最小权限、风险分级、预算和范围限制；删除、付款、发布和对外通信等高风险操作是否有精确审批。它还应该能让人知道做了什么、依据什么、如何取消或恢复，并在信息不足或超出授权时主动停止。 |

这四个维度存在明显的短板效应：没有必要的数据和工具，目标不可达；没有状态和编排，长链路任务无法稳定持续；没有外部验证，所谓完成不可信；没有权限和风险边界，自主执行就不可托付。

### 如何实际评估

评估时应固定模型版本、配置、权限、资源预算和任务集，通过多次运行对比不同 Agent 架构。测试用例应包含初始状态、目标结果、约束条件、允许的副作用和机器可验证的验收标准，同时覆盖正常流程、模糊指令、工具失败、权限不足、部分成功、环境变化和中断恢复等场景。

| 指标 | 衡量内容 |
| --- | --- |
| 最终验收通过率 | 用外部标准判断任务是否真正完成，而不是使用 Agent 的自评结果。 |
| 无需人工介入的完成率 | 在没有超越风险边界的前提下，能够自主完成的任务比例。 |
| 虚假完成率 | Agent 声称已完成，但实际没有通过验收的比例。这往往比可识别的普通失败更需要关注。 |
| 异常发现与失败恢复率 | 能否识别工具异常、部分执行和状态偏差，并通过重试、替代方案、回滚或恢复继续推进。 |
| 重复执行一致性 | 同一类任务多次运行时，结果是否稳定，是否会因重试造成重复写入或其他额外副作用。 |
| 越权和高风险事件率 | 是否发生未授权读写、超出影响范围、绕过审批或不必要的高风险动作。 |
| 时间与资源效率 | 在结果质量一致的前提下，比较完成时间、工具调用数、Token 和外部服务成本。 |

为了进一步隔离模型影响，可以在相同条件下移除记忆、结果验证、失败恢复或某个工具，再做消融实验。这样可以看出工具体系、工作流、状态管理、验证机制和安全策略分别带来了多少真实增益。

### 例子：代码修复 Agent

对一个代码修复 Agent，不考虑模型时，我会检查它是否能获取仓库、错误日志和项目规范，是否能在隔离环境中搜索代码、运行测试和编辑文件，是否会保存进度并根据测试结果继续修正。完成不能只依赖 Agent 宣布“已修复”，而应以相关测试通过、未引入新回归和改动范围符合要求为证据。工具失败时它应能诊断或恢复，连续无进展时应停止并说明阻塞，推送代码或创建 PR 等外部动作则必须符合授权边界。

### 常见误区

- **工具越多不代表能力越强**：工具重叠、语义模糊或返回内容不可验证，反而会增加错误选择和上下文污染。
- **一次成功不代表稳定能力**：需要在多次运行、环境扰动和异常场景下观察一致性。
- **工具调用成功不代表任务完成**：必须以真实业务状态和外部验收标准判断结果。
- **自主性越高不代表能力越强**：成熟 Agent 的重要能力之一，是在信息不足、风险过高或超出权限时知道停止并交给人。

### 面试回答模板

> 如果不考虑底层模型，我会把 Agent 看成一个目标驱动的执行系统，重点考察四个方面：目标是否可达，即上下文、工具和权限是否覆盖任务；执行是否可控，即能否编排多步任务、维护状态并从中断中恢复；结果是否闭环，即能否用外部证据验证完成并在失败后纠错；过程是否可托付，即是否具备权限、审批、审计和人工接管边界。实际评估时，我会在固定模型和环境下使用端到端任务集，重点看验收通过率、虚假完成率、失败恢复率、重复执行一致性、风险事件和成本效率。Agent 的强弱不在于“能做多少事”，而在于能否稳定完成、证明结果、失败可恢复，并知道什么时候不应该继续行动。

---

## 问题：在模型选型受限的场景下，如何针对不同模型优化 Agent 的全流程表现？

### 核心回答

在模型受限的情况下，我不会先脱离业务场景评测模型，也不会让不同模型直接复用同一套 Prompt 和工作流。我会先明确 Agent 服务什么场景、需要完成哪些任务以及全流程如何拆分，再针对流程中真正需要模型参与的节点测试不同模型，最后根据节点短板调整任务粒度、上下文、工具、流程和验证机制。

核心原则是：

> 先确定 Agent 在什么场景、经过哪些节点完成任务，再评估模型能否承担这些节点。模型能力越弱，单次承担的任务越小，工具空间越窄，流程约束和外部验证越强。

整体可以分为四步：

```text
明确业务场景和验收标准
    ↓
拆分 Agent 流程并定义节点
    ↓
针对每个节点评测模型并进行适配
    ↓
通过端到端评测和线上 Trace 持续优化
```

### 第一，先明确业务场景，再拆分 Agent 流程

首先要明确 Agent 服务哪些用户、处理哪些任务、输入和输出是什么、什么业务状态代表完成，以及允许的时延、成本、权限和风险范围。模型的部署方式、数据合规性、上下文上限和调用成本可以作为前置硬条件筛选，但此时还不需要对模型做详细能力评分。

然后按照真实业务处理过程拆分 Agent，而不是先假设模型必须具备哪些通用能力。一条典型 Agent 链路可以拆成：

```text
理解用户请求
    ↓
检索和组织上下文
    ↓
生成计划
    ↓
选择工具和参数
    ↓
执行工具
    ↓
理解工具结果
    ↓
验证和修正
    ↓
生成最终结果
```

每个节点都要定义清楚输入、输出、需要的上下文、可用工具、验收标准和失败处理，并进一步判断它应该由模型完成，还是由规则、模板、状态机、编译器或普通程序完成。确定性很强的节点没有必要交给模型，也不需要参与模型能力评测。

先拆场景和流程的原因是，模型能力只有放在具体节点上才有意义。例如，如果规划已经由固定状态机完成，就没有必要重点测试模型的开放式规划能力；如果某个节点只接收少量结构化信息，模型的超长上下文能力也不是主要指标。

### 第二，针对流程中的每个节点建立模型能力画像

流程确定后，再使用真实业务样本，在节点实际使用的 Prompt、上下文、工具 Schema 和输出约束下测试不同模型。需要评测的不是抽象的“模型智力”，而是模型能否稳定完成这个节点的具体职责：

- 输入理解节点：意图识别和参数抽取是否正确。
- 上下文节点：能否找到并使用关键信息，是否会遗漏重要约束。
- 规划节点：步骤是否完整，依赖关系和停止条件是否正确。
- 工具节点：工具选择和参数填写是否正确。
- 结果理解节点：能否识别工具错误、空结果和异常状态。
- 验证与修正节点：能否根据明确反馈进行局部修正，是否会虚假宣布完成。
- 风险判断节点：能否识别需要拒绝、审批或人工介入的操作。

最终得到的不是一个简单总分，而是一张“模型—流程节点”能力矩阵。例如：

```text
          意图抽取  查询规划  工具调用  SQL 修正  结果验证
模型 A       强        一般      强        一般      强
模型 B       强        强        一般      强        一般
```

每项能力都应重复测试，区分稳定短板和偶然波动。工具调用失败时，还要继续判断是选错工具、参数提取错误、Schema 太复杂、工具返回内容过长，还是模型没有理解错误信息。这样才能知道应该修改 Prompt、Context、工具设计还是 Workflow，而不是笼统地认为模型能力不足。

### 第三，根据模型短板调整 Agent，而不是强行依赖模型能力

优化的重点不是无限增加 Prompt，而是重新分配模型和程序的职责。

如果模型结构化输出不稳定，就缩短单次任务、简化 Schema，并使用程序做格式校验和有限次修复。

如果模型规划能力较弱，就不要让它一次生成完整复杂计划，而是使用固定状态机或计划模板，把任务拆成多个小步骤，每完成一步再决定下一步。

如果模型工具调用能力较弱，就只暴露当前节点需要的少量工具，把参数默认值、类型转换、权限检查和字段归一化放到程序中完成。

如果模型容易遗漏长上下文，就只提供当前步骤需要的信息，把历史状态保存在数据库或 Checkpoint 中，通过检索和摘要按需加载，而不是把全部对话反复塞入上下文。

如果模型生成 SQL 或代码的能力有限，就让模型生成受约束的中间结构，再由模板或编译器生成最终代码，并通过 AST 检查、静态分析、测试、`EXPLAIN` 和沙箱执行进行验证。

如果模型自我修正能力较弱，就不要依赖模型自己判断结果是否正确，而是使用外部 Validator 返回明确错误，例如：

```json
{
  "status": "failed",
  "error_type": "missing_time_filter",
  "field": "dt",
  "message": "查询缺少时间分区条件"
}
```

模型只需要修正失败部分，而不是重新生成整个结果。

因此，对不同能力的模型，Agent 结构也应不同：

```text
能力较强的模型：
较大的任务粒度
较灵活的规划
较多的可选工具
较少的流程约束

能力较弱的模型：
较小的任务粒度
固定状态机或计划模板
更少的工具
更强的程序校验
更严格的停止和重试条件
```

### 第四，用端到端业务指标判断优化是否有效

最终选择模型时，不能只比较模型单次调用价格，也不能只比较某个节点的准确率。

真正需要比较的是：

```text
任务成功率
安全违规率
虚假完成率
人工介入率
P95 时延
平均工具调用次数
平均重试次数
每个成功任务的总成本
```

例如一个小模型单次调用很便宜，但如果经常选错工具，需要多次重试和人工处理，那么它的实际成本可能高于更强的模型。

评测时应分成两个阶段。

第一阶段使用相同 Prompt、工具和 Workflow 测试不同模型，识别模型的原始能力差异。

第二阶段针对每个模型分别优化 Agent 配置，再比较它们在相同业务约束下能够达到的最佳端到端表现。

否则，拿已经深度适配的模型 A 和仍使用通用 Prompt 的模型 B 比较，是不公平的。

### 线上迭代方式

上线后需要保留完整 Trace，包括：

```text
用户输入
检索到的上下文
模型生成的计划
工具选择和参数
工具返回结果
验证结果
重试过程
最终结果
人工修改内容
```

然后按照以下闭环优化：

```text
发现线上失败
    ↓
定位失败节点
    ↓
提出单一修改假设
    ↓
加入回归测试集
    ↓
离线评测
    ↓
影子流量或小比例灰度
    ↓
验证成功率、时延和成本
```

每次只调整一个主要变量，例如 Prompt、工具 Schema、上下文策略或重试机制，才能判断收益来自哪里。

### 面试回答

> 在模型选型受限时，我不会先脱离业务场景评测模型，也不会让不同模型直接复用同一套 Prompt 和工作流。我会先明确 Agent 服务什么业务场景、处理哪些任务以及什么证据代表成功，再把流程拆成输入理解、上下文组织、规划、工具调用、执行、验证和恢复等节点，并判断哪些节点需要模型，哪些应该由确定性程序完成。

> 流程明确后，我才会在每个节点的真实 Prompt、上下文、工具 Schema 和输出约束下评测不同模型，建立“模型—流程节点”能力矩阵并定位具体瓶颈。模型越弱，我会把单次任务拆得越细，减少同时暴露的工具，使用更固定的 Workflow，并把参数校验、权限控制、代码检查和结果验证下沉到确定性程序中，而不是靠更长的 Prompt 弥补。

> 评测时先在统一配置下比较模型的原始能力，再分别完成适配，比较它们在相同安全、成功率和时延约束下的最佳端到端表现。最终选择的不是单项能力最强或单次调用最便宜的模型，而是每个成功任务总成本最低、整体业务表现最稳定的 Agent 配置。

---

## 问题：真实的业务场景下，一类 Agent 的需求通常是收敛的，你需要对 Agent 在特定场景上做优化，你有什么优化的思路吗？

### 简短回答

当业务需求已经收敛时，优化重点不应是让 Agent 变得更“通用”，而应是利用已知的任务分布缩小它的决策空间。我会先从真实请求中提炼稳定的任务类型、标准路径、异常分支和验收条件，再把确定性强的部分固化为 Workflow、规则和专用工具，只把意图理解、参数补全、方案选择和异常处理等不确定节点交给 Agent。

核心目标是把一个开放式 Agent 变成特定场景下**路径更短、输入更准、动作更少、结果可验证、失败可恢复**的执行系统。

### 第一步：建立真实的任务地图

优化不应从改 Prompt 开始，而应先分析线上请求和执行轨迹。需要明确高频任务、低频长尾任务、必要输入、期望输出、工具依赖、风险等级和常见失败。然后为每类任务定义能力契约：需要哪些参数，允许什么动作，什么状态代表成功，哪些情况必须拒绝、降级或交给人。

这一步的价值在于把“提高 Agent 效果”转化为可定位的具体问题，例如是意图识别错误、必要信息缺失、工具选择错误、执行中断，还是结果缺少验证。

### 场景化优化的主要方向

| 方向 | 优化思路 |
| --- | --- |
| 任务路由 | 用明确的任务分类器先识别场景，再进入对应的专用流程。简单任务直接使用规则或确定性程序，不必全部进入自由 Agent 循环。 |
| 上下文组装 | 根据当前任务动态加载必要的业务规则、用户状态、环境信息和成功案例，避免把所有知识和历史一次性塞入上下文。还要明确信息的时效性、来源和优先级。 |
| 流程编排 | 将高频主路径固化为状态机或 Workflow，明确步骤依赖、分支、重试和停止条件。Agent 只在确定性程序无法处理的节点做动态决策，减少路径漂移和无效循环。 |
| 工具设计 | 优先提供语义单一、参数类型明确的领域工具，而不是将底层通用接口直接暴露给 Agent。工具内部负责参数校验、权限、幂等、超时和错误归一，返回结构化且可操作的结果。 |
| 中间表示 | 让 Agent 先生成结构化的意图、任务计划或领域对象，再由确定性程序转换为真正动作。这比让 Agent 直接生成并执行自由文本更容易校验、回放和复用。 |
| 结果验证 | 为每类任务设置独立的验收器，使用 Schema、业务规则、状态回读、测试或人工审批判断是否完成。验证失败时返回明确的错误类型和可修正信息，而不是让 Agent 完全依靠自我评价。 |
| 稳定性与恢复 | 保存任务状态和 Checkpoint，对可重试、不可重试和需要人工处理的错误分类处理。同时限制最大轮数、工具调用数、执行时间和副作用范围。 |
| 性能与成本 | 对已归一化的输入、不变的业务知识和工具结果做缓存；只加载当前流程需要的工具和上下文；将简单请求路由到更短的执行链路，减少无效推理和重复调用。 |

### 用 Eval 驱动持续优化

场景化优化必须建立在可重放的任务集上。测试集应来自真实业务分布，同时包含高频正常请求、边界输入、工具异常、超时、权限不足和部分成功等情况。评估不只看最终回答质量，还要记录任务路由准确率、工具调用成功率、端到端验收通过率、虚假完成率、重试后恢复率、人工介入率、延迟和成本。

对每次失败进行分层归因：如果是任务识别错误，就优化路由；如果是信息不足，就优化上下文组装；如果是工具误用，就收窄工具语义和参数约束；如果是流程漂移，就增加状态机和停止条件；如果是虚假完成，就强化独立验收器。只有这样，线上轨迹才能持续转化为 Workflow、规则、工具和回归用例，而不是只积累更长的 Prompt。

### 优化顺序

实际落地时，应先建立权限、影响范围、人工审批和审计等安全底线，再按“**完成率、稳定性、成本和体验**”的顺序推进。先让高频主路径形成最小可验证闭环，再根据 Trace 和 Eval 逐步处理长尾分支，通常比一开始追求覆盖所有情况更有效。

### 面试回答模板

> 真实业务中一类 Agent 的任务通常是收敛的，所以我的优化思路不是继续扩大通用能力，而是先基于真实请求建立任务地图和验收标准，再把稳定的主路径固化为 Workflow 和状态机，只把不确定节点交给 Agent。同时通过任务路由、动态上下文、专用类型化工具、结构化中间表示、独立结果验证、Checkpoint 和有界重试来提高完成率和稳定性。最后使用来自真实业务分布的 Eval 集和 Trace 做分层归因，把高频失败持续沉淀成流程、规则、工具和回归用例。场景化优化的本质，是用更小的决策空间换取更高的端到端成功率、可验证性和可控性。

---

## 问题：如果让你负责建设一个生产级 Agent 系统，你会考虑哪些方面，如何设计？

### 核心回答

生产级 Agent 不能只是“模型 + Prompt + 工具”，而应设计成一个可验证、可恢复、可观测、可控制成本的任务系统。稳定路径由 Workflow 和状态机控制，模型只处理理解、规划和异常决策等不确定节点。

### 总体架构

```text
API / 事件入口
       ↓
任务契约、鉴权与风险路由
       ↓
Workflow / 状态机 / 任务队列
       ↓
模型适配与上下文组装 ↔ 受控工具网关
       ↓
验证器、人工审批与结果输出
       ↕
状态存储、Trace、监控、Eval 与审计
```

### 五个核心方面

| 方面 | 设计重点 |
| --- | --- |
| 业务契约 | 明确用户、任务边界、输入输出、完成证据和 SLO。对拒绝、降级和人工介入条件做显式定义。 |
| 编排与状态 | 用 Workflow 或状态机管理步骤、分支、超时和停止条件；长任务通过队列、Checkpoint 和持久化状态支持断点恢复。 |
| 模型、上下文与工具 | 通过模型适配层统一调用、超时和降级，上下文按需组装并将大型结果存入外部 Artifact。工具应语义单一、参数类型明确，并由网关统一处理权限、限流和结果压缩。 |
| 安全与可靠性 | 采用最小权限、凭证隔离、沙箱、高风险审批和全量审计。工具调用需要幂等、超时、有界重试、熔断和补偿机制，避免重复副作用和故障扩散。 |
| 验证与运营 | 使用测试、Schema、业务规则和状态回读验证结果，不依赖模型自评。通过 Trace 和 Eval 监控成功率、虚假完成率、P95 时延、人工介入率和每个成功任务的成本。 |

落地时应先让一条高频业务主路径形成最小可验证闭环，再补充异常分支、故障注入、容灾和容量测试。Prompt、模型、工具 Schema、Workflow 和验证器都要独立版本化，通过影子流量或小比例灰度发布，出现回退时能快速回滚。

### 面试回答

> 如果让我负责生产级 Agent 系统，我会先定义业务契约和可执行的验收标准，再用 Workflow、状态机和任务队列管理执行过程。模型只负责不确定决策，所有外部动作通过受控工具网关执行，并使用最小权限、幂等、超时、重试、审批和状态回读保证安全与可靠。系统需要保存状态和全链路 Trace，使用 Eval、灰度和回滚机制持续优化端到端成功率、时延和成本。

---

## API超时和报错你会如何解决？

报错先区分为什么报错，如果是因为没登录、参数错误等，那么不重试而是保证登陆或者参数正确。如果是服务端异常或者429之类的报错，那么就指数退避重试。
超时的话，重试要保证幂等性，防止操作被重复执行。

```text
调用 API
   ↓
设置连接、读取和总超时
   ↓
成功 → 返回结果
   ↓
失败
├── 400/401/403/业务错误 → 不重试
├── 429/502/503/网络异常 → 有限重试
└── 超时 → 先判断接口是否幂等，再决定重试
             ↓
       仍然失败
       → 降级、熔断、告警
```